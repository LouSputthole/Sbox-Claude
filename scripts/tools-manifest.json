{
  "toolCount": 228,
  "tools": [
    {
      "module": "assets",
      "name": "copy_asset_with_dependencies",
      "description": "Copy a project asset and its full dependency closure (via Asset.GetReferences(deep:true)) into a target directory, preserving relative path structure so material references to textures keep resolving. SHADOW GUARD: refuses to write under core engine trees (models/citizen, models/dev, materials/dev, materials/default) -- copying there triggers an infinite asset-recompile loop (BRIDGE_GOTCHAS #5). Cloud/procedural/transient assets are skipped with a reason. Returns { copied:[{from,to}], skipped:[{path,reason}], count, note }.",
      "params": [
        {
          "name": "sourcePath",
          "description": "Absolute path OR project-relative path of the source asset (e.g. 'models/props/crate.vmdl'). Use search_assets to find the exact path.",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetDir",
          "description": "Project-relative destination directory (e.g. 'Assets/library'). Defaults to 'Assets/library'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "overwrite",
          "description": "Overwrite existing files at the destination. Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "assets",
      "name": "get_asset_info",
      "description": "Get detailed metadata about a specific asset — type, path, tags, package source",
      "params": [
        {
          "name": "path",
          "description": "Asset path (e.g. 'models/citizen/citizen.vmdl')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "assets",
      "name": "install_asset",
      "description": "Install a community asset package into the project by its ident (e.g. 'facepunch.flatgrass'). Adds it as a project dependency. Returns { installed, ident, name, path, relativePath, restartRecommended, note } — pass the returned path to spawn_model / assign_model. CAUTION: if the package is a code LIBRARY (adds a PackageReference), trigger_hotload will NOT surface its types — call restart_editor so the new package compiles into the project",
      "params": [
        {
          "name": "ident",
          "description": "Package identifier (e.g. 'facepunch.flatgrass', 'author.package_name')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "assets",
      "name": "list_asset_library",
      "description": "List assets visible to the editor's AssetSystem — project assets plus mounted engine/installed-package content (it does NOT query the remote sbox.game library; use install_asset to pull in new packages). Returns { count, assets:[{ name, path, relativePath, assetType }] } — pass a returned path to spawn_model, assign_model, or get_asset_info",
      "params": [
        {
          "name": "query",
          "description": "Search term, case-insensitive substring match against asset NAMES",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "type",
          "description": "Asset type filter, substring-matched against the asset's type (e.g. 'model', 'material', 'sound')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxResults",
          "description": "Maximum results. Defaults to 200",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "assets",
      "name": "search_assets",
      "description": "Search for assets in the project by name, type, or keyword. Returns models, materials, sounds, textures, prefabs, etc.",
      "params": [
        {
          "name": "query",
          "description": "Search term to match against asset name or path",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "type",
          "description": "Asset type filter (e.g. 'model', 'material', 'sound', 'texture', 'prefab')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxResults",
          "description": "Maximum results to return. Defaults to 50",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "audio",
      "name": "assign_sound",
      "description": "Attach a sound event to a GameObject via SoundPointComponent. Creates the component if needed. Returns { assigned, id, sound, soundLoaded, playOnStart } — soundLoaded:false means the .sound path did not resolve (the component is still added with no event; verify the path with list_sounds)",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "sound",
          "description": "Sound event path (e.g. 'sounds/ambient_wind.sound')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "playOnStart",
          "description": "If true, the handler calls StartSound() immediately, so the sound starts playing right away (audible in the editor)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "audio",
      "name": "create_sound_event",
      "description": "Create a .sound event file wired to a source .vsnd. Returns { created, path, soundReferenced, note } (path is project-relative); errors if the file already exists. Preview the result with play_sound_preview or attach it to an object with assign_sound. Note: .sound events have no loop flag — looping lives on the SoundPointComponent that plays the event",
      "params": [
        {
          "name": "path",
          "description": "Project-relative path for the sound event file (e.g. 'sounds/footstep.sound'; '.sound' appended if missing)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "sound",
          "description": "Path to the source sound asset (.vsnd) the event plays. Omit to create an empty event and wire it later",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "volume",
          "description": "Volume multiplier (0-1). Defaults to 1.0",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "pitch",
          "description": "Pitch multiplier. Defaults to 1.0",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "maxDistance",
          "description": "Maximum audible distance in units (sets Distance + enables DistanceAttenuation). Omit for the engine default",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "audio",
      "name": "list_sounds",
      "description": "List the project's .sound event files (recursive scan of the project root for *.sound). Returns { count, sounds } — project-relative paths ready to pass to assign_sound, play_sound_preview, or add_lipsync. NOTE: the current handler returns every match (filter/maxResults are not applied) and only covers .sound files in the project tree — use search_assets type='sound' for other sound assets",
      "params": [
        {
          "name": "filter",
          "description": "Search filter for sound name or path (currently not applied by the handler — all .sound files are returned)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxResults",
          "description": "Maximum results. Defaults to 50 (currently not applied by the handler)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "audio",
      "name": "play_sound_preview",
      "description": "Play a sound in the editor for testing without entering play mode. Returns { playing, sound, volume }. Fire-and-forget via Sound.Play — there is no stop control, and the volume param is echoed back but not currently applied to playback",
      "params": [
        {
          "name": "sound",
          "description": "Sound event or asset path to preview",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "volume",
          "description": "Preview volume (0-1). Defaults to 1.0 (echoed in the response but not currently applied to playback)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "add_lipsync",
      "description": "Add + wire s&box's LipSync component (new in the 2026-07 engine update): drives a SkinnedModelRenderer's facial morphs (visemes) from a playing sound. Wires the renderer on the target GameObject (or rendererId) and, if soundEvent is given, a SoundPointComponent bound to that .sound asset (see list_sounds / create_sound_event). Morphs animate at RUNTIME while the sound plays — verify in play mode with capture_view, not the static editor pose.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to add LipSync to (e.g. a spawn_citizen)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "rendererId",
          "description": "GUID of a different GameObject holding the SkinnedModelRenderer (default: same as id)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "soundEvent",
          "description": "Path to a .sound event to speak, e.g. 'sounds/dialogue/hello.sound' — creates/reuses a SoundPointComponent. Omit to wire an existing sound component on the GameObject (or wire audio later).",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "playOnStart",
          "description": "Play the sound on scene start (default true; only with soundEvent)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "volume",
          "description": "SoundPointComponent volume (only with soundEvent)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "morphScale",
          "description": "LipSync.MorphScale — exaggerate/dampen mouth movement (engine default when omitted)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "morphSmoothTime",
          "description": "LipSync.MorphSmoothTime — morph smoothing seconds (engine default when omitted)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "add_ragdoll",
      "description": "Add ModelPhysics to a skinned model so it becomes a ragdoll (physics-driven bones). NOTE: the ragdoll only flops in PLAY mode — it won't move in the static editor view, so this one is verified structurally, not by screenshot. Returns { ragdoll, note, gameObject } — check gameObject.components for ModelPhysics, then verify at runtime with start_play + capture_view.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject with a SkinnedModelRenderer",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "motionEnabled",
          "description": "Whether physics bodies start with motion enabled",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "dress_citizen",
      "description": "Dress a spawned Citizen (or any GameObject with a SkinnedModelRenderer) by applying .clothing resources. Pass an array of clothing resource paths; they're loaded, added to a ClothingContainer, and applied to the body. Returns which paths applied vs were not found.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the Citizen GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "clothing",
          "description": "Clothing resource paths, e.g. ['models/citizen_clothes/jacket/jacket.clothing']",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "tint",
          "description": "ClothingContainer tint position 0-1 (skin/clothing colour variation)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "equip_model",
      "description": "Attach a prop model (weapon, hat, tool) to a Citizen's bone or attachment point — the prop is parented so it follows that point. Tries the point as an attachment (hand_R, hand_L, eyes, hat) then as a bone. Great for arming NPCs or adding accessories.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject with a SkinnedModelRenderer",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "model",
          "description": "Prop model path, e.g. 'models/dev/box.vmdl'",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "point",
          "description": "Attachment or bone name (default 'hand_R'; try hand_L, eyes, hat, head)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "offset",
          "description": "Local position offset from the point",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "Local rotation offset",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "tint",
          "description": "Prop tint colour",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "name",
          "description": "Name for the prop GameObject",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "list_animations",
      "description": "List the animation sequences available on a GameObject's SkinnedModelRenderer (a spawned Citizen or animated model), plus whether it's driven by an AnimationGraph. Call this before play_animation or set_animgraph_param to see valid names.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject with a SkinnedModelRenderer",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "play_animation",
      "description": "Play a named animation sequence on a GameObject's SkinnedModelRenderer (sets the Sequence). Best for models with raw sequences; for AnimationGraph characters (Citizen) prefer set_animgraph_param. The renderer needs PlayAnimationsInEditorScene = true to animate in-editor — then screenshot to verify. Use list_animations for valid names.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject with a SkinnedModelRenderer",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "animation",
          "description": "Sequence name to play (see list_animations)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "looping",
          "description": "Loop the sequence (default: the model's setting)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "speed",
          "description": "Playback rate multiplier (default 1)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "pose_citizen",
      "description": "Pose a Citizen by setting CitizenAnimationHelper params (enables PlayAnimationsInEditorScene so the pose shows in-editor). Set holdType (None/Pistol/Rifle/Shotgun/HoldItem/Punch/Swing), moveStyle (Auto/Walk/Run), specialMove, sitting (bool), and/or duckLevel (0-1). Returns { posed, changed:[...], gameObject } — changed lists which helper params were applied; take_screenshot to verify the pose.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the Citizen GameObject (must have a CitizenAnimationHelper)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "holdType",
          "description": "Hold pose, e.g. None, Pistol, Rifle, Shotgun, HoldItem",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "moveStyle",
          "description": "Movement style, e.g. Auto, Walk, Run",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "specialMove",
          "description": "Special move style, e.g. None, LedgeGrab, Roll",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "sitting",
          "description": "Sit the character (IsSitting)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "duckLevel",
          "description": "Crouch amount, 0-1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "set_animgraph_param",
      "description": "Set an AnimationGraph parameter on a GameObject's SkinnedModelRenderer (calls Set). This drives Citizen/animgraph motion — e.g. 'move_x'/'move_y' (float), 'b_grounded'/'b_ducked' (bool), or a Vector3. Pose previews in-editor when PlayAnimationsInEditorScene is on; screenshot to verify. Param names are defined by the model's animation graph — use list_animations to check whether the model is animgraph-driven. Returns { set, param, kind, note } — kind is the type actually applied (float/int/bool/vector).",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject with a SkinnedModelRenderer",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "param",
          "description": "Animgraph parameter name, e.g. 'move_x', 'b_grounded'",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "value",
          "description": "Value: number (float), boolean, or a vector as {x,y,z} OR a comma string \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "type",
          "description": "Force the parameter type (default: inferred from value)",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "float",
              "int",
              "bool",
              "vector"
            ]
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "set_bodygroup",
      "description": "Show/hide a bodygroup on a SkinnedModelRenderer (e.g. hide hands when holding a tool, swap head variants). Provide value (int index) or choice (string name). Returns { set, bodygroup } on success (errors if the object has no SkinnedModelRenderer or neither value nor choice is given); take_screenshot to verify the visual change.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject with a SkinnedModelRenderer",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "name",
          "description": "Bodygroup name",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "value",
          "description": "Bodygroup choice index",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "choice",
          "description": "Bodygroup choice by name (alternative to value)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "set_expression",
      "description": "Set a facial morph (blendshape) on a skinned model — e.g. smile, frown, blink. Call with NO morph to list the model's available morph names (returned as availableMorphs). weight is typically 0-1. Returns { set, morph, weight, availableMorphs } — availableMorphs is included in every response, so a wrong name can be corrected without an extra listing call.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject with a SkinnedModelRenderer",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "morph",
          "description": "Morph/blendshape name (omit to list available morphs)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "weight",
          "description": "Morph weight, typically 0-1 (default 1)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "set_look_at",
      "description": "Aim a Citizen's gaze. Pass target {x,y,z} (spawns a LookTarget) or targetId (existing GameObject) and the head/eyes track it. Pass enabled:false to turn gaze tracking off. Tune eyesWeight/headWeight/bodyWeight (0-1).",
      "params": [
        {
          "name": "id",
          "description": "GUID of the Citizen (must have a CitizenAnimationHelper)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "target",
          "description": "World point to look at",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a GameObject to look at (overrides target)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "enabled",
          "description": "false to disable gaze tracking",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "eyesWeight",
          "description": "Eye look weight 0-1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "headWeight",
          "description": "Head turn weight 0-1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "bodyWeight",
          "description": "Body turn weight 0-1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "spawn_citizen",
      "description": "Spawn an animated Citizen character: a SkinnedModelRenderer with the Citizen model, plus (by default) a CitizenAnimationHelper so it idles. PlayAnimationsInEditorScene is enabled so the idle pose shows in the editor view (screenshot-verifiable). Returns { created, hasAnimator, gameObject:{ id, name, position, components, ... } } — pass gameObject.id to the follow-ups: dress_citizen, pose_citizen, set_expression, equip_model.",
      "params": [
        {
          "name": "name",
          "description": "GameObject name (default 'Citizen')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "model",
          "description": "Override the skinned model (default 'models/citizen/citizen.vmdl')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "World rotation",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "scale",
          "description": "World scale (default 1,1,1)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "tint",
          "description": "Body tint colour",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "animator",
          "description": "Add a CitizenAnimationHelper for idle/pose (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "holdType",
          "description": "Initial hold pose, e.g. None, Pistol, Rifle, Shotgun, HoldItem, Punch, Swing",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "moveStyle",
          "description": "Movement style, e.g. Auto, Walk, Run",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "parentId",
          "description": "GUID of a parent GameObject",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "characters",
      "name": "spawn_model",
      "description": "Spawn a GameObject with a ModelRenderer showing a model — the quick way to place a prop. Pass an engine/project model path (e.g. 'models/citizen/citizen.vmdl', 'models/dev/box.vmdl'). Cloud (sbox.game) models must be installed first via install_asset, then pass the resulting path. Add physics separately with add_collider/add_physics if needed.",
      "params": [
        {
          "name": "model",
          "description": "Model path, e.g. 'models/dev/box.vmdl' or an installed model path",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "name",
          "description": "GameObject name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "World rotation",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "scale",
          "description": "World scale (default 1,1,1)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "tint",
          "description": "Model tint colour",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "parentId",
          "description": "GUID of a parent GameObject",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "cinematics",
      "name": "create_cutscene_director",
      "description": "Generate a hand-authored cutscene player component that needs NO .movie asset — the zero-asset alternative to the MovieMaker family (add_movie_player/play_movie, which play keyframed .movie clips authored in the editor's Movie Maker dock). You author the shots directly in the inspector as parallel lists: ShotPositions (Vector3), ShotAngles (pitch/yaw/roll — Angles, not raw quaternions), ShotHoldSeconds, ShotBlendSeconds, and an optional per-shot ShotLookAt GameObject (aim at a target instead of using ShotAngles). At runtime it takes over the main camera (Scene.Camera) in OnPreRender ONLY while playing — smoothstep-eased Vector3.Lerp + Rotation.Slerp between shots — captures the camera's prior transform and restores it exactly when finished (same un-apply discipline as create_camera_shake). Play from any game code via the static <Name>.Play() (first director) or <Name>.Play(\"name\") (matches CutsceneName); subscribe the static <Name>.OnCutsceneFinished, or gate game logic on the static <Name>.IsCutscenePlaying. LockInput freezes player input each frame via Input.ClearActions() while still reading the SkipAction press first so the cutscene stays skippable. Optional letterbox generates a razor_lint-safe black-bars overlay panel (host under a ScreenPanel) shown while IsCutscenePlaying. LOCAL-only (each client renders its own view) — trigger inside an [Rpc.Broadcast] for all clients. Attach to ANY GameObject; it drives the camera itself and does not need to sit on the camera. Returns { created, path, className, skipAction, lockInput, letterbox, nextSteps } — path is the generated .cs (letterbox lists the overlay files when enabled). Follow with trigger_hotload, then get_compile_errors, then attach via add_component_with_properties (component=className).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'CutsceneDirector'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated files. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "skipAction",
          "description": "Input action that ends the cutscene early (read before input is cleared, so a locked cutscene is still skippable). Sanitized to a safe token. Empty = not skippable. Defaults to 'jump'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "lockInput",
          "description": "Freeze player input during playback via Input.ClearActions() each frame. Defaults to true",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "letterbox",
          "description": "Also generate a razor_lint-safe letterbox overlay (two black bars, shown while IsCutscenePlaying — host it under a ScreenPanel). Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "cinematics",
      "name": "create_dialogue_system",
      "description": "Generate an NPC/story dialogue system — a sealed state+data Component paired with a razor_lint-safe Razor HUD panel. Lines are authored in the inspector as a List<string> using the 'Speaker: text' convention (the part before the first colon is the speaker). The generated HUD panel binds to <Name>.Current automatically (no wiring) and renders the current line with a TimeSince-driven typewriter reveal at CharsPerSecond, folding the visible substring into BuildHash so it re-renders as characters appear. Press the AdvanceAction once to snap the whole line into view instantly, again to move to the next line; dismissing the last line ends the conversation. Start from any game code via the static <Name>.StartDialogue(string[] lines) or set Lines and call the instance Begin(). Static events for hooks: OnLineShown(index, speaker) — pair with add_lipsync to drive facial morphs / audio per line — and OnDialogueFinished when the conversation ends. Pairs with create_interactable to trigger dialogue on use. LOCAL-only (per-client HUD) — call StartDialogue inside an [Rpc.Broadcast] if every client should see it. Attach the panel under a ScreenPanel (add_screen_panel) so the HUD renders. Returns { created, path, className, charsPerSecond, advanceAction, panel, nextSteps } — panel lists the generated '<Name>Panel' HUD files. Follow with trigger_hotload, then get_compile_errors, then attach both components via add_component_with_properties.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated dialogue component (the HUD panel is generated as '<Name>Panel'). Defaults to 'DialogueSystem'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated files. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "charsPerSecond",
          "description": "Typewriter reveal speed in characters per second (clamped to >= 1). Defaults to 40",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "advanceAction",
          "description": "Input action that completes the reveal / advances to the next line. Sanitized to a safe token. Defaults to 'use'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "components",
      "name": "add_component_with_properties",
      "description": "Add a component to a GameObject and configure its properties in one call (properties PERSIST through save+reload). Use list_available_components to find valid types. Returns appliedProperties + failedProperties so you can see exactly what stuck",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name (e.g. 'ModelRenderer', 'Rigidbody', 'BoxCollider')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "properties",
          "description": "Key-value map of property names to values, each auto-converted to the property's real type. Primitives '5'/true; Color/Vector3 as comma strings '1,0,0,1'; enum member names; ASSET refs as a path ('Model':'models/dev/box.vmdl', 'MaterialOverride':'materials/x.vmat'); GameObject/Component refs as a target GUID. Best-effort per key — failures are reported in failedProperties, not silently dropped",
          "optional": true,
          "type": {
            "kind": "any"
          }
        }
      ]
    },
    {
      "module": "components",
      "name": "get_all_properties",
      "description": "Dump all public properties of every component on a GameObject. Returns { id, components } where each entry is { component, properties: [{ name, type, value }] } — values are stringified (unreadable ones show '<error>'). Use the exact component/property names it reports with set_property or get_property; can be large on component-heavy objects.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "components",
      "name": "get_property",
      "description": "Read a single property value from a component on a GameObject",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name (e.g. 'ModelRenderer', 'PlayerController')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "Property name to read",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "components",
      "name": "invoke_method",
      "description": "Call a public method BY NAME on a component of a live scene GameObject, passing ARGUMENTS. The with-args sibling of invoke_button (which only calls parameterless [Button]/methods on a scene component). Finds a public method matching name + arg-count, coerces each JSON arg to the parameter type (primitives/enums; Color/Vector3 as comma strings '1,0,0,1'; asset refs as a path; GameObject/Component refs as a target GUID), invokes it, and returns the method's return value as a string (null for void). Returns success=false with a clear error on resolve/coerce/throw",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name to target (e.g. 'Health', 'PlayerController'). Omit to search all components on the object for a method matching name + arg-count",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "method",
          "description": "Name of the public method to call (e.g. 'TakeDamage', 'AddGold')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "args",
          "description": "Ordered arguments, each coerced to the matching parameter's type. Numbers/bools/strings pass through; Color/Vector3/Rotation as comma strings '1,0,0,1'; enum member names; ASSET refs as a path ('models/dev/box.vmdl'); GameObject/Component refs as a target GUID. Omit (or []) for a no-arg method",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "any"
            }
          }
        }
      ]
    },
    {
      "module": "components",
      "name": "list_available_components",
      "description": "List all instantiable component types in the TypeLibrary — built-in AND your project's custom components (abstract types excluded); filter does a substring match on the type name. Returns { count, components } with { name, title, description, fullName } per type, sorted by name — the unfiltered list is LARGE, so pass filter. Use the returned name with add_component_with_properties, and describe_type for a type's full property list.",
      "params": [
        {
          "name": "filter",
          "description": "Search filter — matches against component name and title",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "category",
          "description": "Filter by category/group (e.g. 'Rendering', 'Physics', 'Audio')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "debugdraw",
      "name": "debug_clear",
      "description": "Remove ALL debug-draw primitives at once by destroying the debug holder for the current scene (edit or play) — there is no way to remove a single shape. Call before redrawing a fresh frame of debug shapes. Returns { cleared, removed } where removed is how many primitives were destroyed.",
      "params": []
    },
    {
      "module": "debugdraw",
      "name": "debug_draw_box",
      "description": "Draw a wireframe debug box centered at a point. Renders in editor and play. Ideal for visualizing a trigger_zone's bounds or a physics_overlap box volume. Accumulates until debug_clear. Returns { drawn: 'box', count, mode } — count is the total accumulated primitives, mode is 'edit' or 'play' (edit-mode gizmos are NOT in take_screenshot; use capture_view in play mode).",
      "params": [
        {
          "name": "center",
          "description": "Box center, world-space \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "size",
          "description": "Full size \"x,y,z\" in units (default \"32,32,32\")",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "color",
          "description": "Color as \"r,g,b\" or \"r,g,b,a\" floats 0–1 (default varies per shape)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "thickness",
          "description": "Line thickness for edit-mode gizmos (default 2)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "debugdraw",
      "name": "debug_draw_line",
      "description": "Draw a debug line between two world points. Renders in the editor viewport (Gizmo) AND in play mode (DebugOverlay), accumulating until debug_clear. NB: edit-mode gizmos show in the live editor but NOT in take_screenshot/screenshot_from — use capture_view in play mode to see debug draws through the bridge.",
      "params": [
        {
          "name": "from",
          "description": "Start point, world-space \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "to",
          "description": "End point, world-space \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "color",
          "description": "Color as \"r,g,b\" or \"r,g,b,a\" floats 0–1 (default varies per shape)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "thickness",
          "description": "Line thickness for edit-mode gizmos (default 2)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "debugdraw",
      "name": "debug_draw_ray",
      "description": "Draw a debug ray (drawn as an arrow) from an origin along a direction for a given length. Renders in editor and play. Ideal for visualizing a raycast result or a facing/normal direction. Accumulates until debug_clear. Returns { drawn: 'ray', count, mode } — count is the total accumulated primitives, mode is 'edit' or 'play' (edit-mode gizmos are NOT in take_screenshot; use capture_view in play mode).",
      "params": [
        {
          "name": "origin",
          "description": "Ray origin, world-space \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "direction",
          "description": "Direction vector \"x,y,z\" (normalized internally)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "length",
          "description": "Ray length in units (default 64)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "color",
          "description": "Color as \"r,g,b\" or \"r,g,b,a\" floats 0–1 (default varies per shape)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "thickness",
          "description": "Line thickness for edit-mode gizmos (default 2)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "debugdraw",
      "name": "debug_draw_sphere",
      "description": "Draw a wireframe debug sphere at a point. Renders in editor and play. Ideal for visualizing a physics_overlap radius or an NPC's hearing/sight range. Accumulates until debug_clear. Returns { drawn: 'sphere', count, mode } — count is the total accumulated primitives, mode is 'edit' or 'play' (edit-mode gizmos are NOT in take_screenshot; use capture_view in play mode).",
      "params": [
        {
          "name": "center",
          "description": "Sphere center, world-space \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "radius",
          "description": "Radius in units (default 32)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "color",
          "description": "Color as \"r,g,b\" or \"r,g,b,a\" floats 0–1 (default varies per shape)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "thickness",
          "description": "Line thickness for edit-mode gizmos (default 2)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "debugviz",
      "name": "get_profiler_stats",
      "description": "Read live engine performance counters (ported from Unity's get_profiler_stats): FPS, frame ms, GPU ms, bytes allocated, process memory, exception count, and per-category timings (update/physics/ui/render/network/gcPause) averaged over `frames`. Reads Sandbox.Diagnostics.PerformanceStats. Most meaningful during play (call start_play first) but populated in the editor too. Read-only.",
      "params": [
        {
          "name": "frames",
          "description": "Averaging window (frames) for the per-category timings. Default 60",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "debugviz",
      "name": "set_time_scale",
      "description": "Set the running game's time scale DURING PLAY MODE (ported from Unity's playtest_set_time_scale). 0 = pause, 1 = normal, 0.1 = slow-mo to watch a fast interaction frame-by-frame, 2+ = fast-forward idle/economy ticks. Requires start_play first — the edit scene doesn't tick, so this no-ops outside play mode and returns an error. Sets Game.ActiveScene.TimeScale (clamped to 0–100). Returns the applied and previous values.",
      "params": [
        {
          "name": "scale",
          "description": "Time multiplier: 0 = pause, 1 = normal speed, 0.1 = slow-mo, 2 = double speed. Clamped to 0–100",
          "optional": false,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "capture_view",
      "description": "Capture a PNG of the scene from a camera — and crucially this WORKS IN PLAY MODE, capturing the RUNNING game (via CameraComponent.RenderToBitmap, unlike take_screenshot/screenshot_from which are edit-only). With no args it renders the live main camera = the player's POV (incl. HUD). Pass position {x,y,z} (+ lookAt or rotation) or id (a GameObject to frame) to capture from a temporary camera that never disturbs the game's own camera. Returns the saved PNG's absolute 'path' — READ it to see the result.",
      "params": [
        {
          "name": "id",
          "description": "GUID of a GameObject to frame (uses a temp camera)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "Camera world position (temp camera; use instead of id) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "lookAt",
          "description": "World point to look at (pair with position) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "Explicit camera rotation (pair with position)",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "fov",
          "description": "Field of view for the temp camera",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "renderUI",
          "description": "Include UI/HUD (default true). Renders world + world-space UI but NOT fullscreen screen-space panels (lobby/title overlays) — so capture_view sees 'through' menus; use take_screenshot for screen-space UI.",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "width",
          "description": "Width (default 1280)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "height",
          "description": "Height (default 720)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "console_run",
      "description": "Run an s&box console command / ConCmd via Sandbox.ConsoleSystem.Run — e.g. a cvar ('sv_cheats 1') or a registered command. Also the invocation primitive behind execute_csharp. Fire-and-forget: returns only { ran, command } and does NOT capture console output — follow with read_log to see what the command printed.",
      "params": [
        {
          "name": "command",
          "description": "The console command line to run",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "execute_csharp",
      "description": "EXPERIMENTAL. Compile + run a C# snippet inside the s&box EDITOR (which is unsandboxed): writes a temp [ConCmd] into the project's Editor/ folder, hotloads, runs it, reads the result/exception from the log, then deletes the temp file. With expression:true, returns the JSON value of a single expression; otherwise runs statements. CAVEATS: each call triggers a hotload (~2-8s) and recompiles the project's editor assembly; a snippet that fails to compile is reported + cleaned up, but briefly taints the editor assembly until cleanup.",
      "params": [
        {
          "name": "code",
          "description": "C# to run (editor-context, unsandboxed). With expression:true, a single expression; otherwise statements.",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "expression",
          "description": "Treat code as an expression and return its JSON value (default false)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "timeoutMs",
          "description": "Max wait for compile + run, ms (default 20000)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "frame_camera",
      "description": "Aim the s&box EDITOR viewport camera at a GameObject (by id) or a world point (position + optional radius), then call take_screenshot to capture that view. This is how Claude points its own screenshots at what it's working on — frame a spawned object, then screenshot to verify it actually looks right.",
      "params": [
        {
          "name": "id",
          "description": "GUID of a GameObject to frame on",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World point to frame on (use instead of id) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "radius",
          "description": "Frame radius around the position, in units (default 128)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "get_bounds",
      "description": "Get a GameObject's world-space bounding box. Returns { id, name, center, size, extents, mins, maxs, radius, position, empty } — objects with no renderer report empty:true (bounds collapse to the world position) plus an explanatory note. Feed center/radius into screenshot_from or frame_camera to frame the object, or use mins/maxs for placement math (screenshot_orbit calls this internally).",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to measure",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "get_compile_errors",
      "description": "Scan the recent s&box log for compile errors and exceptions — the fast way for Claude to confirm whether its last script/addon edit actually compiled. Reads sbox-dev.log directly (works even if the editor is mid-crash). Filters out the noisy 'Broken Reference: package.local.* (the compiler failed)' cascade (which masks the real cause) and surfaces the underlying '[Generic] Error | ...CSxxxx... file:line' diagnostics. Returns the real error lines, or an all-clear.",
      "params": [
        {
          "name": "lines",
          "description": "How many lines from the end to scan (default 400, max 4000)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "read_log",
      "description": "Read s&box's editor log (sbox-dev.log) so Claude can see compile errors, exceptions, and Log.Info output directly. Reads the log file (not via the bridge), so it works even when the editor has crashed. If auto-detection fails (non-Windows / non-Steam install), set the SBOX_LOG_PATH environment variable to the full log path.",
      "params": [
        {
          "name": "lines",
          "description": "How many lines from the end to return (default 80, max 1000)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "filter",
          "description": "Only return lines containing this substring (case-insensitive)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "screenshot_from",
      "description": "Take a screenshot from a chosen angle. take_screenshot is locked to the scene's main camera; this temporarily moves that camera to frame your target, captures, then restores it — so Claude can finally AIM its own screenshots. Pass id (frame an object) OR position {x,y,z} with optional lookAt {x,y,z} or rotation {pitch,yaw,roll}. After it returns, read the newest PNG in the editor's screenshots folder.",
      "params": [
        {
          "name": "id",
          "description": "GUID of a GameObject to frame",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "Camera world position (use instead of id) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "lookAt",
          "description": "World point to look at (pair with position) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "Explicit camera rotation (pair with position)",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "width",
          "description": "Screenshot width (default 1920)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "height",
          "description": "Screenshot height (default 1080)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "diagnostics",
      "name": "screenshot_orbit",
      "description": "Capture a GameObject from several angles in ONE call — orbits the scene's main camera around the object and screenshots each angle, so Claude can verify 3D work from multiple sides instead of guessing from one. Drives get_bounds (framing) + screenshot_from per angle (each its own frame, the reliable capture path). Returns the saved PNG paths in order — READ them to inspect.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to orbit",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "shots",
          "description": "Number of angles around the object (default 4, clamped 2-8)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "elevation",
          "description": "Camera height factor: 0 = level, 1 = high (default 0.4)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "distance",
          "description": "Camera distance in units (default: auto from bounds)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "width",
          "description": "Screenshot width (default 1280)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "height",
          "description": "Screenshot height (default 720)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "director",
      "name": "create_event_director",
      "description": "Generate a generalized L4D-style AI/pacing director component (host-authoritative). On a configurable interval the host rolls a weighted pick over a [Property] List<GameObject> EventPrefabs (with a parallel List<float> Weights), skips any event already active (dedupe) and anything past a MaxActive concurrency cap, clones the chosen prefab, NetworkSpawns it, and attaches a generated {name}TimedEvent companion so each spawned event self-destructs after EventLifetime seconds. Great for ambient events, waves, and world events. Single-player safe (IsProxy guard; NetworkSpawn falls back to a local clone). Fill EventPrefabs/Weights in the inspector or via the bridge after a hotload; edit the RollInterval() stub to make pacing adaptive (player-count/inactivity/time-pressure factors) per the ai-director cookbook. Optionally attached to an existing GameObject by GUID (after a hotload). NOTE: emits ONE .cs file containing two classes ({name} + {name}TimedEvent); the type only resolves after trigger_hotload.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the director (a {name}TimedEvent companion is generated alongside it). Defaults to 'EventDirector'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "path",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "intervalSeconds",
          "description": "Base seconds between director rolls. Defaults to 30 (clamped to >= 0.1)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "maxActive",
          "description": "Maximum number of concurrently-live events. Defaults to 3 (clamped to >= 1)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "eventLifetime",
          "description": "Seconds before each spawned event self-destructs. Defaults to 60 (clamped to >= 0.1)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach the director to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "discovery",
      "name": "describe_type",
      "description": "Inspect a type's full surface — properties, methods, events, attributes — via reflection on Game.TypeLibrary and loaded assemblies. Use this before writing code touching an unfamiliar component or s&box API. Examples: 'MeshComponent', 'PlayerController', 'NetworkHelper', 'Vector3'.",
      "params": [
        {
          "name": "name",
          "description": "Type name (short or fully-qualified)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "discovery",
      "name": "find_in_project",
      "description": "Grep the user's s&box project for a symbol (case-sensitive substring; skips .git/bin/obj). Useful for finding usage examples of an API or seeing how the project already does something. Returns `symbol`, `count`, and `results` [{file, line, text}], capped at `max_results` (default 25) — raise it if you may be missing hits. Follow up with read_file on a result's file path.",
      "params": [
        {
          "name": "symbol",
          "description": "Substring or symbol to search for",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "extension",
          "description": "File extension filter",
          "optional": true,
          "default": ".cs",
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "max_results",
          "description": "Maximum hits to return (default 25); the search stops once reached",
          "optional": true,
          "default": 25,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "discovery",
      "name": "get_method_signature",
      "description": "Get the formal signature(s) of a method on a type — parameter names, types, defaults, return type, all overloads. Use before invoking an API you're unsure of.",
      "params": [
        {
          "name": "type",
          "description": "Type name (e.g. 'Scene', 'GameObject')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "method",
          "description": "Method name (case-sensitive)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "discovery",
      "name": "list_libraries",
      "description": "List the s&box libraries/addons installed in this project (reads Libraries/ + each .sbproj). Discovers what's available to build ON — e.g. character controllers (fish.scc = Shrimple Character Controller, facepunch.playercontroller), world/spline/road tools — so you can leverage an installed library (add its components via add_component_with_properties, or generate code against its API) instead of writing from scratch. Returns `count` and `libraries` [{folder, ident, org, title, type, enabled}] — ALL libraries, no limit or pagination; `enabled` is false when the library's .sbproj has been disabled (renamed .sbproj.disabled). Read-only.",
      "params": []
    },
    {
      "module": "discovery",
      "name": "search_types",
      "description": "Find loaded types matching a name pattern. Useful for discovering 'is there a built-in X for this?'. Returns `count` and `matches` with each type's name, fullName, isComponent, and isAbstract — results are silently truncated at `limit` (default 50), so narrow the pattern if you hit the cap. Pass a match's name to describe_type for its full member surface.",
      "params": [
        {
          "name": "pattern",
          "description": "Substring to match against type name (case-insensitive)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "namespace",
          "description": "Optional namespace filter (case-insensitive substring)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "components_only",
          "description": "Only return Component subclasses (default false)",
          "optional": true,
          "default": false,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "limit",
          "description": "Maximum matches to return (default 50); the search stops silently at this cap",
          "optional": true,
          "default": 50,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "docs",
      "name": "get_doc_page",
      "description": "Fetch the full Markdown of one s&box guide page (from search_docs). Pass a slug like \"networking/rpcs\", a raw repo path like \"docs/networking/rpcs.md\", or a full sbox.game/dev/doc URL. YAML frontmatter is stripped. Long pages are paginated: pass chunk to read further (the footer tells you when there's more). Read-only.",
      "params": [
        {
          "name": "slug",
          "description": "Page slug (e.g. \"networking/rpcs\"), a \"docs/...md\" path, or a full sbox.game/dev/doc URL",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "chunk",
          "description": "1-based chunk index for long pages (default 1)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "chunkSize",
          "description": "Max characters per chunk (default 8000)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "docs",
      "name": "list_doc_categories",
      "description": "List the categories of the official s&box guide documentation, with how many pages each has. Use a category name to filter search_docs. Read-only.",
      "params": []
    },
    {
      "module": "docs",
      "name": "search_docs",
      "description": "Search the official s&box guide documentation (the Facepunch/sbox-docs pages rendered at sbox.game/dev/doc) by keyword. Returns the best-matching pages with their title, category, slug, and web URL — use the slug with get_doc_page to read one. The s&box API changes between SDK versions, so prefer these real docs over guessing. Read-only.",
      "params": [
        {
          "name": "query",
          "description": "Keywords to match against page titles, slugs, and paths",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "category",
          "description": "Optional category filter (the 2nd path segment, e.g. \"networking\", \"ui\"). Use list_doc_categories to see them.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "limit",
          "description": "Max results to return (default 10, max 50)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "gamefeel",
      "name": "add_flicker_light",
      "description": "Generate a light-flicker animator and optionally attach it to an existing light GameObject by GUID. Five presets: Candle (soft organic sway), Fluorescent (mostly steady with random dips), Faulty (hard on/off cuts), Pulse (slow sine breathing), Lightning (dim baseline with rare bright flashes). Modulates the sibling Light component's LightColor around the color it found on enable (works on PointLight / SpotLight / DirectionalLight) and restores it exactly on disable; intensity 0..1 sets flicker depth, speed scales the whole pattern. The single biggest atmosphere win per call for horror/night scenes — pairs with apply_atmosphere. LOCAL/visual-only.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'FlickerLight'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "style",
          "description": "Default flicker preset baked into the component (editable per-instance in the inspector). Defaults to 'Candle'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "Candle",
              "Fluorescent",
              "Faulty",
              "Pulse",
              "Lightning"
            ]
          }
        },
        {
          "name": "intensity",
          "description": "Flicker depth 0..1: 0 = steady, 1 = full blackouts / double-bright flashes. Defaults to 0.5",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "speed",
          "description": "Speed multiplier for the whole pattern. Defaults to 1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "lightId",
          "description": "GUID of a GameObject holding a light component to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gamefeel",
      "name": "create_camera_shake",
      "description": "Generate a trauma-based camera shake component (the standard game-feel model: events add Trauma 0..1, shake magnitude = Trauma², smooth Perlin offsets — not white-noise jitter — and Trauma decays every frame, so explosions slam and footsteps barely register). Attach the generated component to the CAMERA GameObject; fire from any game code via the static <Name>.Shake(0.4f). Applies in OnPreRender AFTER controllers position the camera, with an un-apply guard so it neither fights a controller-driven camera nor accumulates on a static one, and restores the camera when trauma hits zero. LOCAL-only (no [Sync]) — call Shake inside an [Rpc.Broadcast] handler if every client should feel it. Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'CameraShake'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxOffset",
          "description": "Positional shake at full trauma, in world units. Defaults to 6",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "maxAngle",
          "description": "Rotational shake at full trauma, in degrees (applied to pitch/yaw/roll). Defaults to 4",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "frequency",
          "description": "Noise speed — higher = violent rattle, lower = drunken sway. Defaults to 10",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "decayPerSecond",
          "description": "How much trauma drains per second. Defaults to 1.5",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of the camera GameObject to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gamefeel",
      "name": "create_floating_combat_text",
      "description": "Generate a floating combat text component — rising, fading, camera-billboarded world-space popups for damage numbers, '+10 gold', pickup names. TextRenderer-based: no Razor, no WorldPanel, zero UI setup. Nothing to place in the scene — the generated class carries a static factory: <Name>.Spawn(position, \"-25\", Color.Red[, sizeMultiplier]) spawns a popup that rises at RiseSpeed, fades over Lifetime, and destroys itself. Pairs with create_health_system (spawn from the damage path so every hit prints its number). LOCAL-only — spawn inside an [Rpc.Broadcast] handler if every client should see it.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'FloatingCombatText'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "riseSpeed",
          "description": "World units the popup rises per second. Defaults to 48",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "lifetime",
          "description": "Seconds until the popup is fully faded and destroyed. Defaults to 1.1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "fontSize",
          "description": "Base font size baked into Spawn() (the optional Spawn size argument multiplies it). Defaults to 24",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "create_gameobject",
      "description": "Create a new GameObject in the active scene. Returns its GUID for future reference",
      "params": [
        {
          "name": "name",
          "description": "Display name (e.g. 'Player', 'Enemy Spawn Point'). Defaults to 'New Object'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "World rotation",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "scale",
          "description": "Uniform scale (number) or per-axis scale — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "parent",
          "description": "GUID of parent GameObject. Omit for scene root",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "delete_gameobject",
      "description": "Delete a GameObject from the active scene by its GUID",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to delete",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "duplicate_gameobject",
      "description": "Clone a GameObject with all its components. Returns { duplicated, original, gameObject } — gameObject.id is the clone's new GUID; pass it to set_transform / add_component_with_properties. If offset is omitted the clone lands exactly on top of the original.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to duplicate",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "name",
          "description": "New name for the clone",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "offset",
          "description": "Position offset from original so the clone doesn't overlap",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "focus_object",
      "description": "Highlight a GameObject by selecting it in the editor. NOTE: s&box exposes no dedicated focus API, so this only sets the selection — it does NOT move any camera (returns { focused, id, note } saying so). To actually point the viewport at an object use frame_camera; to aim a screenshot use screenshot_from.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to focus",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "get_scene_hierarchy",
      "description": "Get the scene tree — GameObjects with their names, GUIDs, components, and parent/child relationships. Pair maxDepth with rootId to drill into a subtree without paying for the whole scene",
      "params": [
        {
          "name": "maxDepth",
          "description": "Maximum recursion depth. Defaults to 10. Use 1 or 2 for cheap top-level overviews",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "rootId",
          "description": "Optional GUID of a GameObject to start traversal from. Omit to walk from the scene roots",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "get_selected_objects",
      "description": "Get the GameObjects currently selected by the user in the s&box editor. Returns { count, selected } where each entry is a serialized GameObject (id, name, enabled, position, rotation, scale, components, childCount) — use the ids with set_transform, add_component_with_properties, etc. Handy for 'do X to what I have selected' requests.",
      "params": []
    },
    {
      "module": "gameobjects",
      "name": "rename_gameobject",
      "description": "Change the display name of a GameObject identified by its GUID (the GUID itself never changes, so existing references stay valid). Returns { renamed, id, oldName, newName }. Name-based lookups (e.g. find_objects) will see the new name immediately.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "name",
          "description": "New display name",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "select_object",
      "description": "Select a GameObject in the editor (highlights it in the hierarchy and scene view). Replaces the current selection unless addToSelection=true. Returns { selected, id }; confirm the result with get_selected_objects.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to select",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "addToSelection",
          "description": "If true, adds to current selection instead of replacing it",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "set_enabled",
      "description": "Enable or disable a GameObject (disabled objects are invisible and inactive, including their components and children). Returns { id, enabled } confirming the new state.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "enabled",
          "description": "true to enable, false to disable",
          "optional": false,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "set_parent",
      "description": "Reparent a GameObject. Set parentId to null or omit to move to scene root",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to reparent",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "parentId",
          "description": "GUID of the new parent. Null or omitted = scene root",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameobjects",
      "name": "set_transform",
      "description": "Set position, rotation, and/or scale on a GameObject. Only provided values are changed; values apply in world space unless local=true. Returns { transformed, gameObject } with the full serialized object (id, name, position, rotation, scale, components) so you can verify what actually applied.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "New position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "New rotation",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "scale",
          "description": "New scale — uniform number, per-axis object {x,y,z}, or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "local",
          "description": "If true, values are in local space. Default is world space",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "add_component_to_new_object",
      "description": "Create a new GameObject, add a component, set its properties, and optionally parent/position/tag it — all in one atomic call. Collapses the create_gameobject → add_component_with_properties → set_parent sequence. NOTE: a freshly GENERATED component type only resolves after a trigger_hotload; generate the script, hotload, THEN call this",
      "params": [
        {
          "name": "name",
          "description": "Display name for the new GameObject. Defaults to the component type name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name to add (e.g. 'CameraComponent', 'ObjectiveManager'). Use list_available_components to find valid types",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "properties",
          "description": "Key-value map of property names to values, auto-converted to the right type (same convention as add_component_with_properties)",
          "optional": true,
          "type": {
            "kind": "any"
          }
        },
        {
          "name": "position",
          "description": "World position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "World rotation",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "scale",
          "description": "World scale (per-axis)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "parentId",
          "description": "GUID of a parent GameObject. Omit for scene root",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "tags",
          "description": "Tags to add to the new GameObject (e.g. ['player'])",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_day_night_clock",
      "description": "Generate a host-authoritative time-of-day clock: [Sync(SyncFlags.FromHost)] TimeOfDay (0–24) + Day advancing by Time.Delta, IsDay/IsNight from sunrise/sunset hours, and static OnNewDay / OnDayNightChanged events to drive lighting, NPC schedules, or spawns. Single-player safe. Pairs with create_round_phase_machine. Optionally attached to a GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'DayNightClock'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "dayLengthSeconds",
          "description": "Real seconds per in-game day. Defaults to 600 (10 min)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "startHour",
          "description": "Hour the clock starts at (0–24). Defaults to 8",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "sunriseHour",
          "description": "Hour day begins. Defaults to 6",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "sunsetHour",
          "description": "Hour night begins. Defaults to 20",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach to (hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_economy_wallet",
      "description": "Generate a host-authoritative currency Wallet component: a [Sync(SyncFlags.FromHost)] Money balance (only the host can write it — plain [Sync] money is the classic economy exploit) with AddMoney / TrySpend / SetMoney / CanAfford and an OnMoneyChanged event. Single-player safe. Optionally attached to an existing GameObject by GUID (after a hotload). Pairs with a save system for persistence. Mined from the most-requested currency pattern across 51 games.",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'Wallet'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "startingMoney",
          "description": "Initial balance the host seeds on start. Defaults to 0",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach the Wallet to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_health_system",
      "description": "Generate a Health component: MaxHealth, [Sync] CurrentHealth, TakeDamage/Heal, an OnDeath event, optional regen and respawn. Host-authoritative damage when networked, single-player safe. Optionally attached to an existing GameObject by GUID",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'Health'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxHealth",
          "description": "Starting/maximum health. Defaults to 100",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "regen",
          "description": "Include passive health regeneration after a delay. Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "respawn",
          "description": "On death, respawn at a RespawnPoint (wire it with set_component_reference) instead of disabling. Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach the Health component to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_interactable",
      "description": "Generate a Component.IPressable interactable: the built-in PlayerController 'use' key drives Press()/Hover()/Blur() with no custom player code. Includes a static OnPressed event, an optional cooldown (TimeUntil), and a private OnPress() extensionpoint for effects. For host-authoritative side-effects call an [Rpc.Host] from OnPress(). The Prompt property is left to your game's HUD. Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'Interactable'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "prompt",
          "description": "Prompt string shown by the game's HUD when hovering. Defaults to 'Press'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "cooldownSeconds",
          "description": "Seconds before the interactable can be pressed again. 0 = no cooldown. Defaults to 0",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach the component to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_inventory",
      "description": "Generate a slot-based inventory component using parallel List<string> ItemIds / List<int> Counts (serialization-safe, inspector-editable). Includes TryAdd (stack-first, partial-add rejected), TryRemove, CountOf, Move (swap or merge same-id slots), and Clear. Static OnChanged event fires after every successful mutation. Host-authoritative usage note: mutate on the host in multiplayer, replicate via your own [Sync]/RPC. Pairs with create_pickup.",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'Inventory'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "capacity",
          "description": "Total slot count. Defaults to 24",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "maxStack",
          "description": "Maximum items per slot (stack cap). Defaults to 99",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach to (hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_leaderboard_panel",
      "description": "Generate a Razor PanelComponent that fetches and displays a Sandbox.Services leaderboard derived from a stat name. Produces TWO files: {name}.razor and {name}.razor.scss. The panel auto-refreshes every 30 s, shows rank/displayName/value rows, handles loading state, and includes a BuildHash() override (razor-lint clean). Must be hosted under a ScreenPanel or WorldPanel. Stats must be configured for the project ident on sbox.game. Uses Leaderboards.Get(statName) + board.Refresh() -- the exact API from ServicesQueryHandler. Returns { created, razorPath, scssPath, className, note }. Follow with trigger_hotload, then get_compile_errors, then host it via add_screen_panel (panelComponent=className).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the panel component. Defaults to 'LeaderboardPanel'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated files. Defaults to 'Code/UI'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "statName",
          "description": "Sandbox.Services stat name the leaderboard is derived from. Defaults to 'score'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "title",
          "description": "Display title shown at the top of the panel. Defaults to 'Leaderboard'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxRows",
          "description": "Maximum leaderboard rows to fetch and display. Defaults to 10",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_objective_system",
      "description": "Generate an ObjectiveManager component — the win/lose brain of a game. Tracks an objective (collect_all / reach_goal / survive_time / eliminate_all), fires a win, and handles a lose condition (fall below kill-Z / timer / out of lives). Self-contained C#; other systems call ObjectiveManager.Instance. Optionally placed as a scene singleton. Returns { created, path, className, gameObject, note } — gameObject is the placed singleton, or null with a note when the fresh type isn't in the TypeLibrary yet. Follow with trigger_hotload, then get_compile_errors; if placement was skipped, place with add_component_to_new_object after the hotload",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'ObjectiveManager'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "objective",
          "description": "Win condition. Defaults to 'reach_goal'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "collect_all",
              "reach_goal",
              "survive_time",
              "eliminate_all"
            ]
          }
        },
        {
          "name": "targetCount",
          "description": "How many to collect/eliminate (for collect_all / eliminate_all). Defaults to 3",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "timeLimit",
          "description": "Seconds — survive this long to win (survive_time) or before losing (loseOn=timer). Defaults to 60",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "loseOn",
          "description": "Lose condition. 'fall' = player drops below killZ. Defaults to 'fall'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "fall",
              "timer",
              "lives",
              "none"
            ]
          }
        },
        {
          "name": "killZ",
          "description": "World Z below which the player is considered fallen out of the world. Defaults to -1000",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "lives",
          "description": "Lives before game over (loseOn=lives). Defaults to 1",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "placeInScene",
          "description": "Place the manager as a scene singleton. Defaults to true. (Only attaches if the type is already loaded — generate, hotload, then it places; otherwise add it after hotload.)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_pickup",
      "description": "Generate a trigger-based collectible component. On enter by a tagged object it raises OnCollected (wire it to your objective/score system) and despawns. Optionally builds a visible pickup GameObject with a trigger SphereCollider (+ a model) in one call. Returns { created, path, className, gameObject, note } — gameObject is the placed pickup (null unless placeInScene=true); a note flags when the component couldn't attach because the fresh type needs a hotload. Follow with trigger_hotload, then get_compile_errors",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'Pickup'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "action",
          "description": "Effect flavour (all self-contained; the heal/item branches show the typed call to a companion system in comments). Defaults to 'score'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "score",
              "heal",
              "item",
              "custom"
            ]
          }
        },
        {
          "name": "amount",
          "description": "Magnitude of the effect (score points, heal amount). Defaults to 1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "filterTag",
          "description": "Only collect for objects with this tag. Defaults to 'player'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "placeInScene",
          "description": "Also build a pickup GameObject (trigger SphereCollider + optional model). Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "position",
          "description": "World position when placeInScene is true",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "radius",
          "description": "Trigger sphere radius when placed. Defaults to 24",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "model",
          "description": "Optional model path for a visible pickup (e.g. 'models/dev/box.vmdl'). Cloud assets must be installed first",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_placement_mode",
      "description": "Generate a ghost-preview + commit placement component (single class). StartPlacing() clones GhostPrefab as a NetworkMode.Never preview with colliders disabled and ModelRenderers tinted semi-transparent. Each frame while placing: ray from Scene.Camera.GetMouseRay(), IgnoreGameObjectHierarchy(ghost), snap hit position to GridSize (0 = freeform), move ghost. On Input.Pressed('attack1') TryPlace() re-validates distance and commits a real clone. StopPlacing() destroys the ghost. Static OnPlaced(GameObject, Vector3) event. Includes a multiplayer RPC note. API grounded in building-placement cookbook (enifun.shop_manager pattern).",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'PlacementMode'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "gridSize",
          "description": "Snap grid size in world units (0 = freeform placement). Defaults to 0",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach to (hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_round_phase_machine",
      "description": "Generate a host-authoritative round/phase machine: a [Sync(SyncFlags.FromHost)] CurrentPhase cycled through your named phases on a per-phase timer (host-only), with a static OnPhaseChanged event that fires on every machine. Great for round/match flow, match phases, or a day/night cycle. Single-player safe. Optionally attached to an existing GameObject by GUID (after a hotload). Mined from the round-flow pattern across the 51 games.",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'GameDirector'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "phases",
          "description": "Ordered phase names (become an enum), e.g. [\"Lobby\",\"Day\",\"Night\",\"Payout\"]. Defaults to [\"Lobby\",\"Active\",\"Ended\"]",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "duration",
          "description": "Default seconds per phase (each phase also gets its own tunable [Property]). Defaults to 60",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "loop",
          "description": "Loop back to the first phase after the last (true) or hold on the last phase (false). Defaults to true",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach to (only if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_save_system",
      "description": "Generate a versioned save-system component: a SaveData POCO with Version bump on schema change, dirty-flag autosave on a TimeUntil timer, clamp-on-load Sanitize() for corrupt/hand-edited saves, and delete-on-version-mismatch to start fresh instead of crashing. Runs only on the owning machine (IsProxy guard). Fires static OnLoaded/OnSaved hooks for HUD and analytics. FileSystem.Data.ReadJsonOrDefault/WriteJson verified live on the current SDK. Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'SaveSystem'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "fileName",
          "description": "Save file name under FileSystem.Data (e.g. 'save.json'). Defaults to 'save.json'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "version",
          "description": "Schema version embedded in SaveData. Old saves with a different version start fresh. Defaults to 1",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "autosaveSeconds",
          "description": "Seconds between autosave ticks (0 disables autosave). Defaults to 10",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach to (only if the type is already loaded -- hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_stat_modifier_system",
      "description": "Generate an enum-keyed stat modifier system with three modifier layers: SET (highest-priority-wins hard override), ADD (summed bonuses), MULT (multiplied factors applied last). Modifier storage uses parallel private Lists of primitive types (serialization-safe). RemoveModifiersFrom(source) cleans up all mods from a buff/debuff source by reference. Static OnStatChanged(stat, value) event fires after every add/remove. Mined from RPG/buff/debuff patterns across shipped s&box games. Returns { created, path, className, stats, placedOn, note } — stats echoes the sanitized stat names ({name}Stat enum values); placedOn is the target GameObject when attached (needs the type hotloaded). Follow with trigger_hotload, then get_compile_errors.",
      "params": [
        {
          "name": "name",
          "description": "Class name prefix -- generates {name}Stat enum + {name} Component. Defaults to 'StatSystem'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "stats",
          "description": "Stat names as a JSON array or comma-separated string. Defaults to 'Health,Speed,Damage'",
          "optional": true,
          "type": {
            "kind": "any",
            "unionOf": [
              "array",
              "string"
            ]
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach to (hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "create_weighted_loot_table",
      "description": "Generate a cumulative-weight random loot picker: parallel Name/Weight lists (inspector-editable), a Roll() method that returns a winning entry name and fires a static OnLoot event, and optional pity (guarantee the last/rarest entry after PityAfter consecutive non-rare rolls). Roll() is host-authoritative -- only call it on the host and replicate the result (clients rolling their own loot is equivalent to clients writing their own money balance). Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'LootTable'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "entries",
          "description": "Loot table entries. Defaults to common:70 / uncommon:25 / rare:5",
          "optional": true,
          "type": {
            "kind": "any",
            "unionOf": [
              "array",
              "string"
            ]
          }
        },
        {
          "name": "pity",
          "description": "If true, guarantee the last (rarest) entry after PityAfter consecutive non-rare rolls. Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach to (only if the type is already loaded -- hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "gameplay",
      "name": "set_component_reference",
      "description": "Wire a component's GameObject/Component-typed property to ANOTHER live object in the scene by GUID (e.g. ObjectiveManager.Player = the player, a camera's follow target, a door's hinge). Preferred for object/component refs (can pick a specific component type off the target via targetComponent, and validates). set_property also accepts a GUID for ref props; set_prefab_ref is for prefab assets. Set clear:true to null the reference",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject that HOLDS the component you're writing into",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name on that object (e.g. 'ObjectiveManager', 'CameraComponent')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "The property to set (must be a GameObject- or Component-typed property)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of the GameObject to reference. Required unless clear:true",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetComponent",
          "description": "If the property is a Component subtype, the specific component type to pull off the target object. Omit to auto-match by the property's type",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "clear",
          "description": "If true, set the reference to null instead of assigning a target",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "inputs",
      "name": "ensure_input_action",
      "description": "Register a custom named INPUT ACTION in the project so a generated game's custom verbs work in play mode. Writes to <project>.sbproj → Metadata.InputSettings.Actions[]. Idempotent: if the action already exists it is left alone (pass update=true to rebind its key). If the project has no InputSettings yet, the full DEFAULT action set (Forward/Back/Left/Right/Jump/Use/attack1/...) is seeded first so player movement/use are preserved — the engine only auto-injects defaults when a game defines NONE. After adding, call it from game code with Input.Pressed(\"name\") / Input.Down(\"name\") / Input.Released(\"name\"). Note: input config is read at project load, so restart_editor (or reload the project) for a new action to take effect in play mode.",
      "params": [
        {
          "name": "name",
          "description": "The action verb game code will call, e.g. \"interact\", \"sprint\", \"drop\". Matches Input.Pressed(\"interact\").",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "keyboardKey",
          "description": "Default keyboard binding, e.g. \"e\", \"f\", \"space\", \"mouse1\", \"shift\". Omit to add the action with no default key (player can bind it).",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "group",
          "description": "UI group the action is listed under in the bindings menu (e.g. \"Actions\", \"Movement\", \"Other\"). Defaults to \"Actions\".",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "update",
          "description": "If the action already exists, rebind its keyboardKey to the provided value instead of leaving it untouched. Default false (idempotent no-op when present).",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "inspection",
      "name": "inspect_networked_object",
      "description": "Inspect the live networking contract of a GameObject. Returns {id, name, network: {active, isProxy, isOwner, isCreator, ownerId, ownerSteamId, ownerTransfer, orphaned, flags}, components: [{component, fields: [{name, type, isSync, syncFlags, value}]}]} — by default only [Sync]-marked fields are listed (components with none are omitted). Unlike get_network_status (session-only), this is per-object — the way to verify a host-authoritative or ownership change actually replicated; works in edit or play mode. Follow up with set_ownership to change the owner, or networking_lint to find the code-level cause of a bad [Sync] value.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to inspect",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "allProps",
          "description": "Include all component properties, not just [Sync]-marked ones",
          "optional": true,
          "default": false,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "inspection",
      "name": "networking_lint",
      "description": "Static-scan the project's C# for the highest-frequency networking/authority bugs: a mutator that writes a [Sync] field with no IsProxy/Networking.IsHost guard; money/health/score-shaped fields marked plain [Sync] (should be SyncFlags.FromHost); List<>/Dictionary<> marked [Sync] (should be NetList/NetDictionary); [Sync] fields typed Connection/GameObject (sync a Guid instead); [Rpc.Host] methods that mutate without re-checking Rpc.Caller; and component swaps / reflection writes missing Network.Refresh(). Returns findings with file:line + the suggested fix.",
      "params": [
        {
          "name": "path",
          "description": "Optional sub-path under the project (e.g. 'Code/Player') to scope the scan; omit for the whole project",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "inspection",
      "name": "razor_lint",
      "description": "Static-scan .razor and .razor.scss files for the silent footguns that crash the Razor transpiler or stylesheet engine with no useful error message: switch expressions inside @code blocks (use if/else instead), non-ASCII/emoji inside @code (move to markup or a string constant), PanelComponent subclasses missing a BuildHash override (panel never re-renders), and root uppercase type-selector rules in .razor.scss (silently skipped -- use a class selector like .my-panel). Returns { scanned, findings: [{file, line, match, advice}], clean } matching the sandbox_lint shape.",
      "params": [
        {
          "name": "directory",
          "description": "Subdirectory under the project root to scan (e.g. 'UI', 'Code'). Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "inspection",
      "name": "sandbox_lint",
      "description": "Static-scan the project's C# for s&box sandbox whitelist violations BEFORE they cause compile errors: System.MathF (use MathX), System.Math (use MathX), Array.Clone() (use .ToArray()), System.Net / raw sockets (use Sandbox.Http), System.IO.File (use FileSystem.Data), and raw System.Threading.Thread (use async/Task or GameTask). Returns { scanned, findings: [{file, line, match, advice}], clean }. Scope to a subdirectory with the directory param.",
      "params": [
        {
          "name": "directory",
          "description": "Subdirectory under the project root to scan (e.g. 'Code', 'Code/Player'). Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "inspection",
      "name": "save_inspect",
      "description": "Inspect the game's FileSystem.Data save files — the assistant is otherwise blind to persisted state. action='list' (default) returns `directories` and `files` [{name, path, size}] under `path` (omit path for the Data root); action='read' returns {path, length, content}, truncating content at 60,000 chars; action='diff' compares two save files key-by-key, returning `diffCount` and up to 200 `diffs` [{key, change: added|removed|changed}]. Use to verify a save actually wrote, debug a load/migration, or confirm a sanitize/clamp ran.",
      "params": [
        {
          "name": "action",
          "description": "'list' (default) enumerates a folder; 'read' dumps one file's JSON; 'diff' compares `path` vs `pathB`",
          "optional": true,
          "default": "list",
          "type": {
            "kind": "enum",
            "values": [
              "list",
              "read",
              "diff"
            ]
          }
        },
        {
          "name": "path",
          "description": "File or folder path under FileSystem.Data (e.g. 'lumber_corp2_progress' or '<folder>/steam_123.json')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "pathB",
          "description": "Second file path for action='diff'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "inspection",
      "name": "scene_validate",
      "description": "Validate the active scene for the silent setup footguns that break controllers/physics/cameras: no CameraComponent, no player controller, multiple root Rigidbodies, a Rigidbody with MotionEnabled=false fighting a kinematic root, IsTrigger colliders that Scene.Trace will ignore, child Rigidbodies breaking collider binding, and missing required child anchors. Returns each issue with the GameObject and the exact fix.",
      "params": []
    },
    {
      "module": "inspection",
      "name": "services_query",
      "description": "Read from Sandbox.Services — the cloud stats/leaderboard layer many games use as their real DB. action='stats' with `name` returns the local player's stat {ident, value, sum, min, max, lastValue, valueString}; without `name` it returns only the package ident plus a usage note (it does NOT list stat definitions). action='leaderboard' (name required) returns {board, displayName, totalEntries, count, entries} with at most `limit` entries (default 10). Read-only; use to verify a Stats.Increment/SetValue path or a leaderboard wired correctly.",
      "params": [
        {
          "name": "action",
          "description": "'stats' (default) reads a local-player stat by `name`; 'leaderboard' fetches a board's top entries",
          "optional": true,
          "default": "stats",
          "type": {
            "kind": "enum",
            "values": [
              "stats",
              "leaderboard"
            ]
          }
        },
        {
          "name": "name",
          "description": "Stat name (action='stats') or leaderboard/board name (action='leaderboard')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "limit",
          "description": "Max leaderboard entries to return",
          "optional": true,
          "default": 10,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "inspection",
      "name": "simulate_input",
      "description": "Press or release a named input action during PLAY mode (via Sandbox.Input.SetAction) so input-driven behavior — jump, attack1, use, reload, IPressable, weapon fire — can be verified without a human at the keyboard. REQUIRES play mode and a named `action`; returns {action, state: 'down'|'up', note}. Caveats: SetAction applies to the current input frame ('press' and 'hold' both set the action down; 'release' clears it), and analogMove/analogLook/durationMs are accepted but IGNORED — Sandbox.Input has no analog injection API, so for movement use drive_player or the playtest harness instead.",
      "params": [
        {
          "name": "action",
          "description": "Named input action to drive (must exist in Input.config), e.g. 'jump', 'attack1', 'use'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "state",
          "description": "press = one tick (Pressed); hold = held for durationMs; release = clear a held action",
          "optional": true,
          "default": "press",
          "type": {
            "kind": "enum",
            "values": [
              "press",
              "hold",
              "release"
            ]
          }
        },
        {
          "name": "analogMove",
          "description": "AnalogMove vector to apply (forward/left) -- object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "analogLook",
          "description": "AnalogLook angles delta to apply",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "durationMs",
          "description": "How long to hold the action / apply the analog values",
          "optional": true,
          "default": 100,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "interactionpack",
      "name": "add_interaction_prompt",
      "description": "Generate an eye-traced interaction-prompt HUD — a PanelComponent (.razor + .razor.scss pair, like create_leaderboard_panel) that every frame traces a ray from the scene camera (Scene.Trace.Ray, out to [Property] float Range) and, when the crosshair is on a component implementing Component.IPressable, shows a centered \"Press E\"-style pill. The prompt text comes from the target's IPressable.GetTooltip() when it overrides it (most don't), else a [Property] DefaultPrompt built from the action. This is the visible half of the interaction loop: it PAIRS with create_interactable / add_interaction_station (which implement IPressable) — this tool tells the player they CAN press, those tools handle the press. Host it under a ScreenPanel (add_screen_panel), then add the component to that panel object. The generated Razor is razor_lint-safe by construction: PanelComponent + BuildHash override folding the visible state, no switch-expressions and no non-ASCII in @code, and a class root selector in the SCSS. LOCAL/visual-only (no [Sync]).",
      "params": [
        {
          "name": "name",
          "description": "Class/file name for the generated .razor. Defaults to 'InteractionPrompt'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .razor + .razor.scss. Defaults to 'Code/UI'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "action",
          "description": "Verb woven into the default prompt text ('Press E to <action>'). Defaults to 'use'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "range",
          "description": "Eye-trace reach in world units — how close the crosshair must be to a pressable to show the prompt. Defaults to 120",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "interactionpack",
      "name": "create_carry_system",
      "description": "Generate a first-person pickup / carry / throw component (sealed Component) for physics props. Attach it to the PLAYER (the object that owns the camera). It eye-traces from Scene.Camera for a Rigidbody-bearing GameObject tagged [Property] CarryTag (default 'carryable') within [Property] Range; grabbing routes a host-authoritative [Rpc.Host] request that re-validates the target and caller, hands the object's network ownership to the carrier (GameObject.Network.AssignOwnership), and disables the rigidbody's MotionEnabled while held. The held object follows a hold point ([Property] Vector3 HoldOffset in front of the camera) each FixedUpdate; dropping restores physics, throwing applies an impulse ([Property] float ThrowForce). The held-object id is [Sync(SyncFlags.FromHost)] so proxies see the carrying state, and static OnPickedUp / OnDropped events fire uniformly for SFX/VFX. PAIRS with physics props — give each carryable a Rigidbody + Collider and the CarryTag (set_tags); network-spawn them for multiplayer so ownership + transform replicate. Single-player safe (IsProxy is false and RPCs run locally with no session). Inputs: GrabAction (default 'use') grabs/drops, ThrowAction (default 'attack1') throws. Optionally attach to an existing player GameObject by GUID after a hotload.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'CarrySystem'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "range",
          "description": "Eye-trace reach for grabbing a carryable, in world units. Defaults to 130",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "throwForce",
          "description": "Impulse magnitude applied on throw (scales with the prop's mass — tune per game). Defaults to 20000",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "carryTag",
          "description": "Only objects with this tag (and a Rigidbody) can be picked up; lower-cased/underscored to match s&box tag convention. Defaults to 'carryable'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of the PLAYER GameObject (the one with the camera) to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "interactionpack",
      "name": "create_hold_to_confirm",
      "description": "Generate a hold-to-confirm action component (sealed Component). While a named input action is held (Input.Down), a public Progress value fills 0→1 over [Property] float HoldSeconds; releasing early snaps back to 0, or drains down if [Property] bool DecayOnRelease. Reaching 1 fires the static OnConfirmed(GameObject) event, then a short CooldownSeconds blocks re-triggering. The classic 'hold E to disarm / open / revive' interaction. No UI is generated — read the public Progress (0..1) from your own HUD to draw a radial or bar; a #region Feedback hook marks where to tie in a sound/effect. LOCAL/owner-only: input is IsProxy-guarded so it never fires on proxies and is single-player safe. For a host-authoritative outcome, call an [Rpc.Host] from inside the OnConfirmed subscriber. Attach to the player (or any owned object that reads input); optionally attach to an existing GameObject by GUID after a hotload.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'HoldToConfirm'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "action",
          "description": "Input action name that must be held (must exist in the project's Input settings — see ensure_input_action). Defaults to 'use'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "holdSeconds",
          "description": "Seconds of continuous hold required to confirm. Defaults to 1.5",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "decayOnRelease",
          "description": "Baked default for DecayOnRelease: if true, releasing early drains Progress back down instead of snapping to 0 (editable per-instance). Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a GameObject to attach the component to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "align_objects",
      "description": "Align several GameObjects on one axis so they share a coordinate. mode = first (match the first object), min, max, or average; defaults to first. Returns { aligned, axis, mode, target } — aligned is the object count and target the shared coordinate; verify positions with get_scene_hierarchy or a screenshot.",
      "params": [
        {
          "name": "ids",
          "description": "GUIDs of the GameObjects to align (>= 2)",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "axis",
          "description": "Axis to align on",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "x",
              "y",
              "z"
            ]
          }
        },
        {
          "name": "mode",
          "description": "Target coordinate to align to (default first)",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "first",
              "min",
              "max",
              "average"
            ]
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "distribute_objects",
      "description": "Evenly space GameObjects along an axis between the lowest and highest (keeps the two ends fixed, spreads the rest evenly). Returns { distributed, axis, from, to } — the object count and the fixed end coordinates the rest were spread between.",
      "params": [
        {
          "name": "ids",
          "description": "GUIDs of the GameObjects to distribute (>= 3)",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "axis",
          "description": "Axis to distribute along",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "x",
              "y",
              "z"
            ]
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "grid_duplicate",
      "description": "Clone a GameObject into an X/Y/Z grid with fixed spacing (the original stays in place). Each count is clamped to 50 and total clones to 500. Great for fences, crates, pillars, foliage rows.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to clone",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "countX",
          "description": "Copies along X (default 1)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "countY",
          "description": "Copies along Y (default 1)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "countZ",
          "description": "Copies along Z (default 1)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "spacing",
          "description": "Spacing between copies per axis (default 100,100,100)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "group_objects",
      "description": "Parent a set of GameObjects under a new empty group object (placed at their centroid) — tidies the hierarchy and lets you move/rotate them together.",
      "params": [
        {
          "name": "ids",
          "description": "GUIDs of the GameObjects to group",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "name",
          "description": "Name for the group object (default 'Group')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "measure_distance",
      "description": "Measure the distance between two points or two GameObjects. Provide a/b as {x,y,z} or idA/idB as GUIDs. Returns straight-line distance, horizontal (ground) distance, and the delta vector. Read-only (works during play).",
      "params": [
        {
          "name": "a",
          "description": "First point {x,y,z}",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "b",
          "description": "Second point {x,y,z}",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "idA",
          "description": "First GameObject GUID (overrides a)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "idB",
          "description": "Second GameObject GUID (overrides b)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "randomize_transforms",
      "description": "Add natural variation to existing objects: random yaw and/or random uniform scale within a range. Great for breaking up repetition in placed foliage/rocks/crates. Seeded — the same seed reproduces the same layout. Returns { randomized, seed } (the count of objects changed); scale only varies when scaleMax > scaleMin.",
      "params": [
        {
          "name": "ids",
          "description": "GUIDs of the GameObjects to randomize",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "randomYaw",
          "description": "Randomize Z rotation (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "scaleMin",
          "description": "Min uniform scale (default 1)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "scaleMax",
          "description": "Max uniform scale (default 1; set >min to vary)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "seed",
          "description": "PRNG seed (default 1)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "scatter_props",
      "description": "Scatter N copies of a model randomly within a radius around a center point — instant foliage, rocks, debris. Each copy gets a random yaw and (by default) is snapped to the ground. Seeded for reproducibility; copies are grouped under one parent by default. Count capped at 300. Returns { scattered, groupId, seed } — individual prop GUIDs are not returned, so use groupId with get_scene_hierarchy (rootId) to enumerate them, or with set_transform/delete_gameobject to move/remove the whole batch.",
      "params": [
        {
          "name": "model",
          "description": "Model path to scatter, e.g. 'models/dev/box.vmdl'",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "center",
          "description": "Centre of the scatter area (default origin)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "radius",
          "description": "Scatter radius in units (default 256)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "count",
          "description": "How many to place (default 10, max 300)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "randomYaw",
          "description": "Randomly rotate each around Z (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "snapToGround",
          "description": "Raycast each onto the surface below (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "scaleMin",
          "description": "Min uniform scale (default 1)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "scaleMax",
          "description": "Max uniform scale (default 1; set >min for size variation)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "tint",
          "description": "Tint applied to every copy — object {r,g,b,a} or comma string \"r,g,b,a\"",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "seed",
          "description": "PRNG seed for a reproducible layout (default 1)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "group",
          "description": "Parent all copies under one group object (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "name",
          "description": "Base name for the props/group (default 'Prop')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "leveltools",
      "name": "snap_to_ground",
      "description": "Drop a GameObject straight down onto the surface below it (physics raycast). Works best on collider-less props (an object with its own collider may self-hit). Optional offset lifts it off the surface. Returns { snapped, groundZ, gameObject } with the object's updated transform — or { snapped: false, reason } (not an error) when no ground was hit below.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to snap",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "offset",
          "description": "Height above the surface to place it (default 0)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "startHeight",
          "description": "How far above the object to start the trace (default 2000)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "maxDistance",
          "description": "Max trace distance downward (default 20000)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "looteconomy",
      "name": "create_currency_pickup",
      "description": "Generate a networked coin / currency pickup component (sealed, Component.ITriggerListener). Host-spawned; when a GameObject carrying PlayerTag ('player') enters its trigger the HOST validates and grants Value (default 1) into a wallet on the player, then destroys the pickup network-wide (the host Destroy() replicates — there is no NetworkDestroy on this SDK). Optional magnet: while MagnetRadius (default 0 = off) is > 0 the coin accelerates toward the nearest player each FixedUpdate (host-side, capped by MaxMagnetSpeed). IsProxy guards keep the grant + despawn host-only in multiplayer (NetworkSpawn the coin on the host); single-player works with no networking. The deposit is reflection-free and dependency-free: a static Grant seam is wired ONCE to the direct typed call — player.Components.Get<EconomyWallet>()?.AddMoney(amount) — so the component compiles with NO hard reference to a specific wallet class (rename the wallet type if yours differs; mirrors create_pickup's self-contained convention). WalletComponentName (default 'EconomyWallet') is used to locate the wallet and name the fix if Grant is left unwired (never silent). Pairs with create_economy_wallet (AddMoney/TrySpend/CanAfford) and create_floating_combat_text (spawn a '+N' popup from OnCollected).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'CurrencyPickup'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "value",
          "description": "How much currency the pickup grants into the wallet. Defaults to 1",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "magnetRadius",
          "description": "Magnet range in world units — within it the coin flies to the nearest player each FixedUpdate. 0 = magnet off. Defaults to 0",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "walletComponentName",
          "description": "Type name of the wallet component to deposit into (used to locate it and to name the fix if the Grant seam is left unwired). Defaults to 'EconomyWallet'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a coin GameObject to attach to — give it a trigger Collider (SphereCollider, IsTrigger=true). Only attaches if the type is already loaded — hotload first",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "looteconomy",
      "name": "create_gacha_drop_table",
      "description": "Generate a host-authoritative gacha / loot-box roller component. Two-level pick: parallel [Property] lists RarityNames + RarityWeights select a RARITY by cumulative weight (the create_weighted_loot_table shape), then a flat 'Rarity:Item' [Property] list (e.g. 'Legendary:Dragon Fang') picks an ITEM uniformly within that rarity — simple and inspector-editable. A pity counter (PityAfter, default 50) guarantees the rarest tier (the LAST entry in RarityNames) after N rolls without it and resets on a hit. Duplicate detection against an owned-items set fires a host-side OnDuplicate hook (marked TODO: convert dupes to shards/currency). Roll() routes to the host via an [Rpc.Host] RequestRoll (Rpc.Caller re-validated — NetFlags is not security) and the result fans out via [Rpc.Broadcast] so every machine fires the static OnRolled(rarity, item, isDuplicate) event; single-player safe (RPCs run locally). Use create_weighted_loot_table instead for a simpler single-tier weighted pick with no pity/dupe/networking. Pairs with create_economy_wallet (spend currency to roll) and create_inventory (store the pulls).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'GachaDropTable'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "pityAfter",
          "description": "Rolls without a rarest-tier hit before the next roll is guaranteed rarest. 0 disables pity. Defaults to 50",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a per-player/manager GameObject to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "looteconomy",
      "name": "create_offline_progress",
      "description": "Generate an offline / idle-progress component (sealed, owner/host-only) — the idle-game staple. Persists LastSeenUtc (DateTime) to FileSystem.Data JSON on a dirty-flag autosave heartbeat (AutosaveSeconds) and on OnDisabled, copying create_save_system's persistence patterns. On enable it computes elapsed = now − LastSeenUtc, guards a clock rollback (negative → 0), clamps to MaxOfflineHours (default 8), then replays that time through a SimulateOffline(double seconds) TODO hook in fixed TickSeconds chunks (default 1) so idle accumulation is deterministic (frame-rate independent), and fires the static OnOfflineProgressApplied(seconds) event (drive a 'welcome back, you earned X' screen). IsProxy-guarded so a client can't author their own offline earnings. Fill in the SimulateOffline hook with your idle math (e.g. wallet.AddMoney(rate*seconds)). Pairs with create_economy_wallet / create_save_system / create_stat_modifier_system.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'OfflineProgress'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxOfflineHours",
          "description": "Offline time is clamped to this many hours (stops a week-away paying out a week). Defaults to 8",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "tickSeconds",
          "description": "SimulateOffline chunk size in seconds — smaller = finer-grained deterministic replay (floored at 0.1). Defaults to 1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an idle/save-manager GameObject to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "materials",
      "name": "assign_material",
      "description": "Apply a material to a GameObject by setting its ModelRenderer's MaterialOverride (overrides the whole model's material). Requires an existing ModelRenderer (assign_model first) and errors if the material path can't be loaded. Returns { assigned, id, material } — tweak values afterwards with set_material_property.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "material",
          "description": "Material path (e.g. 'materials/walls/brick.vmat')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "slot",
          "description": "Material slot index. Defaults to 0 (first slot)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "materials",
      "name": "assign_model",
      "description": "Set a 3D model on a GameObject's ModelRenderer. Creates the renderer component if it doesn't exist; errors if the model path can't be loaded. Returns { assigned, id, model } — follow with assign_material / set_material_property to style it, or take a screenshot to verify.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "model",
          "description": "Model path (e.g. 'models/citizen/citizen.vmdl', 'models/dev/box.vmdl')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "materials",
      "name": "create_material",
      "description": "Create a new material file (.vmat, KV1 format) with a shader and properties like color, roughness, metallic, texture. Errors if the file already exists; when no properties are given it writes sensible PBR defaults (g_flMetalness 0, g_flRoughness 1). Returns { created, path, shader, propertiesWritten } — pass the returned path to recompile_asset (so the editor compiles it) and then assign_material.",
      "params": [
        {
          "name": "path",
          "description": "Relative path for the material (e.g. 'materials/walls/brick.vmat')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "shader",
          "description": "Shader to use. Defaults to 'shaders/complex.shader' (PBR)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "properties",
          "description": "Material properties as key-value pairs (e.g. { \"Color\": \"#ff0000\", \"Roughness\": 0.8 })",
          "optional": true,
          "type": {
            "kind": "any"
          }
        }
      ]
    },
    {
      "module": "materials",
      "name": "set_material_property",
      "description": "Change a property on the material assigned to a GameObject — color, roughness, metallic, texture, etc. Operates on the ModelRenderer's MaterialOverride; if none is assigned it auto-creates one from the default complex shader (no separate assign_material step needed). Returns { set, id, property, autoCreatedMaterial } — screenshot to verify the visual change.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "Material property name (e.g. 'Color', 'Roughness', 'Metalness', 'Normal')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "value",
          "description": "Property value — number for floats, string for texture paths/colors, {r,g,b,a} for colors",
          "optional": false,
          "type": {
            "kind": "any"
          }
        }
      ]
    },
    {
      "module": "moviemaker",
      "name": "add_movie_player",
      "description": "Add a Sandbox.MovieMaker.MoviePlayer component and optionally wire a .movie resource into it — the cutscene playback primitive. Creates a new 'Movie Player' GameObject when no id is given. Set playOnStart to begin playback the moment play mode starts (intro cinematics), or leave it and trigger via play_movie (scripted cutscenes — call it from a trigger zone or dialogue beat). isLooping + timeScale map straight onto the component. Movies must already exist as .movie assets (list_movies; author in the Movie Maker dock). Scene-mutating — refused during play mode.",
      "params": [
        {
          "name": "id",
          "description": "GameObject GUID to attach to. Omit to create a new 'Movie Player' object",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "moviePath",
          "description": "Asset-relative path of the .movie resource to wire (see list_movies)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "isLooping",
          "description": "Loop playback",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "timeScale",
          "description": "Playback speed multiplier (1 = normal)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "createTargets",
          "description": "Let the player create missing track-target objects on play",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "playOnStart",
          "description": "Begin playing as soon as play mode starts (intro cinematic)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "moviemaker",
      "name": "list_movies",
      "description": "List the project's .movie resources (Sandbox.MovieMaker clips authored in the editor's Movie Maker dock: Window → Movie Maker). Scans the ENTIRE Assets folder recursively and returns every .movie found — no limit or paging. Returns { count, movies, note } where each movie has { path (asset-relative — the form add_movie_player/play_movie expect), name, loadable (resolves via ResourceLibrary), hasCompiledClip }. Start here before add_movie_player / play_movie — if the list is empty, the movie has to be authored in the dock first (the bridge plays movies; it doesn't author keyframes).",
      "params": []
    },
    {
      "module": "moviemaker",
      "name": "play_movie",
      "description": "Start MoviePlayer playback. Targets the MoviePlayer on the given GameObject, or the first MoviePlayer in the scene when id is omitted. Pass moviePath to load-and-play a different .movie on the same player; positionSeconds seeks before playing; isLooping/timeScale apply immediately. Clips genuinely advance in PLAY MODE (start_play first, then verify with capture_view) — in edit mode this only sets state, which the response calls out. NOT scene-mutating, so it works during play mode.",
      "params": [
        {
          "name": "id",
          "description": "GameObject GUID holding the MoviePlayer. Omit to use the first MoviePlayer in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "moviePath",
          "description": "Asset-relative .movie path to load and play (otherwise plays the wired Resource)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "positionSeconds",
          "description": "Seek to this time (seconds) before playing",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "timeScale",
          "description": "Playback speed multiplier (1 = normal)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "isLooping",
          "description": "Loop playback",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "moviemaker",
      "name": "stop_movie",
      "description": "Stop MoviePlayer playback (the counterpart to play_movie). Targets the MoviePlayer on the given GameObject, or the first MoviePlayer in the scene when id is omitted. Pass rewind to also reset the playhead to 0 so the next play_movie starts from the top. Works during play mode.",
      "params": [
        {
          "name": "id",
          "description": "GameObject GUID holding the MoviePlayer. Omit to use the first MoviePlayer in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "rewind",
          "description": "Also reset the playhead to 0",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "navigation",
      "name": "bake_navmesh",
      "description": "Enable + bake the active scene's navigation mesh so NavMeshAgents can pathfind. This is an editor operation (NavMesh.BakeNavMesh), not a component. The bake runs ASYNC — it returns immediately with baking:true; the editor shows a progress bar and isGenerating flips false when done (give it a moment before querying paths). Optional agent params let you size the mesh to your characters.",
      "params": [
        {
          "name": "agentRadius",
          "description": "Agent radius (default scene setting)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "agentHeight",
          "description": "Agent height",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "agentStepSize",
          "description": "Max step-up height",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "agentMaxSlope",
          "description": "Max walkable slope, degrees",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "includeStaticBodies",
          "description": "Include static physics bodies in the bake",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "navigation",
      "name": "get_navmesh_path",
      "description": "Query the baked navmesh for a walkable path between two world points (NavMesh.GetSimplePath). Returns the ordered path points, or reachable:false if no route exists. Requires bake_navmesh to have run first. Read-only — useful for validating connectivity, AI patrol routes, and spawn reachability.",
      "params": [
        {
          "name": "from",
          "description": "Start point (world space)",
          "optional": false,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "to",
          "description": "Destination point (world space)",
          "optional": false,
          "type": {
            "kind": "vector3"
          }
        }
      ]
    },
    {
      "module": "netprimitives",
      "name": "add_host_migration_recovery",
      "description": "Generate a host-migration recovery component — it detects when THIS machine takes authority over its GameObject (the proxy→authority transition that promotes a client to host when the old host leaves) and gives you a clean hook to rebuild host-only state. It tracks IsProxy each frame; when it flips from true (someone else was the authority) to false (now it's us), it fires the static OnBecameHost(GameObject) event and runs a virtual-style TODO rebuild region, then — after a tunable SettleSeconds delay so in-flight packets can land — runs a deferred validation region. s&box has a known bug where networked objects are often destroyed during host migration and the new host inherits stale transient state, so the recommended shape is: detect becoming host, aggressively rebuild (re-arm host-only loops/TimeUntil timers against your clock, TakeOwnership of orphans, rebuild handle maps by world position, reconcile the [Sync] registry against the real scene), and defer the sanity check ~1s. Inert offline (IsProxy is always false with no session). Attach to your host-authoritative manager object (NetworkSpawn'd). Next steps: hotload, attach, subscribe to OnBecameHost, and fill the two TODO regions. Requires a real host migration (a second client that becomes host) to fire — it can't be exercised in a solo playtest. Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'HostMigrationRecovery'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a GameObject to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "netprimitives",
      "name": "add_targeted_rpc",
      "description": "Generate a component demonstrating the unicast (single-client) RPC pattern via Rpc.FilterInclude. A normal [Rpc.Broadcast] runs on EVERY machine; the generated component's host-side SendTo(Connection target, string message) wraps its [Rpc.Broadcast] call in `using ( Rpc.FilterInclude( target ) )` so ONLY the target connection executes the body, which raises the static OnReceived(string) event. This is the RIGHT way to send something to one player — a private prompt, a personal reward toast, a per-player cutscene cue — instead of broadcasting to everyone and filtering on the client (which leaks data and wastes bandwidth). Call SendTo on the host (guarded behind Networking.IsActive so Networking.IsHost can't throw with no session; runs locally in solo). Attach to a networked manager object that is NetworkSpawn'd. Next steps: hotload, attach, call SendTo(player.Network.Owner, \"...\") from the host, and subscribe to OnReceived on the target client. Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'TargetedRpc'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a GameObject to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "netprimitives",
      "name": "create_host_rpc_action",
      "description": "Generate a validated, rate-limited host-action component — the SAFE skeleton for 'a client asks the host to DO something' (buy, use, vote, interact, spend). The generated sealed Component exposes a client-callable Request() that forwards to an [Rpc.Host] SubmitRequest() which runs ON THE HOST: it re-resolves WHO called it via Rpc.Caller (never trusting client args for identity), enforces a per-SteamId cooldown backed by a Dictionary<ulong, TimeSince>, runs a clearly-marked TODO block for your authoritative action, and fires the static OnActionExecuted(Connection) event. This is the correct answer to the #1 multiplayer exploit class: [Rpc.Host] is callable by ANY client with forged args — NetFlags restrict who may INVOKE, which is not security — so identity + rate-limit + validation all live inside the host body. Single-player safe (no session → the RPC runs locally, caller falls back to Connection.Local). Attach it to the object that owns the action (a player, a station, or your game manager). Next steps: hotload, tag/attach, call Request() from input or a UI button, fill the TODO host block, and subscribe to OnActionExecuted. Covers the backlog's add_rate_limited_rpc. Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'HostRpcAction'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "cooldownSeconds",
          "description": "Minimum seconds between accepted requests, per calling player (the per-SteamId rate limit). Defaults to 1",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a GameObject to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "netprimitives",
      "name": "create_local_player_resolver",
      "description": "Generate a proxy-safe 'who is MY player?' resolver — the corpus footgun-killer that stops you running local-player logic against a proxy of someone else's player. The generated sealed Component exposes a static Local property that lazily finds the player GameObject belonging to THIS machine: online it's the tagged object whose Network.Owner is the local connection (Network.Owner == Connection.Local, or Network.IsOwner); offline/solo (no session) it's the first/only tagged player. The result is cached and revalidated with IsValid() so a destroyed or respawned player is re-resolved automatically. Also exposes a static IsLocal(GameObject) helper for filtering events (e.g. only open a station overlay for your own player). Works identically online and offline — no `if (Network.IsOwner)` guard that silently disables everything in a solo playtest. Attach ONE to a persistent object (your game manager) so PlayerTag is configurable in the inspector; the resolver is static and callable from anywhere. Next steps: hotload, attach, tag each player GameObject with the PlayerTag, then read <Name>.Local / filter with <Name>.IsLocal(go). Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'LocalPlayerResolver'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "playerTag",
          "description": "Tag that marks a player GameObject (players must carry this tag). Baked as the default PlayerTag, editable per-instance in the inspector. Defaults to 'player'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of a GameObject to attach to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "add_network_helper",
      "description": "Add a NetworkHelper component (with StartServer=true) to an existing GameObject for quick multiplayer setup — at runtime it creates the lobby and spawns the player prefab per connection. Returns { added, id, component:'NetworkHelper' }. NOTE: the current handler requires id (create a holder with create_gameobject first) and does not apply maxPlayers/playerPrefab — wire PlayerPrefab afterward with set_prefab_ref/set_property",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to attach to. Required in practice — the current handler errors when omitted (create a holder with create_gameobject first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "name",
          "description": "Rename the target GameObject to this. Omit to keep its current name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxPlayers",
          "description": "Maximum number of players in the lobby (currently not applied by the handler)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "playerPrefab",
          "description": "Path to the player prefab to spawn for each connection (currently not applied by the handler — set the NetworkHelper's PlayerPrefab afterward with set_prefab_ref)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "add_rpc_method",
      "description": "Generate an RPC method stub in a C# script. Inserts the chosen RPC attribute ([Rpc.Broadcast] all clients, [Rpc.Host] host only, [Rpc.Owner] owner only) above a method with an empty body. Pass methodParams to give it a parameter list (e.g. 'Vector3 pos, int damage'); the body is left as a TODO for you to fill in",
      "params": [
        {
          "name": "path",
          "description": "Relative path to the script file",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "methodName",
          "description": "Name for the RPC method",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "rpcType",
          "description": "RPC type: Broadcast (all), Host (server), Owner (owning client). Defaults to Broadcast",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "Broadcast",
              "Host",
              "Owner"
            ]
          }
        },
        {
          "name": "methodParams",
          "description": "Optional parameter list for the RPC method signature, e.g. 'Vector3 pos, int damage'. Omit for a parameterless method",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "body",
          "description": "Currently ignored — not yet implemented. The addon emits an empty method body; fill it in yourself afterward",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "add_sync_property",
      "description": "Annotate an EXISTING public property in a C# script with the [Sync] attribute so s&box replicates it across the network. This does NOT create a new property — the property named by `propertyName` must already be declared in the file; the tool only inserts the [Sync] attribute above it. Returns { added, path, property, attribute } — attribute echoes the exact [Sync...] emitted; errors if the property already has [Sync] or isn't found. Follow with trigger_hotload, then get_compile_errors",
      "params": [
        {
          "name": "path",
          "description": "Relative path to the script file (e.g. 'code/Player.cs')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "propertyName",
          "description": "Name of the existing public property to annotate with [Sync]",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "propertyType",
          "description": "Currently ignored — not yet implemented. The addon only annotates an existing property; it does not declare a new one, so the type comes from the existing declaration",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "syncFlags",
          "description": "Optional SyncFlags to emit as [Sync( SyncFlags.X )] — e.g. 'Interpolate' (smooth interpolation), 'Query', 'FromHost'. Omit for a plain [Sync]",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "defaultValue",
          "description": "Currently ignored — not yet implemented. The addon does not create or initialize a property, so no default is written",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "configure_network",
      "description": "Configure lobby settings on Sandbox.Networking. Currently only lobbyName is applied (sets Networking.ServerName) — Networking.MaxPlayers is read-only on this SDK, and playerPrefab/startServer are not applied by the handler (use add_network_helper + set_prefab_ref for those). Returns { configured, maxPlayers, serverName } reflecting the live Networking values",
      "params": [
        {
          "name": "maxPlayers",
          "description": "Maximum number of players (currently not applied — Networking.MaxPlayers is read-only on this SDK; the live value is echoed in the response)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "lobbyName",
          "description": "Display name for the lobby (sets Networking.ServerName — the only setting this handler applies)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "playerPrefab",
          "description": "Path to the player prefab (currently not applied by the handler — set the NetworkHelper's PlayerPrefab via set_prefab_ref instead)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "startServer",
          "description": "Start the server/lobby immediately (currently not applied by the handler — add_network_helper sets StartServer=true on the NetworkHelper)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "create_lobby_manager",
      "description": "Generate a lobby manager Component implementing Component.INetworkListener: a static Instance singleton, a [Sync] PlayerCount maintained in OnActive/OnDisconnected, and a LobbyState [Property] that flips to 'playing' when the lobby fills. Writes <name>.cs and returns { created, path, className }. Follow with trigger_hotload, then get_compile_errors, then place via add_component_to_new_object",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'LobbyManager'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxPlayers",
          "description": "Currently not applied by the handler — the generated MaxPlayers [Property] defaults to 16; tune it per-instance with set_property",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "create_network_events",
      "description": "Generate a network event relay Component: [Rpc.Broadcast] SendEvent(eventName, payload) to all clients and [Rpc.Host] SendEventToHost(...), both dispatching into a local OnNetworkEvent switch you extend. It does NOT implement INetworkListener — use create_lobby_manager for connect/disconnect hooks. Writes <name>.cs and returns { created, path, className }. Follow with trigger_hotload, then get_compile_errors",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'NetworkEvents'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "includeChat",
          "description": "Currently not applied by the handler — no chat system is generated",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "create_networked_player",
      "description": "Generate a network-aware player controller with [Sync] properties, owner-only input, and [Rpc.Broadcast] actions. Writes <name>.cs and returns { created, path, className }. The generated Component syncs PlayerName/Health, moves a CharacterController from Input.AnalogMove behind an IsProxy guard, and exposes a [Property] MoveSpeed plus an [Rpc.Broadcast] TakeDamage(int). Follow with trigger_hotload, then get_compile_errors, then attach + network_spawn",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'NetworkedPlayer'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "moveSpeed",
          "description": "Movement speed (generated MoveSpeed [Property]). Defaults to 200",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "includeHealth",
          "description": "Currently not applied by the handler — the [Sync] Health and [Rpc.Broadcast] TakeDamage are always generated",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "get_network_status",
      "description": "Check the current multiplayer status. Returns { isActive, isHost, isClient, isConnecting, maxPlayers, serverName } read from Sandbox.Networking — meaningful mostly in play mode with networking active. It does NOT return a player list or networked-object dump; use inspect_networked_object for a specific object's Network/[Sync] state",
      "params": []
    },
    {
      "module": "networking",
      "name": "network_spawn",
      "description": "Network-enable a GameObject so it is synchronized across all connected clients. Calls NetworkSpawn(). Returns { spawned, id } on success — follow with inspect_networked_object to confirm the Network state, or set_ownership to hand it to a connection",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to network",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "networking",
      "name": "set_ownership",
      "description": "Assign network ownership of a GameObject to a connection, or drop ownership. Omitting connectionId (or passing an empty string) calls Network.DropOwnership(); passing a connection Id or SteamId calls Network.AssignOwnership() on the matching live Connection. Returns { ownershipAssigned, id, connectionId } or { ownershipDropped, id }; errors if no connection matches (connections only exist while networking is active)",
      "params": [
        {
          "name": "id",
          "description": "GUID of the networked GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "connectionId",
          "description": "Connection Id GUID or SteamId of the target connection. Omit OR pass an empty string to DROP ownership (there is no take-ownership mode in the current handler)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "npc",
      "name": "assign_patrol_route",
      "description": "Wire a placed route (or an arbitrary ordered GUID list) into an NpcBrain's Waypoints list on a target NPC. This is the list-of-GameObject-references case that plain set_property can't express. Pass either waypointIds (explicit order) or routeId (a route parent whose children become the waypoints in hierarchy order). The list count is returned; List<GameObject> refs may read back as handles/GUIDs via get_property, so trust the count or confirm patrol in play mode.",
      "params": [
        {
          "name": "npcId",
          "description": "GUID of the GameObject holding the NpcBrain (or any component with a List<GameObject> waypoint property).",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "waypointIds",
          "description": "Ordered waypoint GameObject GUIDs (e.g. from place_patrol_route). Takes precedence over routeId.",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "routeId",
          "description": "A route parent GUID whose children (in hierarchy order) become the waypoints.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "The List<GameObject> property name to set. Defaults to 'Waypoints'. (Use 'SpawnPoints' to wire spawn points on a spawner.)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "npc",
      "name": "create_npc_brain",
      "description": "Generate an NpcBrain Component: a behavior state machine (Idle/Patrol/Wander/Chase/Search/Flee/Ambush) driven by occlusion-aware perception — FOV cone + sight range + a line-of-sight trace (respects walls/trees) + proximity hearing — with last-known-position memory (lose-LOS -> search -> give up -> resume). This is the decision layer on top of bake_navmesh / NavMeshAgent movement. Pick a behavior preset, then tune via the generated [Property] fields with set_property. After generating: trigger_hotload + get_compile_errors, place a route with place_patrol_route + assign_patrol_route, bake_navmesh, and verify perception in EDIT mode with simulate_npc_perception (chase/search behavior needs play mode). The component is added to a GameObject like any other; it auto-adds a NavMeshAgent in OnStart.",
      "params": [
        {
          "name": "name",
          "description": "Class/file name. Defaults to 'NpcBrain'. Sanitized to a valid C# identifier.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under the project root for the .cs file. Defaults to 'Code'.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "behavior",
          "description": "Preset (sets StartState + flee toggle): 'patrol' (walk waypoints), 'guard' (Ambush near spawn until a target enters range), 'hunter' (patrol->chase->search, the Sasquatch), 'swarm' (wander/idle->chase nearest, RUN mobs), 'skittish' (chase but flee on low health). The generated file is the same shape; the preset just changes defaults. Defaults to 'hunter'.",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "patrol",
              "guard",
              "hunter",
              "swarm",
              "skittish"
            ]
          }
        },
        {
          "name": "targetTag",
          "description": "Tag the NPC hunts (its candidates are GameObjects with this tag). Defaults to 'player'.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "moveSpeed",
          "description": "Patrol/wander speed (NavMeshAgent MaxSpeed). Default 130.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "chaseSpeed",
          "description": "Chase/flee speed. Default 200.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "sightRange",
          "description": "Max sight distance. Default 1500.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "fovDegrees",
          "description": "Full field-of-view cone angle in degrees. Default 110. (Baked into a cosine threshold for cheap, trig-free checks.)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "eyeHeight",
          "description": "Trace origin height above the NPC's feet. Default 64.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "hearingRadius",
          "description": "Proximity-hearing radius — a target within it is investigated (sets last-known-pos) but NOT instantly aggroed. Default 600.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "giveUpTime",
          "description": "Seconds to search after losing line-of-sight before giving up and resuming the start state. Default 6.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "searchRadius",
          "description": "Wander radius around the last-known position while searching. Default 400.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "waypointStopDistance",
          "description": "How close the NPC must get to a waypoint/target before it counts as reached. Default 80.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "canFlee",
          "description": "Enable the Flee state (else the NPC never flees). Defaults from the preset.",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "fleeHealthFrac",
          "description": "Flee when CurrentHealthFrac drops to/below this (the game sets CurrentHealthFrac 0..1). Default 0.25.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "networked",
          "description": "When true (default), emit a host-authoritative brain: 'if (IsProxy) return;' + [Sync] CurrentState. NOTE: a no-session solo playtest makes everything a proxy, so a networked brain won't think until a host session exists — pass false to iterate solo in the edit scene.",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "npc",
      "name": "create_npc_spawner",
      "description": "Generate a spawner Component that instantiates an NPC prefab over time / in escalating waves at spawn points, capped by maxAlive. RUN's swarm backbone and Sasquatched's round-start spawn. After generating: set NpcPrefab via set_prefab_ref, set SpawnPoints (reuse place_patrol_route to make a set of empties, then assign_patrol_route with property='SpawnPoints'), trigger_hotload + get_compile_errors. Verify by watching the GameObject count over time in play mode. Networked spawns use NetworkSpawn() and are host-only.",
      "params": [
        {
          "name": "name",
          "description": "Class/file name. Defaults to 'NpcSpawner'.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under the project root. Defaults to 'Code'.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "mode",
          "description": "'continuous' (one every interval), 'waves' (a batch every interval, waveCount times), 'burst' (one batch then stop). Default 'waves'.",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "continuous",
              "waves",
              "burst"
            ]
          }
        },
        {
          "name": "count",
          "description": "NPCs per wave (waves) or per batch (burst/continuous batch). Default 5.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "interval",
          "description": "Seconds between spawns (continuous) or between waves (waves). Default 8.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "waveCount",
          "description": "Number of waves (waves mode). Default 3.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "waveGrowth",
          "description": "Multiply count each wave (>1 = escalating). Default 1.0.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "radius",
          "description": "Random scatter radius around a spawn point. Default 200.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "maxAlive",
          "description": "Cap on concurrent live NPCs (important so swarms don't melt the frame rate). Default 12.",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "networked",
          "description": "When true (default), spawn via NetworkSpawn() (host-only, try/catch solo-safe) so clients see the NPCs; false = a plain local Clone for solo/edit testing.",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "npc",
      "name": "place_patrol_route",
      "description": "Place a set of waypoint GameObjects (tagged empties) for a patrol route and group them under a parent route object — authorable in one call. Optionally snaps each point to the ground (raycast down) so waypoints sit on the navmesh, not floating. Returns the route parent GUID + ordered waypoint GUIDs to feed into assign_patrol_route. Validate connectivity afterward with get_navmesh_path between consecutive waypoints (catches a 'point in a wall').",
      "params": [
        {
          "name": "points",
          "description": "Ordered world positions for the route (at least 2).",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "vector3"
            }
          }
        },
        {
          "name": "name",
          "description": "Route name. Defaults to 'PatrolRoute'. Waypoints are named <route>_WP0, _WP1, ...",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "tag",
          "description": "Tag applied to each waypoint. Defaults to 'waypoint'.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "snapToGround",
          "description": "Drop each point onto the surface below via a downward raycast. Default true.",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "parentId",
          "description": "Existing parent GameObject GUID to nest the waypoints under; otherwise a new route empty is created at the points' centroid.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "npc",
      "name": "simulate_npc_perception",
      "description": "READ-ONLY edit-mode verifier: evaluate the NPC's perception math RIGHT NOW without entering play mode. Given an NPC (reads its NpcBrain SightRange/FovDegrees/EyeHeight/TargetTag + transform) and either a targetId or a point, it runs the SAME line-of-sight check the brain uses — FOV cone (dot vs the baked cosine), sight-range gate, and an occlusion trace from the eye to the target — and reports the result AND why. This is the keystone verifier: it makes the perception layer checkable in edit mode (no flaky screenshot timing) — e.g. place the Sasquatch, place a camper behind a tree, and confirm the tree blocks LOS. Call params override the brain's values, so it also works before/without an NpcBrain (uses defaults). Safe in play mode too (read-only, like raycast).",
      "params": [
        {
          "name": "npcId",
          "description": "GUID of the NPC GameObject (ideally with an NpcBrain; its perception [Property] values are read).",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of the target GameObject to test visibility to (e.g. a player). Provide this OR point.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "point",
          "description": "A raw world point to test visibility to. Provide this OR targetId.",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "sightRange",
          "description": "Override the sight range for this check (else read from the NpcBrain / default 1500).",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "fovDegrees",
          "description": "Override the FOV cone angle for this check (else read from the NpcBrain / default 110).",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "eyeHeight",
          "description": "Override the eye height for this check (else read from the NpcBrain / default 64).",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "targetTag",
          "description": "Override the target tag (canSee also requires the target to carry this tag; else read from the NpcBrain / default 'player').",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "objecttools",
      "name": "find_objects",
      "description": "Query the scene for GameObjects by name (case-insensitive substring), component type name, and/or tag — combine filters (AND). Returns {id,name} for matches (limit default 50, max 500). Read-only; works during play. Use it to get GUIDs to feed into align/distribute/set_tint/group/delete/etc.",
      "params": [
        {
          "name": "name",
          "description": "Name substring (case-insensitive)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name, e.g. 'PointLight', 'SkinnedModelRenderer'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "tag",
          "description": "Tag the object must have",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "limit",
          "description": "Max results (default 50, max 500)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "objecttools",
      "name": "get_tags",
      "description": "Read the tags currently on a GameObject. (Pair with set_tags to add/remove/clear, and find_objects to query by tag.)",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "objecttools",
      "name": "remove_component",
      "description": "Remove a component from a GameObject by type name (e.g. 'PointLight', 'ModelRenderer'). Removes the first match; pass all:true to remove every matching one. The counterpart to add_component_with_properties.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name to remove",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "all",
          "description": "Remove all matching components, not just the first",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "objecttools",
      "name": "replace_model",
      "description": "Swap the model on one object (id) or many (ids) — e.g. retheme a row of props in one call.",
      "params": [
        {
          "name": "id",
          "description": "Single GameObject GUID",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "ids",
          "description": "Multiple GameObject GUIDs",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "model",
          "description": "New model path, e.g. 'models/dev/sphere.vmdl'",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "objecttools",
      "name": "set_tags",
      "description": "Add, remove, and/or clear gameplay tags on one object (id) or many (ids). Tags drive collision groups, queries, and triggers.",
      "params": [
        {
          "name": "id",
          "description": "Single GameObject GUID",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "ids",
          "description": "Multiple GameObject GUIDs",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "add",
          "description": "Tags to add",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "remove",
          "description": "Tags to remove",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "clear",
          "description": "Remove all existing tags first",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "objecttools",
      "name": "set_tint",
      "description": "Set the renderer tint colour on one object (id) or many (ids) at once. Works on any ModelRenderer/SkinnedModelRenderer. Pass the colour as \"tint\" (or its alias \"color\"); each accepts an object {r,g,b,a} OR a comma string \"r,g,b,a\".",
      "params": [
        {
          "name": "id",
          "description": "Single GameObject GUID",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "ids",
          "description": "Multiple GameObject GUIDs",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "tint",
          "description": "Tint colour to apply (object or comma string)",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "color",
          "description": "Alias for \"tint\" (object or comma string)",
          "optional": true,
          "type": {
            "kind": "color"
          }
        }
      ]
    },
    {
      "module": "physics",
      "name": "add_collider",
      "description": "Add a specific collider component to a GameObject (no Rigidbody — use add_physics for a dynamic body). Can be configured as a trigger. Returns { added, id, collider, isTrigger } where collider is the actual component type added — note 'mesh' maps to HullCollider (s&box has no MeshCollider) and unrecognized types fall back to BoxCollider.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "type",
          "description": "Type of collider to add",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "box",
              "sphere",
              "capsule",
              "mesh",
              "hull"
            ]
          }
        },
        {
          "name": "isTrigger",
          "description": "If true, the collider acts as a trigger (no physics collision). Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "size",
          "description": "Size for BoxCollider (x, y, z dimensions) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "radius",
          "description": "Radius for SphereCollider or CapsuleCollider",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "height",
          "description": "Height/length for CapsuleCollider",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "physics",
      "name": "add_joint",
      "description": "Add a physics joint/constraint component to a GameObject, optionally connected to a target body (targetId). Returns { added, id, joint, targetId } — joint is the component type added (FixedJoint/SpringJoint/SliderJoint). If targetId is omitted the joint is added unconnected; wire it later via set_property. Both objects need physics (add_physics) for the constraint to simulate in play mode.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to add the joint to",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "type",
          "description": "Type of joint to create",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "fixed",
              "spring",
              "slider"
            ]
          }
        },
        {
          "name": "targetId",
          "description": "GUID of the target GameObject to connect to",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "frequency",
          "description": "Spring frequency (spring joints only)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "damping",
          "description": "Damping ratio (spring joints only). 0 = no damping, 1 = critical",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "physics",
      "name": "add_physics",
      "description": "Add a Rigidbody and collider to a GameObject, making it a dynamic physics object. Auto-selects BoxCollider if no collider type specified. Returns { physicsAdded, id, components } listing exactly which components were added (e.g. Rigidbody + BoxCollider) — enter play mode (start_play) to see it simulate.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "collider",
          "description": "Collider type to add. Defaults to 'box'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "box",
              "sphere",
              "capsule",
              "mesh"
            ]
          }
        },
        {
          "name": "mass",
          "description": "Mass of the physics body in kg",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "gravity",
          "description": "Whether gravity affects this object. Defaults to true",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "physics",
      "name": "physics_overlap",
      "description": "Spatial volume query: return the GameObjects whose colliders intersect a SPHERE (center + radius) or a BOX (center + size) — the volume counterpart to raycast's ray. Use it for 'what's near this point' / 'what's inside this trigger volume' checks (proximity, blast radius, spawn-clearance). Read-only.",
      "params": [
        {
          "name": "center",
          "description": "Center of the query volume (world space) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "radius",
          "description": "Sphere radius. Provide this OR size (box), not both",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "size",
          "description": "Full box size (not half-extents). Provide this OR radius",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        }
      ]
    },
    {
      "module": "physics",
      "name": "raycast",
      "description": "Perform a physics raycast (Scene.Trace.Ray) from start to end — pass both; the handler traces the start→end segment. Useful for line-of-sight checks, object placement, and collision detection. Returns { hit, hitPosition, normal, distance, gameObjectId, gameObjectName } — feed gameObjectId into get_all_properties/set_transform, or visualize the result with debug_draw_ray. A 'Default Surface not found' error is a known transient; call restart_editor and retry.",
      "params": [
        {
          "name": "start",
          "description": "Ray start position (world space) — object {x,y,z} or comma string \"x,y,z\"",
          "optional": false,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "end",
          "description": "Ray end position. Use either end or direction+maxDistance",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "direction",
          "description": "Ray direction (normalized). Used with maxDistance instead of end",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "maxDistance",
          "description": "Maximum ray distance when using direction. Defaults to 10000",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "radius",
          "description": "Sphere/box trace radius. 0 = thin ray (default)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "ignoreIds",
          "description": "GUIDs of GameObjects to ignore",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "all",
          "description": "If true, returns all hits along the ray. Defaults to false (first hit only)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "playmode",
      "name": "drive_player",
      "description": "EXPERIMENTAL — Drive the active PlayerController DURING PLAY MODE across multiple frames: synthesize sustained look (EyeAngles), analog movement (wish velocity), and/or hold a named action down long enough that Input.Pressed fires its rising edge. This is the reliable alternative to simulate_input, which only sets an action for ONE frame and so MISSES edge-triggered controls (Input.Pressed) and cannot inject analog move/look at all. Requires start_play first. Runs ASYNC across editor frames and returns immediately — the job keeps applying for `frames` (or `durationMs`) frames. After it runs, confirm with drive_player_status, then verify the actual effect with capture_view / get_runtime_property. Provide at least one of look / lookDelta / move / action.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject holding the controller. Omit to auto-resolve the first PlayerController (or *Controller with EyeAngles/WishVelocity) in the play scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Controller component type name to target (e.g. 'PlayerController'). Omit to auto-detect",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "frames",
          "description": "How many editor frames to drive for (1–1800, ~60/sec). Default 30 (~0.5s). Takes precedence over durationMs",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "durationMs",
          "description": "Duration in ms, converted at ~60fps (ignored if `frames` is given). Default ~500ms",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "look",
          "description": "Absolute EyeAngles target {pitch,yaw,roll} held for the whole duration (aim the camera/body). Object, [pitch,yaw,roll], or 'pitch,yaw,roll'. pitch is clamped to ±89",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "lookDelta",
          "description": "Per-frame EyeAngles delta added each frame (turn/pan over time, e.g. {yaw:2} to sweep right). Combine with `frames` to control total rotation",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "move",
          "description": "Analog movement in the controller's facing frame: x = forward(+)/back(-), y = left(+)/right(-). Magnitude clamped to 1. e.g. {x:1} walks forward for the whole duration",
          "optional": true,
          "type": {
            "kind": "any",
            "unionOf": [
              "object",
              "array",
              "string"
            ]
          }
        },
        {
          "name": "moveSpeed",
          "description": "Units/sec used when synthesizing WishVelocity from `move` (default 160). Ignored if the controller exposes its own AnalogMove field",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "action",
          "description": "A named input action ('jump','use','attack1',…) HELD DOWN every frame for the whole duration, so Input.Pressed catches the edge single-frame simulate_input misses. Auto-released on the final frame",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "playmode",
      "name": "drive_player_status",
      "description": "EXPERIMENTAL — Read the result of the most recently FINISHED drive_player job: which controller members were actually written (EyeAngles / WishVelocity / AnalogMove…), how many frames applied, and why it ended. Because drive_player runs across frames and returns immediately, this is how you confirm it took effect. Returns lastResult=null if no job has finished yet.",
      "params": []
    },
    {
      "module": "playmode",
      "name": "get_runtime_property",
      "description": "Read a component property value during play mode. Must call start_play first",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "Property name to read",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "playmode",
      "name": "is_playing",
      "description": "Check current play state — returns 'playing', 'paused', or 'stopped'",
      "params": []
    },
    {
      "module": "playmode",
      "name": "redo",
      "description": "Re-apply the editor action most recently reverted by undo (editor UndoSystem redo — the counterpart to the undo tool). Returns { redone: true } without details of what was redone, so verify the resulting state with get_scene_hierarchy or get_all_properties.",
      "params": []
    },
    {
      "module": "playmode",
      "name": "set_property",
      "description": "Set a property value on a component (editor mode), and PERSIST it (survives save+reload). Handles primitives, enums, value types (Color/Vector3 as comma strings), AND references: pass an asset PATH for Model/Material/Texture/SoundEvent props, or a GameObject GUID for GameObject/Component-typed props (resolved like set_component_reference). Returns success=false with a clear error if a path/GUID can't be resolved (no more silent null). For wiring object refs prefer set_component_reference; for prefab refs use set_prefab_ref",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "Property name to set",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "value",
          "description": "New value. Primitive: '5', 'true'. Color/Vector3: a comma string ('1,0,0,1' / '0,0,200'), an array ([0,0,200]), or an object ({r,g,b,a} / {x,y,z}). Enum: the member name. Asset ref (Model/Material/...): the asset path e.g. 'models/dev/box.vmdl'. GameObject/Component ref: the target GameObject's GUID. Empty/'null' clears the property",
          "optional": false,
          "type": {
            "kind": "any"
          }
        }
      ]
    },
    {
      "module": "playmode",
      "name": "set_runtime_property",
      "description": "Set a component property value during play mode — tweak values live while the game runs. Errors if the game is not playing (start_play first). Returns { set, id, component, property, value } like set_property; read it back with get_runtime_property. Runtime changes are DISCARDED when play mode stops — use set_property in edit mode to persist.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "Property name to set",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "value",
          "description": "New value",
          "optional": false,
          "type": {
            "kind": "any"
          }
        }
      ]
    },
    {
      "module": "playmode",
      "name": "start_play",
      "description": "Enter play mode — starts running the game in the editor. Scripts execute, physics simulate, everything goes live. Returns { started, method } (method 'EditorScene.Play', or 'SetPlaying (fallback)' with editorErrorSkipped when the safe path failed). While playing, scene-mutating tools refuse — use get/set_runtime_property, capture_view, and playtest, then stop_play to edit again.",
      "params": []
    },
    {
      "module": "playmode",
      "name": "stop_play",
      "description": "Exit play mode — stops the game and returns to editor. All runtime changes are discarded",
      "params": []
    },
    {
      "module": "playmode",
      "name": "take_screenshot",
      "description": "Capture a 1920x1080 PNG from the scene's Main Camera (ONE fixed angle — the camera may not face what you changed; use screenshot_from to aim at a target object/point). Returns { taken, note, path }, but the engine saves to its default screenshots folder regardless of the path param (<sbox install>/screenshots/sbox.<timestamp>.png) — list the newest file there and READ the image to verify.",
      "params": [
        {
          "name": "path",
          "description": "Output path (e.g. 'screenshots/test.png'). Defaults to screenshots/screenshot_{timestamp}.png",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "playmode",
      "name": "undo",
      "description": "Undo the last editor action via the editor's UndoSystem. Safety net for when a change goes wrong. Returns { undone: true } — it does not report WHAT was undone, so re-check state with get_scene_hierarchy or get_all_properties. Reverse it with redo. Note: file writes (write_file/create_script) are not editor undo steps.",
      "params": []
    },
    {
      "module": "playtest",
      "name": "playtest",
      "description": "Run a scripted gameplay-verification sequence in PLAY MODE and get a pass/fail transcript (start_play first; poll playtest_status until finished).\nEach step is an object with ONE verb:\n• { \"move\": {\"x\":1,\"y\":0}, \"frames\":60 } — analog move in the controller frame (x=fwd/back, y=left/right); auto-sets UseInputControls=false and zeroes WishVelocity after. Movement is controller-specific/best-effort.\n• { \"look\": {\"pitch\":0,\"yaw\":90,\"roll\":0} } / { \"lookDelta\": {\"yaw\":2}, \"frames\":30 } — set/sweep EyeAngles.\n• { \"action\": \"use\", \"frames\":20 } — hold a named input action down (rising-edge safe; for use/dig/attack handled by gameplay components).\n• { \"jump\": \"0,0,400\" } — invoke the controller's Jump(velocity).\n• { \"set\": {\"component\":\"PlayerController\",\"property\":\"WorldPosition\",\"to\":\"100,0,0\"} } — set a runtime property (toggles, or teleport via WorldPosition — the robust positioning fallback).\n• { \"wait\": 10 } — advance N frames.\n• { \"capture\": \"after-jump\" } — screenshot the live player-POV camera at this frame; the PNG path is recorded in the transcript (diagnostic, never pass/fail). Pass true for no label.\n• { \"assert\": {\"read\":\"Displacement\",\"op\":\">\",\"value\":50,\"desc\":\"moved >50u\"} } — read a value and compare IN-FRAME. read = \"Displacement\" (scalar distance moved from job start — the clean facing-independent movement proof), \"WorldPosition[.x|.y|.z]\", or \"<Component>.<Property>[.x|.y|.z|.Count]\". op = > < >= <= == != changed. Records pass/fail.\nTip: prove movement with read:'Displacement' op:'>' (facing-independent, unambiguous), and catch transient state (IsAirborne) right after the action.",
      "params": [
        {
          "name": "steps",
          "description": "Ordered step objects (see verbs above). Runs top-to-bottom in the frame loop.",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "any"
            }
          }
        },
        {
          "name": "id",
          "description": "GUID of the player/controller GameObject. Omit to auto-resolve the first PlayerController.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Controller component type to target (e.g. 'PlayerController'). Omit to auto-detect.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "playtest",
      "name": "playtest_status",
      "description": "Poll the playtest started by the playtest tool. While running: { active:true, step, totalSteps, passed, failed }. When done: { finished:true, reason, verdict:'PASS'|'FAIL', passed, failed, stepsRun, totalSteps, controller, controllerResolved, transcript:[...] } — the full per-step pass/fail record, including any capture PNG paths; the finished summary stays readable until the next playtest starts. If none has run yet: { active:false, finished:false }.",
      "params": []
    },
    {
      "module": "prefabs",
      "name": "create_prefab",
      "description": "Save an existing GameObject as a .prefab file. NOTE: this writes a minimal descriptor (name, enabled flag, component TYPE names) — component property values and children are NOT serialized, so it is a lightweight template rather than a full s&box prefab asset. Returns { created, path, sourceId } — pass path to instantiate_prefab or get_prefab_info.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject to save as prefab",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "path",
          "description": "Path for the prefab file relative to project root (e.g. 'prefabs/enemies/grunt.prefab')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "prefabs",
      "name": "get_prefab_info",
      "description": "Get detailed information about a prefab file. Returns { path, name, size, modified, content } — content is the prefab's full raw JSON, so use this to inspect what a prefab actually contains before instantiate_prefab (find prefabs with list_prefabs).",
      "params": [
        {
          "name": "path",
          "description": "Path to the .prefab file (e.g. 'prefabs/enemies/grunt.prefab')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "prefabs",
      "name": "instantiate_prefab",
      "description": "Spawn a prefab instance into the active scene at an optional position and rotation. NOTE: basic instantiation — it creates a new GameObject named after the prefab; components are NOT recreated from the file (the full s&box prefab pipeline isn't wired), so re-add them with add_component_with_properties. Returns { instantiated, prefab, gameObject, note } — gameObject.id is the new GUID for follow-up calls.",
      "params": [
        {
          "name": "path",
          "description": "Path to the .prefab file (e.g. 'prefabs/enemies/grunt.prefab')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position to spawn at — object {x,y,z} or comma string \"x,y,z\". Defaults to origin",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "Rotation as euler angles. Defaults to identity",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "scale",
          "description": "Uniform scale multiplier. Defaults to 1.0",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "parent",
          "description": "GUID of parent GameObject to attach to",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "prefabs",
      "name": "list_prefabs",
      "description": "List all .prefab files in the project. Filter by name or path",
      "params": [
        {
          "name": "filter",
          "description": "Search filter for prefab name or path",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxResults",
          "description": "Maximum results to return. Defaults to 100",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "project",
      "name": "get_project_info",
      "description": "Get information about the current s&box project — path, name, game type, dependencies, and configuration",
      "params": []
    },
    {
      "module": "project",
      "name": "list_project_files",
      "description": "Browse the project file tree. Optionally filter by directory path and/or file extension (e.g. '.cs', '.scene'). Returns { path, count, files } as project-root-relative paths — CAPPED AT 500 files (count reflects the truncated list, with no marker that more exist), so on large projects narrow with path/extension or use find_in_project. Recursive by default.",
      "params": [
        {
          "name": "path",
          "description": "Relative directory path to list (e.g. 'code/Components'). Defaults to project root",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "extension",
          "description": "Filter by file extension, including the dot (e.g. '.cs', '.scene')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "recursive",
          "description": "Whether to list files recursively. Defaults to true",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "project",
      "name": "read_file",
      "description": "Read the contents of a file in the s&box project (scripts, scenes, configs, etc.)",
      "params": [
        {
          "name": "path",
          "description": "Relative path to the file within the project (e.g. 'code/PlayerController.cs')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "project",
      "name": "recompile_asset",
      "description": "Compile a project asset by path — registers it with the editor's AssetSystem then compiles it (e.g. .vmat → .vmat_c). Use after writing/editing an asset with write_file so the change takes effect without a manual editor step. Verified on materials. NOTE: the engine exposes no reachable particle (.vpcf) compiler, so this can't compile particles — author those in s&box's particle editor, then spawn_vpcf plays them.",
      "params": [
        {
          "name": "path",
          "description": "Asset path — project-relative (e.g. 'materials/foo.vmat') or absolute",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "project",
      "name": "write_file",
      "description": "Write or overwrite a file in the s&box project (SILENTLY replaces existing content — read_file first if you need to preserve it). Creates parent directories as needed; paths are confined to the project root (traversal outside it is denied). Returns a confirmation with the path — for C# follow with trigger_hotload so it compiles; for assets (.vmat etc.) follow with recompile_asset.",
      "params": [
        {
          "name": "path",
          "description": "Relative path for the file (e.g. 'code/Components/Health.cs')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "content",
          "description": "The full file content to write",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "publishing",
      "name": "get_package_details",
      "description": "Fetch package information from the s&box package backend (Package.FetchAsync) by ident. Returns { fullIdent, title, summary, description, org } — no download/rating/dependency data is included. Use it to confirm a package exists and what it is before install_asset.",
      "params": [
        {
          "name": "ident",
          "description": "Package identifier (e.g. 'facepunch.flatgrass', 'myorg.mygame')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "publishing",
      "name": "get_project_config",
      "description": "Read the full project configuration from the .sbproj file including title, description, version, type, package references, metadata, and raw JSON",
      "params": []
    },
    {
      "module": "publishing",
      "name": "set_project_config",
      "description": "Update project configuration fields for publishing: title, description, version, type, package ident, summary, visibility. Only provided fields are changed — edits string values in the .sbproj file in place. Returns { updated, path } (the .sbproj path); read the result back with get_project_config to confirm what actually changed.",
      "params": [
        {
          "name": "title",
          "description": "Project display title",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "description",
          "description": "Project description for publishing",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "version",
          "description": "Version string (e.g. '1.0.0', '2.1.3')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "type",
          "description": "Project type: 'game', 'addon', 'library', or 'template'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "packageIdent",
          "description": "Package identifier (e.g. 'myorg.mygame')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "summary",
          "description": "Short summary for asset.party listing",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "isPublic",
          "description": "Whether the project is publicly visible",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "publishing",
      "name": "set_project_thumbnail",
      "description": "Set or update the project thumbnail image (thumb.png) used for publishing. Provide either a source path or base64 image data",
      "params": [
        {
          "name": "sourcePath",
          "description": "Relative path to an image file within the project to use as thumbnail",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "base64",
          "description": "Base64-encoded image data to write as thumbnail",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "format",
          "description": "Image format when using base64 mode. Defaults to 'png'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "png",
              "jpg"
            ]
          }
        }
      ]
    },
    {
      "module": "publishing",
      "name": "validate_project",
      "description": "Validate that the project is ready for publishing. Runs four checks: .sbproj exists, at least one scene, project Ident set, project Title set. Returns { valid, issueCount, issues, checks } — issues are human-readable problems and each checks entry has { check, pass, detail }; fix metadata gaps with set_project_config (it does NOT check compile errors — use get_compile_errors for that).",
      "params": []
    },
    {
      "module": "roundstate",
      "name": "create_round_state_machine",
      "description": "Generate a host-authoritative MULTI-STATE round machine (the complex variant of create_round_phase_machine). Produces one .cs file: a RoundManager singleton component + an abstract RoundState base (Begin/Tick/OnTimeUp/Finish lifecycle with a per-state [Sync(SyncFlags.FromHost)] TimeUntil timer) + one sealed stub class per named state. The manager auto-attaches the state components on start (you only place the manager), ticks ONLY the active state on the host, Advance()s on timeout with index-wrap, SKIPS any state whose CanEnter() returns false, and announces every transition via a static OnStateChanged event plus an [Rpc.Broadcast] mirror so the host fires immediately and proxies converge without waiting a snapshot (the [Sync] index reconciles late joiners). Single-player safe. USE THIS (not create_round_phase_machine) when each phase needs its OWN behaviour — entry side-effects, per-frame Tick logic, a skip condition, or copy-data-out-on-exit; use the phase machine for 3–5 light phases that differ only in duration. Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Manager class name. Defaults to 'RoundManager'. The abstract base is derived from it (RoundManager → RoundState)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file (path override). Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "states",
          "description": "Ordered state names — each becomes a sealed {Name}State stub class. Defaults to [\"Waiting\",\"Active\",\"PostRound\"]",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "string"
            }
          }
        },
        {
          "name": "duration",
          "description": "Default seconds each state lasts (each state also gets its own tunable [Property] Duration). 0 = no auto-advance for a state. Defaults to 30",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "durations",
          "description": "Optional per-state duration override: an array aligned to `states` ([10,120,8]) OR an object keyed by state name ({\"Waiting\":10,\"Active\":120}). Any state not covered falls back to `duration`",
          "optional": true,
          "type": {
            "kind": "any",
            "unionOf": [
              "array",
              "any"
            ]
          }
        },
        {
          "name": "loop",
          "description": "Loop back to the first state after the last (true) or hold on the last state (false). Defaults to true",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach the manager to (only if the type is already loaded — trigger_hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "saveslots",
      "name": "create_save_slots",
      "description": "Generate a multi-slot save MANAGER component (the slot-picker sibling of create_save_system). Use this when the game needs SEVERAL named save slots the player chooses between (New Game / Load Game menu, per-character or per-run saves) — not one silent autosave. Use create_save_system instead when a single implicit save file is enough. Emits one sealed Component that lists / creates / loads / saves / deletes N slots: a lightweight manifest file (saveslots.json) holds per-slot metadata for the picker (Used flag + Name + SavedAtUnix timestamp + PlaytimeSeconds) so listing never loads a heavy payload, and each slot's game state lives in its own saveslot_<i>.json. Versioned SlotData POCO with clamp-on-load Sanitize() and delete-on-version-mismatch; runs only on the owning machine (IsProxy guard). Static OnSlotLoaded / OnSlotSaved / OnSlotDeleted hooks for HUD. Storage stays within the verified FileSystem.Data.ReadJsonOrDefault / WriteJson / DeleteFile surface (index-file pattern, no directory enumeration). Set sceneReconciliation:true to also reconcile scene objects by GameObject.Id on load — records the save marks destroyed are destroyed, survivors repositioned, missing skipped (good for a placeable-world tycoon). Optionally attached to an existing GameObject by GUID (after a hotload).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'SaveSlotManager'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxSlots",
          "description": "How many save slots the manager manages (manifest is normalized to exactly this many, indexed 0..N-1). Clamped to 1..100. Defaults to 3",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "sceneReconciliation",
          "description": "If true, saved records carry each object's GameObject.Id GUID and load reconciles the live scene against them (destroy the save's destroyed records via Scene.Directory.FindByGuid, reposition survivors, skip missing) — call RecordObject(go) to track a placeable. If false (default), the slot save is a plain payload with no scene reconciliation. Defaults to false",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach the manager to (only attaches if the type is already loaded — hotload first)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "scenes",
      "name": "create_scene",
      "description": "Create a new EMPTY .scene file under the project's Assets folder and register it with the AssetSystem so load_scene works immediately. Errors if the scene already exists. NOTE: includeDefaults is not yet honored — the scene is always created empty, so after load_scene add a camera/light/ground yourself (create_gameobject, add_light). Returns a confirmation with the path — pass it to load_scene next.",
      "params": [
        {
          "name": "path",
          "description": "Relative path for the new scene (e.g. 'scenes/level_01.scene')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "name",
          "description": "Display name for the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "includeDefaults",
          "description": "If true, includes a Camera, Directional Light, and ground plane. Defaults to true",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "scenes",
      "name": "list_scenes",
      "description": "List all .scene files in the project with their paths and basic metadata",
      "params": []
    },
    {
      "module": "scenes",
      "name": "load_scene",
      "description": "Open a scene in the s&box editor by its path",
      "params": [
        {
          "name": "path",
          "description": "Relative path to the .scene file (e.g. 'scenes/main.scene')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "scenes",
      "name": "save_scene",
      "description": "Save the currently open scene in the s&box editor (EditorScene.SaveSession) to its existing .scene file — a provided path does NOT redirect the save. Returns a plain 'Scene saved' confirmation. Call after a batch of scene edits so work survives an editor restart.",
      "params": [
        {
          "name": "path",
          "description": "Path to save to. If omitted, saves to the current scene's path",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "scripts",
      "name": "create_script",
      "description": "Create a new C# component script in the project — a minimal s&box Component class (name is sanitized to a valid identifier), or your exact code when content is provided. Errors if the file already exists. Returns { path, created, className } — the new type is NOT live until a recompile, so call trigger_hotload, then attach it with add_component_with_properties (component=className).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the component (e.g. 'PlayerController'). Will also be the filename",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/ to place the script (e.g. 'Components'). Defaults to 'code/'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "description",
          "description": "Description of what this component does — used to generate appropriate code",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "properties",
          "description": "List of [Property] fields to include in the component",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "object"
            }
          }
        },
        {
          "name": "content",
          "description": "Full C# file content. If provided, ignores name/properties and writes this directly",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "scripts",
      "name": "delete_script",
      "description": "Permanently delete a file from the project by its project-relative path (built for C# scripts, but removes any file; no recycle bin, and editor undo cannot restore it). Errors if the file doesn't exist. Returns a confirmation with the path — follow with trigger_hotload so the removed class actually leaves the compiled assembly.",
      "params": [
        {
          "name": "path",
          "description": "Relative path to the script file to delete",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "scripts",
      "name": "edit_script",
      "description": "Edit an existing C# script in place via exact-text find/replace or a full-content overwrite. Errors if the file or the find text isn't found (find/replace replaces ALL occurrences). Returns { path, edited, operation } where operation is 'find_replace' or 'overwrite' — follow with trigger_hotload so the change compiles, then get_compile_errors if in doubt.",
      "params": [
        {
          "name": "path",
          "description": "Relative path to the script file (e.g. 'code/PlayerController.cs')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "operations",
          "description": "List of edit operations to apply in order",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "object"
            }
          }
        }
      ]
    },
    {
      "module": "scripts",
      "name": "trigger_hotload",
      "description": "Force s&box to recompile and hotload all C# scripts immediately. Use after creating or editing scripts to see changes in real-time",
      "params": []
    },
    {
      "module": "selftest",
      "name": "run_self_test",
      "description": "Run an end-to-end health check of the bridge: create a temp object, add a component, assign + measure a model, capture a screenshot, recompile a temp asset, remove the component — then clean it all up, reporting pass/fail per subsystem. Use it to confirm the install works or to catch regressions before a release. Safe and self-cleaning; refuses to run in play mode.",
      "params": []
    },
    {
      "module": "stations",
      "name": "add_interaction_station",
      "description": "Generate a Component.IPressable 'station' prop (crafting bench / shop till / arcade cabinet) that ONE user occupies at a time. Occupancy is host-authoritative: the occupant is a [Sync(SyncFlags.FromHost)] Guid (GameObject/Connection aren't [Sync]-able) and Press() routes the claim to the host via an [Rpc.Host] Occupy(). Includes a reservation grace window (the station stays reserved for its last user for graceSeconds after they leave, so a brief walk-away can't jump the queue), an optional unlock-level gate (users below requiredLevel can't use it — wire the static ResolveUserLevel hook to your progression system to activate it), and an overlay-open hook (a static OnStationOpened(GameObject) event to open your UI, plus an opt-in [Rpc.Broadcast] mirror). Single-player safe. Optionally attached to an existing GameObject by GUID (only after a trigger_hotload). Give the prop a Collider so the player's use key can raycast it. Mined from interaction-station patterns across shipped s&box games.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'InteractionStation'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the .cs file (path override). Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "graceSeconds",
          "description": "Seconds the station stays reserved for its last user after they leave, before anyone else can claim it. 0 = no grace window. Defaults to 5",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "requiredLevel",
          "description": "Unlock-level gate: users below this level can't use the station. 0 = no gate. The gate only bites once you wire the static ResolveUserLevel hook to your progression system. Defaults to 0",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to attach the station component to (only attaches if the type is already loaded — generate, trigger_hotload, then it places; otherwise add it after the hotload)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "status",
      "name": "get_bridge_status",
      "description": "Check the s&box Bridge connection — call this FIRST in a session. Returns a human summary plus JSON: connected, roundTripOk (heartbeat can be fresh while the editor's request loop is stalled — trust roundTripOk), bridgeVersion vs mcpServerVersion + versionsAligned (a mismatch means restart Claude Code / republish the addon), handlerCount, heartbeatAgeMs, latencyMs, and ipcDir (transport is file IPC; host/port are legacy fields).",
      "params": []
    },
    {
      "module": "status",
      "name": "restart_editor",
      "description": "Restart the s&box editor and wait for the bridge to reconnect — closes the C#-edit→recompile loop so addon/bridge changes apply without a manual restart. Relaunches straight back into the current project (EditorUtility.RestartEditor). Saves unsaved scenes by default (pass save:false to discard them). Blocks until the bridge is back (or waitMs elapses), then reports the handler count.",
      "params": [
        {
          "name": "save",
          "description": "Save unsaved scenes before restarting (default true; false discards them)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "waitMs",
          "description": "Max ms to wait for reconnect (default 150000)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        }
      ]
    },
    {
      "module": "templates",
      "name": "create_game_manager",
      "description": "Generate a minimal game-manager Component: a static Instance singleton, [Property] MaxPlayers / GameState, and a Component.INetworkListener OnActive hook that logs player connects. Writes <name>.cs and returns { created, path, className }. NOTE: the includeScore/includeTimer/includeSpawning params are not currently applied — the same minimal manager is always generated (for richer game-loop scaffolds see create_round_phase_machine / create_objective_system / create_economy_wallet). Follow with trigger_hotload, then get_compile_errors, then place via add_component_to_new_object",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'GameManager'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/ for the file",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "includeScore",
          "description": "Include score tracking (currently not applied by the handler)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "includeTimer",
          "description": "Include round timer with countdown (currently not applied by the handler)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "includeSpawning",
          "description": "Include player spawning from prefab at spawn point (currently not applied by the handler)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        }
      ]
    },
    {
      "module": "templates",
      "name": "create_npc_controller",
      "description": "Generate an NPC controller script with NavMeshAgent pathfinding. Supports patrol, chase, and patrol-chase behaviors",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'NpcController'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/ for the file",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "behavior",
          "description": "AI behavior: 'patrol' (follow waypoints), 'chase' (follow player), 'patrol_chase' (patrol until player nearby). Defaults to 'patrol'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "patrol",
              "chase",
              "patrol_chase"
            ]
          }
        },
        {
          "name": "moveSpeed",
          "description": "Movement speed. Defaults to 150",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "chaseRange",
          "description": "Detection range for chase behavior. Defaults to 500",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "templates",
      "name": "create_player_controller",
      "description": "Generate a player controller script with WASD movement, mouse look, jumping, and sprint. Supports first-person, third-person, and top-down movement modes. Optionally places a player rig (GameObject + CharacterController + Camera) in the scene — note the generated component is attached AFTER a trigger_hotload (it isn't in the TypeLibrary until a recompile)",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'PlayerController'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/ for the file",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "type",
          "description": "Movement mode: 'first_person' (mouse-look body+camera, WASD relative to facing), 'third_person' (mouse yaw, WASD relative to facing, boom camera), or 'top_down' (screen-relative WASD, fixed overhead camera, no jump). Defaults to 'first_person'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "first_person",
              "third_person",
              "top_down"
            ]
          }
        },
        {
          "name": "moveSpeed",
          "description": "Movement speed in units/sec. Defaults to 300",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "jumpForce",
          "description": "Jump force (ignored for top_down). Defaults to 350",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "sprintMultiplier",
          "description": "Sprint speed multiplier (held 'run' action). Defaults to 1.5",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "placeInScene",
          "description": "If true, build a player rig in the scene: a GameObject (tagged 'player') with a CharacterController and (unless createCamera=false) a Camera. The generated controller component is NOT attached in this call — trigger_hotload then add_component_with_properties on the returned GameObject. Defaults to false (file-only).",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "createCamera",
          "description": "When placeInScene is true, also create a Camera (FP/TP: child at eye/boom offset; top_down: fixed overhead). Defaults to true.",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "spawnPosition",
          "description": "When placeInScene is true, the world position to spawn the player rig at — object {x,y,z} or comma string \"x,y,z\". Defaults to the origin.",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        }
      ]
    },
    {
      "module": "templates",
      "name": "create_trigger_zone",
      "description": "Generate a trigger-zone Component (Component.ITriggerListener): auto-adds a trigger BoxCollider on start, filters entrants by a TriggerTag [Property] (default 'player'), and logs enter/exit via private OnPlayerEnter/OnPlayerExit extension points you fill in. Writes <name>.cs and returns { created, path, className }. NOTE: the action/filterTag params are not currently applied at generation time — the zone always logs; implement teleport/damage/spawn in the generated methods (edit_script). Follow with trigger_hotload, then get_compile_errors",
      "params": [
        {
          "name": "name",
          "description": "Class name. Defaults to 'TriggerZone'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/ for the file",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "action",
          "description": "What happens on trigger (currently not applied by the handler — the generated zone always logs; implement the effect in OnPlayerEnter yourself)",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "log",
              "teleport",
              "damage",
              "spawn"
            ]
          }
        },
        {
          "name": "filterTag",
          "description": "Only trigger for objects with this tag (currently not applied at generation — the generated TriggerTag [Property] defaults to 'player'; change it per-instance with set_property)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "ui",
      "name": "add_screen_panel",
      "description": "Create a new GameObject with a ScreenPanel component for full-screen UI overlay (HUD, menus, etc.). Returns { created, gameObject:{ id, name, components, ... } }. NOTE: panelComponent is looked up in the TypeLibrary and SILENTLY skipped if the type isn't loaded (freshly generated panels need trigger_hotload first) — check gameObject.components to confirm it attached",
      "params": [
        {
          "name": "name",
          "description": "Name for the UI GameObject. Defaults to 'Screen Panel'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "zIndex",
          "description": "Z-index for layering multiple screen panels",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "panelComponent",
          "description": "Name of a Razor PanelComponent to add (e.g. 'GameHud')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "parent",
          "description": "GUID of parent GameObject",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "ui",
      "name": "add_world_panel",
      "description": "Create a new GameObject with a WorldPanel component for in-world 3D UI (health bars, signs, nameplates). Returns { created, gameObject:{ id, name, components, ... } }. As with add_screen_panel, panelComponent is SILENTLY skipped if the type isn't in the TypeLibrary (trigger_hotload first, then verify via gameObject.components). For CLICKABLE world UI the scene also needs a Sandbox.WorldInput (see create_worldpanel_ui)",
      "params": [
        {
          "name": "name",
          "description": "Name for the UI GameObject. Defaults to 'World Panel'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position for the panel — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "Rotation as euler angles",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "worldScale",
          "description": "Scale of the world panel. Smaller = smaller in world",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "lookAtCamera",
          "description": "Whether the panel always faces the camera (billboard)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "panelComponent",
          "description": "Name of a Razor PanelComponent to add (e.g. 'NpcNameplate')",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "parent",
          "description": "GUID of parent GameObject",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "ui",
      "name": "create_razor_ui",
      "description": "Create a Razor UI component file (.razor). Returns { created, path, componentName }; errors if the file already exists. NOTE: the current handler generates one fixed basic panel (a root div + label bound to a Title [Property]) from name+directory only — panelType/content/styles/includeStyles are not applied and no .scss is written; for custom markup or a PanelComponent you can host via add_screen_panel, write the files with write_file and check them with razor_lint. Follow with trigger_hotload, then get_compile_errors",
      "params": [
        {
          "name": "name",
          "description": "Component name (e.g. 'GameHud', 'MainMenu')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory under code/ for the file. Defaults to 'UI'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "panelType",
          "description": "Type of panel to generate: 'basic' (simple panel), 'hud' (health/score overlay), 'menu' (title + buttons). Defaults to 'basic'",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "basic",
              "hud",
              "menu"
            ]
          }
        },
        {
          "name": "description",
          "description": "Description of what this UI panel does",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "includeStyles",
          "description": "Generate a companion .razor.scss stylesheet. Defaults to true",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "content",
          "description": "Raw .razor content (skips boilerplate generation)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "styles",
          "description": "Raw .razor.scss content (only used with raw content mode)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "uifeedback",
      "name": "create_combo_meter",
      "description": "Generate a combo system: a sealed Component (the authoritative, headless state) PLUS a small Razor HUD (PanelComponent + scss). Three files: <Name>.cs + <Name>Hud.razor + <Name>Hud.razor.scss. The component exposes a static Bump() — call it on every hit and the Count rises; an idle window (ComboWindowSeconds, tracked with TimeSince) resets it; the Multiplier steps up through [Property] tier thresholds (Tier2Hits/Tier3Hits/Tier4Hits => 2x/3x/4x); and a static OnComboChanged(int count, float multiplier) event fires so any HUD/audio reacts without a reference (Bump() targets the active instance, so callers never need a handle — attach ONE to a persistent object). The HUD subscribes to OnComboChanged, shows \"<count> HITS x<mult>\", and pulses via a CSS animation on every change (razor_lint-safe: BuildHash folds count/mult/pulse, no switch-expressions / non-ASCII in @code, class-selector SCSS root; host it under a ScreenPanel via add_screen_panel). Pairs with create_health_system / create_floating_combat_text — call Bump() from the damage path and Spawn a popup that reflects the multiplier.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the combo component; the HUD is generated as <Name>Hud. Defaults to 'ComboMeter'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs + .razor + .razor.scss. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "comboWindowSeconds",
          "description": "Idle seconds before the combo resets back to zero (clamped to >= 0.25). Defaults to 3",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "uifeedback",
      "name": "create_proxy_nametag",
      "description": "Generate a sealed Component that floats the OWNER'S display name above a networked player. TextRenderer-based (not WorldPanel): a nametag is one short string with a distance fade, so a TextRenderer on a managed child object is far simpler than a WorldPanel + Razor + WorldInput stack — no UI assets, no panel host, per-frame alpha is a one-liner (mirrors create_floating_combat_text). Reads GameObject.Network.Owner.DisplayName (Owner and OwnerConnection are the same Connection on this SDK; falls back to the object name offline). Visibility is the INVERSE of the usual proxy guard: it renders only when GameObject.Network.IsProxy is true — i.e. on OTHER clients' copies of the player — so you never see a tag over your own head (offline / no networking => IsProxy false everywhere => no tags, expected). Spawns a CHILD GameObject for the text so billboarding never rotates the player model; cleaned up on disable. [Property] MaxDistance fades the tag out with distance, HeightOffset floats it above the head, FontSize sizes it. Attach to the ROOT of your networked player object. Pairs with create_networked_player (add it to the generated player prefab so every remote player is labeled). Returns { created, path, className, maxDistance, heightOffset, note, nextSteps }. Follow with trigger_hotload, then get_compile_errors, then attach via add_component_with_properties (component=className).",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated component. Defaults to 'ProxyNametag'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .cs file. Defaults to 'Code'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "maxDistance",
          "description": "Full alpha up close; fades to zero as the camera approaches this distance and is hidden past it, in world units. Defaults to 2000",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "heightOffset",
          "description": "Height above the object's origin to float the tag, in world units (~72 clears a Citizen's head). Defaults to 72",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "uifeedback",
      "name": "create_worldpanel_ui",
      "description": "Generate a diegetic, clickable world-space UI: a Razor PanelComponent (+ .razor.scss) meant to sit on a GameObject that ALSO carries a Sandbox.WorldPanel — the WorldPanel is the world-space render surface (PanelSize / RenderScale / InteractionRange), this component is the actual UI it renders (PanelComponent has no world-panel mode of its own). Ships two example buttons wired to @onclick that raise a static event OnButtonPressed(string id), so game code reacts WITHOUT editing the panel (subscribe: <Name>.OnButtonPressed += id => ...). razor_lint-safe by construction (BuildHash override, no switch-expressions / non-ASCII in @code, class-selector SCSS root). SCENE PREREQUISITE FOR CLICKS: a WorldPanel's buttons only fire when a Sandbox.WorldInput exists in the scene (typically on the camera or the player) with its LeftMouseAction set to your click input action (e.g. \"attack1\"); on this SDK WorldInput drives itself from the camera + that action — there is no manual ray to feed, and WorldInput.Hovered (read-only) reflects the panel under the cursor. Without a WorldInput present, @onclick never fires. Setup: add_world_panel to a GameObject, add this component to the same object, add a WorldInput to the scene.",
      "params": [
        {
          "name": "name",
          "description": "Class name for the generated panel. Defaults to 'WorldPanelUi'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "directory",
          "description": "Subdirectory for the generated .razor + .razor.scss. Defaults to 'Code/UI'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "title",
          "description": "Heading text baked into the panel (editable per-instance via the Title [Property]). Defaults to 'Interact'",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "add_beam",
      "description": "EXPERIMENTAL — create an energy/laser beam (BeamEffect) from a position to a target point (default: 128u straight up) — additive, tintable. Returns { created, gameObject } — gameObject.id is the beam object's GUID. Like the other runtime particle tools, rendering through the bridge is unverified; use spawn_vpcf when you need a guaranteed-visible effect.",
      "params": [
        {
          "name": "position",
          "description": "Beam start (world position of the beam object)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "target",
          "description": "Beam end point in world space (default: 128u up)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "width",
          "description": "Beam width/scale (default 4)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "color",
          "description": "Beam colour (default white)",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "name",
          "description": "GameObject name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "add_envmap_probe",
      "description": "Add an environment reflection/ambient probe (EnvmapProbe) at a position with a cubic influence volume — captures local reflections and indirect light for nearby surfaces. IMPORTANT: a placed probe captures NOTHING until baked — follow with bake_reflections. Returns { created, gameObject } — gameObject.id is the probe object's GUID.",
      "params": [
        {
          "name": "name",
          "description": "GameObject name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position (centre of the probe)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "size",
          "description": "Cubic influence size in units (default 1024)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "tint",
          "description": "Tint applied to the captured environment",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "feathering",
          "description": "Edge feathering 0-1 for blending between overlapping probes",
          "optional": true,
          "type": {
            "kind": "number"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "add_light",
      "description": "Add a light to the active scene. NOTE: s&box lights have no separate brightness field — intensity is the colour magnitude, so 'brightness' scales the colour (use >1 for bright/HDR). Types: directional = sun (aim it with rotation), point = omni-directional (range), spot = cone (range + coneInner/coneOuter degrees), ambient = global fill light.",
      "params": [
        {
          "name": "type",
          "description": "Light type",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "directional",
              "point",
              "spot",
              "ambient"
            ]
          }
        },
        {
          "name": "name",
          "description": "GameObject name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "color",
          "description": "Light colour (default white)",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "brightness",
          "description": "Intensity multiplier on the colour (default 1; try 2-10 for point/spot)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "range",
          "description": "point/spot only: falloff radius in units (maps to Radius)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "coneInner",
          "description": "spot only: inner cone angle in degrees",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "coneOuter",
          "description": "spot only: outer cone angle in degrees",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "shadows",
          "description": "Cast shadows (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "skyColor",
          "description": "directional only: ambient sky colour for the upper hemisphere",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "position",
          "description": "World position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "rotation",
          "description": "World rotation — sets the aim direction for directional/spot lights",
          "optional": true,
          "type": {
            "kind": "rotation"
          }
        },
        {
          "name": "parentId",
          "description": "GUID of a parent GameObject",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "add_post_process",
      "description": "Add (or update) a post-processing effect on the scene's main camera (auto-enables post-processing). Generic: pass the effect component name + any of its properties. Examples — Bloom {Strength, Threshold, Tint}, Tonemapping, ColorAdjustments {Saturation, Brightness, Contrast}, Vignette {Intensity, Color}, FilmGrain, DepthOfField, ChromaticAberration, MotionBlur, Sharpen, AmbientOcclusion. Call describe_type <Effect> to discover a given effect's properties.",
      "params": [
        {
          "name": "effect",
          "description": "Post-process component type name, e.g. 'Bloom', 'Vignette', 'ColorAdjustments'",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "properties",
          "description": "Property name -> value. Floats/ints/bools as numbers/bools, colours as {r,g,b,a}, enums as their string name.",
          "optional": true,
          "type": {
            "kind": "any"
          }
        },
        {
          "name": "cameraId",
          "description": "GUID of a specific camera GameObject (default: the scene's main camera)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "add_trail",
      "description": "Attach a motion trail (TrailRenderer) to an existing GameObject (via targetId) so it leaves a trail as it moves — or create a standalone trail object. Only visible while the object is moving.",
      "params": [
        {
          "name": "targetId",
          "description": "GUID of the GameObject to attach the trail to (else a new 'Trail' object is made)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position when creating a standalone trail",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "lifetime",
          "description": "How long (seconds) trail points persist",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "maxPoints",
          "description": "Max points in the trail",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "pointDistance",
          "description": "Min distance between trail points",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "name",
          "description": "GameObject name when creating a new one",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "apply_atmosphere",
      "description": "One-call scene mood: composes ambient + directional light, gradient fog, and a camera post-fx stack (tonemap + colour grade + vignette) tuned for the chosen mood. Idempotent — re-runs update the same 'Atmosphere *' objects. Returns { applied, mood, components, postFxCamera } — the post-fx stack is only applied when a camera exists (postFxCamera is null otherwise; add a camera and re-run). Screenshot to verify.",
      "params": [
        {
          "name": "mood",
          "description": "Atmosphere preset",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "horror-night",
              "foggy-dawn",
              "overcast",
              "warm-interior"
            ]
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "apply_post_fx_look",
      "description": "Apply just a camera post-processing look (no lights/fog): cinematic (tonemap + bloom + soft vignette), filmic-horror (desaturated, high-contrast, heavy vignette, film grain), or clean (tonemap only). Errors if the scene has no CameraComponent. Returns { applied, look, components, camera } listing the effect components added to the main camera — tune them individually afterwards with add_post_process.",
      "params": [
        {
          "name": "look",
          "description": "Post-fx look preset",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "cinematic",
              "filmic-horror",
              "clean"
            ]
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "bake_reflections",
      "description": "Bake all EnvmapProbe reflection probes in the scene (EnvmapProbe.BakeAll) so they actually capture their surroundings — placing a probe with add_envmap_probe does nothing visible until it's baked. This is a real editor compute step, not a component setter. Runs async — returns { baking, count, note, probes } immediately (each probe: id, name, mode, hasBaked), or { baked: false, count: 0 } when the scene has no probes; re-screenshot after a moment to see reflections appear.",
      "params": []
    },
    {
      "module": "visuals",
      "name": "create_particle_effect",
      "description": "Build a custom additive particle effect from raw params (ParticleEffect + cone emitter + sprite renderer). Use this when the spawn_particle presets aren't what you want. Texture-free (additive Texture.White glow).",
      "params": [
        {
          "name": "position",
          "description": "World position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "color",
          "description": "Particle tint (default white)",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "rate",
          "description": "Particles per second when looping (default 30)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "burst",
          "description": "Particle count for a one-shot burst when loop=false (default 30)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "loop",
          "description": "Continuous emission (default true) vs a single burst",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "lifetime",
          "description": "Particle lifetime in seconds (default 2)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "size",
          "description": "Particle size (default 4)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "speed",
          "description": "Emission speed along the cone (default 100)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "coneAngle",
          "description": "Cone half-angle in degrees; ~85 ≈ hemisphere (default 40)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "gravity",
          "description": "Downward force (default 0 = none)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "additive",
          "description": "Additive (glow) blending (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "maxParticles",
          "description": "Max live particles (default 500)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "name",
          "description": "GameObject name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "set_fog",
      "description": "Add or update fog in the active scene. Types: 'gradient' (distance haze — great for mood/horror), 'cubemap' (sky-tinted distance fog), 'volumetric' (a localized fog volume). Re-running with the same targetId updates it rather than duplicating; without targetId each call creates a new fog object. Returns { created, type, gameObject } — gameObject.id addresses the fog object for later set_property/delete_gameobject; screenshot to verify the look.",
      "params": [
        {
          "name": "type",
          "description": "Fog type (default gradient)",
          "optional": true,
          "type": {
            "kind": "enum",
            "values": [
              "gradient",
              "cubemap",
              "volumetric"
            ]
          }
        },
        {
          "name": "name",
          "description": "GameObject name when creating a new fog object",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "targetId",
          "description": "GUID of an existing GameObject to host the fog (else a new fog object is created)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "color",
          "description": "Fog colour (gradient/volumetric Color, cubemap Tint)",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "startDistance",
          "description": "gradient/cubemap: distance (units) where fog begins",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "endDistance",
          "description": "gradient/cubemap: distance (units) where fog reaches full density",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "height",
          "description": "gradient: world height the fog settles around",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "falloff",
          "description": "Distance/density falloff exponent (higher = sharper onset)",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "blur",
          "description": "cubemap: sky blur amount",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "heightStart",
          "description": "cubemap: world height where height-fog starts",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "heightWidth",
          "description": "cubemap: height-fog band width",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "heightExponent",
          "description": "cubemap: height-fog falloff exponent",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "strength",
          "description": "volumetric: fog density/strength",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "size",
          "description": "volumetric: bounds size (units) centred on the object — object {x,y,z} or comma string \"x,y,z\"",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "set_skybox",
      "description": "Set the scene's 2D skybox tint / indirect lighting (re-uses an existing SkyBox2D or creates one). Darken the tint for night/dusk. Optionally point it at a .vmat sky material (silently kept as-is if the material fails to load). Returns { created, gameObject } — gameObject.id is the sky object; screenshot to verify the change.",
      "params": [
        {
          "name": "tint",
          "description": "Sky tint colour",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "indirectLighting",
          "description": "Whether the sky contributes indirect/ambient light",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "material",
          "description": "Path to a .vmat sky material (optional)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "name",
          "description": "Name for the sky GameObject if one is created",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "spawn_particle",
      "description": "EXPERIMENTAL — build an additive runtime ParticleEffect (no texture asset needed): kind = fire, embers, sparks, magic, dust, blood, or snow ('smoke' returns an error). Returns { created, kind, gameObject } — the component graph is created, but this runtime particle path has NOT been verified to render through the bridge; for particles you can actually see, prefer spawn_vpcf.",
      "params": [
        {
          "name": "kind",
          "description": "Particle preset",
          "optional": false,
          "type": {
            "kind": "enum",
            "values": [
              "fire",
              "embers",
              "sparks",
              "magic",
              "dust",
              "blood",
              "snow"
            ]
          }
        },
        {
          "name": "position",
          "description": "World position",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "color",
          "description": "Override the particle tint",
          "optional": true,
          "type": {
            "kind": "color"
          }
        },
        {
          "name": "name",
          "description": "GameObject name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "visuals",
      "name": "spawn_vpcf",
      "description": "Spawn a REAL particle system by playing a compiled .vpcf asset through LegacyParticleSystem — the reliable path that actually renders, unlike spawn_particle/create_particle_effect (which build a runtime ParticleEffect graph that shows nothing). Defaults to 'particles/impact.generic.vpcf' (a sparks/impact burst — the only particle .vpcf reliably present; set looped + a warm tint for a fire-ish effect). Pass your own compiled .vpcf logical path if you have one. Screenshot-verifiable in edit mode.",
      "params": [
        {
          "name": "vpcf",
          "description": "Logical .vpcf path (default 'particles/impact.generic.vpcf'). NOT the .vpcf_c or .sbox/cloud cache path.",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "position",
          "description": "World position (default origin)",
          "optional": true,
          "type": {
            "kind": "vector3"
          }
        },
        {
          "name": "name",
          "description": "GameObject name",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "looped",
          "description": "Loop the effect (default true)",
          "optional": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "playbackSpeed",
          "description": "Playback speed multiplier",
          "optional": true,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "tint",
          "description": "Color tint (e.g. orange for fire); applied to the live SceneObject if it's ready this frame",
          "optional": true,
          "type": {
            "kind": "color"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "add_cave_waypoint",
      "description": "Append (or insert at `index`) a waypoint to CaveBuilder.Path. Z is depth (negative = underground). Returns `added`, `total` (waypoint count), and `rebuilt` — rebuilds the cave ('Build Cave') immediately unless rebuild=false.",
      "params": [
        {
          "name": "x",
          "description": "World X of the waypoint",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "y",
          "description": "World Y of the waypoint",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "z",
          "description": "Z depth — negative = underground",
          "optional": true,
          "default": 0,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "index",
          "description": "Optional insert position (default: append to end)",
          "optional": true,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "rebuild",
          "description": "Rebuild the cave after adding (default true; set false to batch)",
          "optional": true,
          "default": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the CaveBuilder; omit to auto-find the first CaveBuilder in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "add_forest_poi",
      "description": "Add a point of interest (clearing) to ForestGenerator.POIs. Returns `added`, `index` (the new POI's index — pass it to add_forest_trail as from_index/to_index), `total`, and `rebuilt`. Forest gen is slow (~1s), so rebuild defaults to false — batch your POIs/trails, then regenerate once (rebuild=true on the last call, or invoke_button 'Generate Forest').",
      "params": [
        {
          "name": "name",
          "description": "Display name for the POI. Default 'POI'.",
          "optional": true,
          "default": "POI",
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "x",
          "description": "World X of POI center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "y",
          "description": "World Y of POI center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "radius",
          "description": "POI radius in world units. Default 300.",
          "optional": true,
          "default": 300,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "density_multiplier",
          "description": "Multiplies forest density inside this POI's region",
          "optional": true,
          "default": 1,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "rebuild",
          "description": "Forest gen is slow (~1s); default false to batch",
          "optional": true,
          "default": false,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the ForestGenerator; omit to auto-find the first ForestGenerator in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "add_forest_trail",
      "description": "Add a trail gap between two POIs to ForestGenerator.Trails. Returns `added`, `total` (trail count), and `rebuilt`. rebuild defaults to false — nothing changes visually until you regenerate (rebuild=true, or invoke_button 'Generate Forest').",
      "params": [
        {
          "name": "from_index",
          "description": "Index of the start POI (the `index` returned by add_forest_poi)",
          "optional": false,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "to_index",
          "description": "Index of the end POI (the `index` returned by add_forest_poi)",
          "optional": false,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "rebuild",
          "description": "Regenerate the forest after adding (default false — batch, then rebuild once)",
          "optional": true,
          "default": false,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the ForestGenerator; omit to auto-find the first ForestGenerator in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "add_terrain_clearing",
      "description": "Add a flat clearing zone to MapBuilder's Clearings list (lerps height toward base inside radius). Returns `added`, `total` (clearing count), and `rebuilt`. Rebuilds terrain immediately by default; set rebuild=false to batch edits, then rebuild once (rebuild=true on the last call, or invoke_button 'Build Terrain').",
      "params": [
        {
          "name": "x",
          "description": "World X of clearing center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "y",
          "description": "World Y of clearing center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "radius",
          "description": "Clearing radius in world units. Default 300.",
          "optional": true,
          "default": 300,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "rebuild",
          "description": "Rebuild terrain after adding (default true; set false to batch)",
          "optional": true,
          "default": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the MapBuilder; omit to auto-find the first MapBuilder in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "add_terrain_hill",
      "description": "Add a hill (cosine-falloff bump) to MapBuilder's Hills list. Negative height creates a depression. Returns `added`, `total` (hills now in the list), and `rebuilt`; verify the surface with raycast_terrain at the hill center.",
      "params": [
        {
          "name": "x",
          "description": "World X of hill center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "y",
          "description": "World Y of hill center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "radius",
          "description": "Hill radius in world units",
          "optional": true,
          "default": 500,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "height",
          "description": "Peak height (negative for depression)",
          "optional": true,
          "default": 100,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "rebuild",
          "description": "Rebuild terrain after adding (set false to batch)",
          "optional": true,
          "default": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the MapBuilder; omit to auto-find the first MapBuilder in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "add_terrain_trail",
      "description": "Carve a trail depression between two points on MapBuilder (appends to its Trails list). Returns `added`, `total` (trail count), and `rebuilt`. Rebuilds terrain immediately by default; set rebuild=false to batch, then invoke_button 'Build Terrain' once.",
      "params": [
        {
          "name": "from",
          "description": "Trail start point (world x/y)",
          "optional": false,
          "type": {
            "kind": "object"
          }
        },
        {
          "name": "to",
          "description": "Trail end point (world x/y)",
          "optional": false,
          "type": {
            "kind": "object"
          }
        },
        {
          "name": "rebuild",
          "description": "Rebuild terrain after adding (default true; set false to batch)",
          "optional": true,
          "default": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the MapBuilder; omit to auto-find the first MapBuilder in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "build_terrain_mesh",
      "description": "Build a standalone heightmap terrain mesh (a MeshComponent) from a hills/clearings JSON spec — independent of MapBuilder. Use when you don't have a MapBuilder component in the scene and want one-shot terrain. Returns `built`, `id` (the new GameObject's GUID), `name`, `vertices`, and `faces`; pass `id` to assign_material, set_transform, or delete_gameobject. Note: raycast_terrain cannot sample this mesh (it requires MapBuilder).",
      "params": [
        {
          "name": "size",
          "description": "Total terrain size (world units, square)",
          "optional": true,
          "default": 9600,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "resolution",
          "description": "Grid resolution per side",
          "optional": true,
          "default": 64,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "name",
          "description": "Name for the created terrain GameObject. Default 'Generated Terrain'.",
          "optional": true,
          "default": "Generated Terrain",
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "hills",
          "description": "Hill bumps: array of {x, y, radius (default 500), height (default 100; negative = depression)}. Default: none.",
          "optional": true,
          "default": [],
          "type": {
            "kind": "array",
            "items": {
              "kind": "object"
            }
          }
        },
        {
          "name": "clearings",
          "description": "Zones flattened back toward height 0: array of {x, y, radius (default 300)}. Default: none.",
          "optional": true,
          "default": [],
          "type": {
            "kind": "array",
            "items": {
              "kind": "object"
            }
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "clear_cave_path",
      "description": "Clear all waypoints in CaveBuilder.Path and remove the built cave from the scene (invokes 'Clear Cave'). Destructive. Returns `cleared` — the number of waypoints removed; start a new path with add_cave_waypoint.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject holding the CaveBuilder; omit to auto-find the first CaveBuilder in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "clear_forest_pois",
      "description": "Wipe all POIs and trails in ForestGenerator and clear placed forest objects from the scene (invokes 'Clear Forest'). Destructive. Returns `cleared` — the number of POIs removed; rebuild a layout with add_forest_poi + add_forest_trail, then invoke_button 'Generate Forest'.",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject holding the ForestGenerator; omit to auto-find the first ForestGenerator in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "clear_terrain_features",
      "description": "Wipe MapBuilder feature lists — Hills, Clearings, Trails, CavePath, or all of them (default). Destructive: the feature definitions are removed. Returns `cleared` (map of list name → entries removed) and `rebuilt`; re-add features with add_terrain_hill / add_terrain_clearing / add_terrain_trail.",
      "params": [
        {
          "name": "what",
          "description": "Which feature list to clear. Default 'all' (clears all four).",
          "optional": true,
          "default": "all",
          "type": {
            "kind": "enum",
            "values": [
              "Hills",
              "Clearings",
              "Trails",
              "CavePath",
              "all"
            ]
          }
        },
        {
          "name": "rebuild",
          "description": "Rebuild terrain after clearing (default true)",
          "optional": true,
          "default": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the MapBuilder; omit to auto-find the first MapBuilder in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "invoke_button",
      "description": "Call a public method on a component. Matching is tried in order: (1) a [Button] attribute label, (2) the exact method NAME, (3) case-insensitive name with spaces stripped. Calls ANY public method, not only [Button]-attributed ones (e.g. 'StartGame'). Pass `args` to call methods that take parameters — the arg count must match and each value is coerced to the parameter type (primitives: string/number/bool work; complex types like Vector3 may not coerce). Omit args (or []) for parameterless methods. (list_component_buttons only lists [Button] methods, so a plain method may be invokable yet not appear there.)",
      "params": [
        {
          "name": "component",
          "description": "Component type name (e.g. 'MapBuilder', 'SasquatchedGame')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "button",
          "description": "A [Button] label OR a public method name (e.g. 'Build Terrain', 'StartGame'); case- and space-insensitive",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "id",
          "description": "Optional GameObject GUID — if omitted, finds first matching component in scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "args",
          "description": "Arguments to pass (must match the method's parameter count); coerced to each parameter type",
          "optional": true,
          "type": {
            "kind": "array",
            "items": {
              "kind": "any"
            }
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "list_component_buttons",
      "description": "List the [Button]-attributed methods on a component. NOTE: this only finds methods decorated with [Button]; invoke_button can ALSO call any plain public no-arg method by name, so a method missing here may still be invokable. Use describe_type / get_method_signature to find non-button methods.",
      "params": [
        {
          "name": "component",
          "description": "Component type name",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "id",
          "description": "Optional GameObject GUID",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "paint_forest_density",
      "description": "Add a circular biome region with overridden forest density to ForestGenerator.DensityRegions. Multiple regions stack via cosine falloff. density: 0=no trees, 1=normal, 2=double. Returns `painted`, `total` (region count), and `rebuilt` — rebuild defaults to false, so regenerate (rebuild=true, or invoke_button 'Generate Forest') to see the change.",
      "params": [
        {
          "name": "x",
          "description": "World X of region center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "y",
          "description": "World Y of region center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "radius",
          "description": "Region radius in world units. Default 800.",
          "optional": true,
          "default": 800,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "density",
          "description": "Density multiplier (0=clear, 1=normal, 2=dense)",
          "optional": true,
          "default": 1,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "rebuild",
          "description": "Regenerate the forest after adding (default false; batch, then rebuild once)",
          "optional": true,
          "default": false,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the ForestGenerator; omit to auto-find the first ForestGenerator in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "place_along_path",
      "description": "Drop instances of a model along a path (list of points). Useful for fences, lampposts, road markers, lined-up rocks. Returns `placed` (instance count) and `folder` — the GUID of the new parent GameObject grouping the instances; pass `folder` to delete_gameobject to remove the whole run, or to set_parent/set_transform to move it.",
      "params": [
        {
          "name": "model",
          "description": "Model path (e.g. 'models/dev/box.vmdl' or installed-asset path)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "points",
          "description": "Path waypoints (at least 2)",
          "optional": false,
          "type": {
            "kind": "array",
            "items": {
              "kind": "vector3"
            }
          }
        },
        {
          "name": "spacing",
          "description": "Distance between placements (world units)",
          "optional": true,
          "default": 200,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "jitter",
          "description": "Max random offset perpendicular to path",
          "optional": true,
          "default": 0,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "min_scale",
          "description": "Minimum uniform scale per instance (random between min and max). Default 1.",
          "optional": true,
          "default": 1,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "max_scale",
          "description": "Maximum uniform scale per instance. Default 1.",
          "optional": true,
          "default": 1,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "seed",
          "description": "Random seed for jitter and scale — same seed reproduces the same placement. Default 42.",
          "optional": true,
          "default": 42,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "name",
          "description": "Base name for placed objects",
          "optional": true,
          "default": "PathItem",
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "raycast_terrain",
      "description": "Sample MapBuilder terrain height at world (x, y). Returns z (the surface height). Use to place props on the terrain surface.",
      "params": [
        {
          "name": "x",
          "description": "World X coordinate",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "y",
          "description": "World Y coordinate",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "id",
          "description": "Optional GameObject GUID for MapBuilder",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the terrain component type name (default MapBuilder)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "sculpt_terrain",
      "description": "Apply a heightmap brush at (x, y) to MapBuilder. Modes: raise, lower, flatten, smooth. Modifies the current heightmap directly and rebuilds the mesh; edits survive between calls but are lost when 'Build Terrain' regenerates from the feature lists. Returns `sculpted`, `mode`, and `affected_vertices`; verify with raycast_terrain at the brush center.",
      "params": [
        {
          "name": "x",
          "description": "World X of brush center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "y",
          "description": "World Y of brush center",
          "optional": false,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "radius",
          "description": "Brush radius in world units",
          "optional": true,
          "default": 400,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "strength",
          "description": "Height delta (units) for raise/lower; ignored for flatten/smooth",
          "optional": true,
          "default": 50,
          "type": {
            "kind": "number"
          }
        },
        {
          "name": "mode",
          "description": "Brush mode. Default 'raise'. `strength` applies to raise/lower only.",
          "optional": true,
          "default": "raise",
          "type": {
            "kind": "enum",
            "values": [
              "raise",
              "lower",
              "flatten",
              "smooth"
            ]
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the MapBuilder; omit to auto-find the first MapBuilder in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "set_forest_seed",
      "description": "Set ForestGenerator.Seed and (by default) regenerate — re-rolls the forest layout while keeping POIs, trails, and density regions. Returns `set`, `seed`, and `rebuilt`; take a screenshot afterwards (screenshot_from) to judge the new layout.",
      "params": [
        {
          "name": "seed",
          "description": "New random seed (integer). Default 77.",
          "optional": true,
          "default": 77,
          "type": {
            "kind": "integer"
          }
        },
        {
          "name": "rebuild",
          "description": "Regenerate the forest after setting (default true)",
          "optional": true,
          "default": true,
          "type": {
            "kind": "boolean"
          }
        },
        {
          "name": "id",
          "description": "GUID of the GameObject holding the ForestGenerator; omit to auto-find the first ForestGenerator in the scene",
          "optional": true,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Override the builder component type name — set this if your project's terrain/cave/forest component is named differently than the default (MapBuilder/CaveBuilder/ForestGenerator)",
          "optional": true,
          "type": {
            "kind": "string"
          }
        }
      ]
    },
    {
      "module": "world",
      "name": "set_prefab_ref",
      "description": "Set a GameObject-typed property on a component to a loaded prefab. Use this when set_property can't handle prefab references (which it can't, because prefabs are GameObjects not primitives).",
      "params": [
        {
          "name": "id",
          "description": "GUID of the GameObject holding the component",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "component",
          "description": "Component type name",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "property",
          "description": "Property name to set (must be GameObject-typed)",
          "optional": false,
          "type": {
            "kind": "string"
          }
        },
        {
          "name": "prefabPath",
          "description": "Prefab asset path (e.g. 'prefabs/player.prefab')",
          "optional": false,
          "type": {
            "kind": "string"
          }
        }
      ]
    }
  ]
}