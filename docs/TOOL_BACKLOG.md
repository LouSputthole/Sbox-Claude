# Bridge Tool Backlog — mined from 51 public-source s&box games (2026-06-09)

The 51-game public-source research pass surfaced **352 candidate bridge tools**, **188 flagged "ship-worthy"** by the per-game review. This is the ranked, de-duplicated roadmap. The single biggest signal: the same **scaffold-a-system** tools were independently requested by many games ? those are the highest-confidence additions.

**Folded into v1.10.0:** `create_economy_wallet` (the most-requested gap with no existing scaffold; see below).
**Built in v1.11.0:** `create_round_phase_machine`, `create_day_night_clock` (with v1.10.0's `create_economy_wallet` these form the "game director" trio). Handler count: 173.

**Built in v1.12.0 (Wave 1 + Wave 2, all verify-gated live -- handler count now 179):**
- `create_interactable` (`Component.IPressable` surface confirmed via describe_type; generated component compile-verified)
- `create_weighted_loot_table` (cumulative-weight pick + optional pity; compile-verified)
- `sandbox_lint` (whitelist pre-compile scan; tuned against live deliberate-failure -- Math/MathF NOW whitelisted, Array.Clone() still blocked)
- `create_save_system` (`FileSystem.Data.ReadJsonOrDefault/WriteJson` confirmed + compile-verified; the #1 corpus demand, 7x)
- `razor_lint` (static Razor/SCSS transpiler footgun scan; pure MCP-server-side text scan)
- `copy_asset_with_dependencies` (`Editor.Asset.GetReferences(deep:true)` + shadow-guard against core trees; kills gotchas #4 and #5)

**Built in v1.13.0 (Wave 3), all verify-gated live -- handler count now 183:**
- `create_leaderboard_panel` (Razor PanelComponent + Sandbox.Services.Leaderboards; first scaffold generating .razor + .razor.scss; compile-verified)
- `create_inventory` (slot-based parallel-list inventory, stack-first TryAdd with rollback; compile-verified)
- `create_stat_modifier_system` (Set->Add->Mult engine + per-source removal + OnStatChanged; compile-verified)
- `create_placement_mode` (ghost->commit builder, ScreenPixelToRay mouse ray, grid snap, NetworkSpawn commit; compile-verified)

**10-tool plan COMPLETE -- 10/10 shipped (v1.12.0: 6, v1.13.0: 4).** See `docs/plans/2026-06-09-next-10-tools.md`.

**Built in v1.18.0 (2026-07-02), all verify-gated live -- handler count now 197. TIER 1 IS COMPLETE:**
- `create_round_state_machine` (5x -- the top ask; manager + abstract RoundState lifecycle base + auto-attaching state stubs, CanEnter() skip, static-event + [Rpc.Broadcast] announce w/ late-joiner reconcile; compile-verified)
- `add_interaction_station` ([Sync(FromHost)] Guid occupancy + [Rpc.Host] claim routing + grace window + ResolveUserLevel gate; compile-verified)
- `create_event_director` (parallel-list EventPrefabs+Weights -- ISceneMetadata does NOT exist on this SDK; weighted roll + dedupe + MaxActive + *TimedEvent self-destruct companion; compile-verified)
- `create_save_slots` (saveslots.json manifest + per-slot payloads -- no directory-listing API needed; versioned, optional GUID scene reconciliation; compile-verified)
- Plus `add_lipsync` (not from this corpus -- same-week support for the engine's new `Sandbox.LipSync` component, shipped upstream 2026-07-01).

**Built in v1.19.0 (2026-07-07) -- the Game Feel pack (Tier-2 begins), all verify-gated live -- handler count now 200:**
- `create_camera_shake` (trauma model: Trauma^2 magnitude, Perlin offsets, OnPreRender un-apply guard so it neither fights a controller nor accumulates on a static camera; static Shake() API)
- `add_flicker_light` (Candle/Fluorescent/Faulty/Pulse/Lightning presets; modulates LightColor around the captured base, restores on disable)
- `create_floating_combat_text` (TextRenderer popups -- rise/fade/billboard/self-destruct via a static Spawn() factory; covers add_value_floater + add_damage_popups too)

**Built in v1.20.0 (2026-07-08) -- "Director's Cut", the biggest wave since v1.4.0 (+19 tools, six new handler families). Handler count now 219. Additive; verify-gated live on Gravehold -- the gate CAUGHT A REAL SDK BUG: `Sandbox.Connection` has no `.IsValid` member on this SDK (3 sites null-checked -- host-rpc-action x2, targeted-rpc, gacha), all 15 codegen scaffolds then compiled clean + TypeLibrary-load-confirmed; the 4 MovieMaker tools live-verified end to end:**
- **MovieMaker family** (`Sandbox.MovieMaker` reached the SHIPPING build -- see engine-watch): `list_movies`, `add_movie_player`, `play_movie`, `stop_movie`. Bridge wires + plays `.movie` clips; movies are AUTHORED in the editor's Movie Maker dock (the bridge does not author keyframes).
- **Cinematics & dialogue** (hand-authored, zero-asset): `create_cutscene_director` (inspector camera-shot list + smoothstep blends + `Scene.Camera` exact-restore + skip/input-lock + optional letterbox), `create_dialogue_system` (typewriter Razor HUD + `OnLineShown`/`OnDialogueFinished`).
- **Networking primitives** (Track B): `create_host_rpc_action` (caller-resolve + per-SteamId cooldown; folds `add_rate_limited_rpc`), `add_targeted_rpc` (`Rpc.FilterInclude` unicast), `create_local_player_resolver` (proxy-safe Local, online+offline), `add_host_migration_recovery` (proxy->authority detector + `OnBecameHost`).
- **Interaction + carry** (Tracks E/F): `add_interaction_prompt` (eye-trace "Press E" HUD for `IPressable`), `create_hold_to_confirm` (hold-to-fill + `OnConfirmed`), `create_carry_system` (pickup/drop/throw + host-routed ownership + `MotionEnabled=false` while held).
- **Loot / economy** (Track D): `create_gacha_drop_table` (rarity weights + pity + dup detection; folds `create_pity_loot_roll`), `create_currency_pickup` (networked coin + optional magnet + one-line `Grant` seam into `EconomyWallet`), `create_offline_progress` (`LastSeenUtc` delta + clamp + deterministic `SimulateOffline` replay).
- **UI / feedback** (Track C): `create_worldpanel_ui` (diegetic clickable WorldPanel + documented `WorldInput` prerequisite), `create_proxy_nametag` (`TextRenderer` owner name, proxy-only, distance fade), `create_combo_meter` (static `Bump()` + decay + multiplier tiers + pulsing HUD).

**Built in v2.0.0 (2026-07-09) — alongside the native-MCP migration (waves 1-3, all live gate-verified; handler count 233 as of wave 3 — final v2.0.0 total 237):**
- Platform tools (not from this corpus): `find_broken_references`, `batch_set_property`/`batch_delete`/`batch_add_component`/`batch_reparent` (dryRun convention), `describe_project`, `describe_scene`, `checkpoint_scene`/`restore_checkpoint`/`list_checkpoints` (the agent-side undo), `playtest_abort`; prefab round-trip made REAL (full serialization + true instantiation).
- Corpus Tier-2: `create_team_assigner` ✅ (smallest-bucket draft), `create_idle_income` ✅ (auto-wires wallet siblings via TypeLibrary).

**Built in [2.1.0] (2026-07-12) — Tier-2 completion + gameplay recording (+25 tools, six families), all live-verified on Gravehold: every handler executed for real, all generated code hotload-compiled clean + TypeLibrary-load-confirmed, scene + project left pristine. Handler count now 262 (was 237):**
- **Economy & Save (Batch 55, bridge_scaffold_gameplay):** `create_currency_account` ✅ (audited ledger: [Sync(FromHost)] balance, host-guarded Deposit/Withdraw/TryTransfer, transaction ring buffer), `create_idle_economy` ✅ (geometric bulk-buy: closed-form CostOf/MaxAffordable/BuyMax, auto-wires sibling wallet via TypeLibrary), `create_signed_save` ✅ (FNV-1a tamper-EVIDENT envelope, clamp-on-load Sanitize, forced-reset + OnTampered), `create_meta_progression` ✅ (roguelite meta-currency + unlock flags, BankRun seam), `add_steam_stat_currency` ✅ (currency over Sandbox.Services.Stats; honest cloud semantics), `create_loot_table_resource` ✅ (data-asset loot tables: [AssetType] GameResource `.loot` + resolver component, nested tables w/ depth cap).
- **Stats & Achievements (Batch 56, bridge_scaffold_gameplay):** `add_leaderboard_stat` ✅ (batched 12s flush, baseline-delta idempotency, chunking — the write-side partner to create_leaderboard_panel), `create_achievement_set` ✅ (defs + persisted progress + OnAchievementUnlocked + Razor toast HUD), `add_achievement_trigger` ✅ (trigger-zone GameObject firing Progress/Unlock, once-only latch), `create_speedrun_leaderboard` ✅ (timer + submit-only-on-improvement via Stats.SetValue + Board2 min-aggregation panel w/ friends filter + local-best row), `create_elo_rating_system` ✅ ([Sync(FromHost)] NetDictionary ratings, ReportMatch/ReportTeamMatch, Math.Pow expected score, host-side persistence).
- **Round-flow & UI (Batch 57):** `create_round_timer_hud` ✅ (bridge_scaffold_polish; Razor HUD binding by reflection to either shipped round machine, 1 Hz adaptive BuildHash), `scaffold_map_vote_flow` ✅ (bridge_scaffold_gameplay; [Rpc.Host] votes w/ Rpc.Caller re-validation, [Sync(FromHost)] NetList tallies, LCG tie-break, winner → Scene.LoadFromFile + Razor vote panel), `add_panel_buildhash` ✅ (bridge_ui; FILE-EDIT tool patching an existing .razor to add a BuildHash override — razor_lint's companion fixer; alreadyPresent path verified).
- **World & Render (Batch 58):** `add_water_body` ✅ (bridge_world; WaterVolume physics volume + trigger BoxCollider footprint + optional tinted surface child — honest: not a water shader), `add_render_target_camera` ✅ (bridge_visuals; secondary camera + RT texture via TextureBuilder + generated display component wiring camera.RenderTarget onto a Material — CCTV/mirror path), `add_daynight_sun` ✅ (bridge_visuals; DirectionalLight arc + 5-key color gradient + optional SkyBox2D tint driven by create_day_night_clock's TimeOfDay; refuses generation if clockClass unknown).
- **AI & Systems (Batch 59):** `create_needs_system` ✅ (bridge_scaffold_gameplay; decaying needs → weighted-mean [Sync(FromHost)] Happiness, edge-triggered OnNeedCritical), `create_utility_ai` ✅ (bridge_npc; abstract self-scoring Action base + brain w/ hysteresis — emergent vs FSM, pairs with create_npc_brain), `create_npc_schedule_brain` ✅ (bridge_npc; daily schedule w/ midnight wrap, binds by capability to any float-TimeOfDay clock, honest internal fallback clock, optional NavMeshAgent), `create_event_bus` ✅ (bridge_scaffold_gameplay; typed local pub/sub static class, must-Unsubscribe-in-OnDestroy documented), `add_tts_voice` ✅ (bridge_audio; Sandbox.Speech.Synthesizer fluent API — Say() from game code; audio-only by design: LipSync consumes BaseSoundComponent not SoundHandle; enableVisemeData exposes Handle.LipSync.Visemes).
- **Gameplay Recording (Batch 60, bridge_moviemaker) — closes engine-watch #2** (`Sandbox.MovieMaker.MovieRecorder` reached the shipping build — verified 2026-07-12; absent 2026-07-08): `record_gameplay_clip` ✅ (play-mode recording via monitor-only frame job — Start() AUTO-advances with game time; ids → WithCaptureGameObject or whole-scene default), `stop_gameplay_recording` ✅ (ToClip → MovieResource JSON → Assets/<folder>/<name>.movie → AssetSystem.RegisterFile + Compile → loadable by list_movies/play_movie; optional MoviePlayer wiring), `gameplay_recording_status` ✅ (poll). E2E-proven: 6.54s of live gameplay recorded and replayed through play_movie.
- Also fixed: a latent quote-escaping bug in the shipped `create_npc_brain` codegen (the TargetTag literal is now verbatim-escaped).

**Built in [2.1.0] (2026-07-13) — the cinematic wave (Batches 61–63, +5 tools), all e2e-verified live on Gravehold. Handler count now 267 (was 262):**
- **Batch 61 (bridge_scaffold_polish):** `generate_lipsync_dialogue` ✅ (dialogue lines + positional TTS + DATA-DRIVEN mouths: the live `SoundHandle.LipSync.Visemes` stream — the engine's 15-viseme Oculus set — multiplied through the model's own baked viseme→morph matrix via `Model.GetVisemeMorph` (Citizen ships the data), smoothed; loose TypeLibrary bind to a create_dialogue_system HUD; OnLineStarted/OnDialogueFinished/Skip; the runtime viseme stream honestly marked as needing one human playtest), `create_camera_effects` ✅ (CameraFx statics — Shake/ShakeAt/Punch/PunchAngles/Tilt + HitPunch/ExplosionShake/LandingTilt presets — over the SDK's NEW built-in CameraComponent.AddShake/AddPunch/AddTilt, whitelist-callable from game code, probe-verified; ShakeAt = BaseEffect.Epicenter+Radius distance falloff; composes with create_camera_shake — one-shot built-ins vs continuous trauma).
- **Batch 62 (bridge_moviemaker):** `author_movie_clip` ✅ (data-driven cutscene authoring, NO play mode, NO dock: manual Advance/Capture bake — the doc-official in-editor idiom (movie-maker/recording-api) — of a temp/borrowed camera through a shot list w/ hold/blend + smoothstep/linear + FOV via explicit WithCaptureComponent, live-verified FOV decoded exactly; a 3-shot clip baked in 6 ms, saved dock-loadable, replayed to full duration via play_movie; temp camera destroyed / borrowed camera restored byte-exact; lookAt GUIDs resolve to static bake-time positions — follow-cams are record_gameplay_clip's job).
- **Batch 63 (bridge_moviemaker):** `record_playtest` ✅ (one call = scripted playtest + gameplay recording of the SAME run — pure composition of the shipped playtest + recorder stacks; returns the verdict AND the saved clip — live: a 3-step PASS + a 1.36s clip; poll chain playtest_status → gameplay_recording_status; NOT scene-mutating, the play-mode precedent), `create_killcam` ✅ (a REAL MovieMaker killcam scaffold — sandboxed game code CAN construct/drive MovieRecorder+MoviePlayer, probe-verified, no fallback needed; BufferDuration = TRUE rolling buffer, 8.72s recorded → exactly a 3.00s clip re-based to 0; StartWatching/TriggerReplay/OnReplayFinished; chase-cam takeover w/ byte-exact restore; ReplayTimeScale; live-target fight documented — disable the controller before replay, dead-target is the working path).

**Deliberately skipped (reasons verified live 2026-07-12):**
- `add_network_visible_cull` — `INetworkVisible` is absent from this SDK.
- `create_minigame_mode` — no game-side Gamemode type to subclass.
- `create_daynight_cycle` — duplicate: the clock shipped (v1.11.0 `create_day_night_clock`); the visual half is `add_daynight_sun` ✅.
- `create_grass_streamer` — GPU-instancing risk.
- `create_genetics_system` — niche.
- `create_primitive_builder` — MeshComponent procedural materials are broken (SetFaceMaterial + tint don't apply on PolygonMesh).
- Folded by shipped tools: the 6 remaining save variants (`create_save_service`, `add_json_savegame`, `create_save_dto`, `create_binary_save`, `add_local_player_profile`, `add_saveable_field`), `create_economy_balance`, `create_stat_tracker` / `wire_services_stats`, the interaction trio (`create_interaction_router` / `create_interaction_interface` / `create_proximity_modal`), and `create_loot_table_system`.

**Next up (v2.1.0):** what little remains of Tier-2 by theme below, and the ENGINE-WATCH items:
- **MovieMaker / cutscene tool family** -- ✅ **SHIPPED in v1.20.0.** `Sandbox.MovieMaker` reached the shipping build (`search_types` 2026-07-08; it was absent 2026-07-02), so the bridge covers `list_movies` / `add_movie_player` / `play_movie` / `stop_movie` (the MovieMaker family). `MovieRecorder` **record-gameplay-to-clip** -- ✅ **SHIPPED + covered in [2.1.0] (2026-07-12)**: `MovieRecorder` reached the shipping build (verified 2026-07-12; it was absent 2026-07-08) and the bridge covers it end to end (`record_gameplay_clip` / `stop_gameplay_recording` / `gameplay_recording_status`, E2E-proven above).
- **Multiplayer test harness -- likely SHIPPED. PROMOTED TO THE TOP NEXT-WAVE CANDIDATE** (docs-audit finding 2026-07-13): the official `networking/testing-multiplayer.md` now documents **"Join via new instance"** (the header network icon) plus the **`connect local` / `reconnect`** console commands -- the engine's local multi-instance test path appears to have reached the shipping build. Our earlier "still absent" verdicts (re-verified 2026-07-12: `search_types "loopback"` returned 0, same 2026-07-08 / 2026-07-02) keyed off a TYPE search, which console-command + editor-UI surface never shows up in. **LIVE-VERIFIED 2026-07-13 — IT SHIPPED.** Bridge-driven end-to-end on Gravehold: editor play session + `Networking.CreateLobby()` (parameterless static; `Sandbox.Network.LobbyConfig` for the configured overload — NOT `Sandbox.LobbyConfig`) → `sbox.exe -joinlocal` (flag string confirmed in Sandbox.Engine; `Sandbox.Application.IsJoinLocal` is the readable static; `Editor.EditorPreferences.NewInstanceCommandLineArgs` carries extra args) boots a REAL second game client (~80 s) that auto-joins → host-side `Connection.All` went 1→2 (guest connection, `host=false`). Local transport is `Sandbox.Network.TcpChannel` (expect benign SendThread traces when a client is killed). Play-session gotcha: play mode does NOT host by default — `Networking.IsActive` is false until game code (or the harness) calls CreateLobby. Now build the harness tools: `start_multiplayer_test` (host lobby + spawn N `-joinlocal` clients + track PIDs), `multiplayer_test_status` (connection roster host-side), `stop_multiplayer_test` (kill clients, disconnect), then compose with `playtest`/`drive_player` + `inspect_networked_object` for sync assertions.
- **Offline lipsync generation** -- LIVE PATH ADDRESSED in [2.1.0] (Batch 61): `generate_lipsync_dialogue` speaks lines AND drives mouths from the live viseme stream through each model's baked `Model.GetVisemeMorph` matrix (with `add_tts_voice`'s `enableVisemeData` as the raw-data seam). A bake-visemes-to-ASSET path still needs a public API (only `Editor.VisemeEditor.Visemes` visible so far); if one surfaces, dialogue-ready audio assets could be produced offline.

**Docs-audit adds (2026-07-13)** -- small items surfaced by the official-docs cross-check (sbox.game/dev/doc), each a candidate tool, description patch, or gotcha:
- `GameObject.PlaySound` / `StopAllSounds` -- documented convenience surface with no bridge coverage; fold into bridge_audio guidance or a small tool.
- Surface `MoviePlayer.Binder` / `CreateTargets` in `add_movie_player`'s description -- clips whose capture targets are gone (e.g. `author_movie_clip`'s temp camera) need target recreation on playback.
- Hotloading rules as a gotcha candidate: `[SkipHotload]`, and field-DEFAULT changes need a restart (hotload doesn't re-run initializers).
- `Input.EscapePressed` -- documented input surface the bridge/skills never mention.
- `ITriggerListener` has more overloads than the pair the cookbook shows.
- `FileSystem.OrganizationData` -- documented cross-game (per-org) persistent storage, absent from our save-system guidance.
- Sandboxed `Http` restrictions -- domains only (no raw IPs); localhost only on ports 80/443/8080/8443. sandbox_lint / docs candidate.
- `EnvShake` / `ShakePhysics` / controller `Rumble` / the `ICameraModifier` extension point -- the rest of the documented camera-effects surface `create_camera_effects` doesn't cover.
- DTO `outputSchema` pass for hot read tools -- the docs recommend structured returns; folds into the existing typed-DTO roadmap item.

**Queued:** everything else here, grouped by theme. The raw research notes are intentionally kept outside the distributable repository.

Legend: **(Nx)** = independently proposed by N games · `easy`/`medium` = miner-estimated build risk.

---

## Tier 1 — multi-game, high-confidence scaffolds (do these first)

| Tool | Games | Risk | What it scaffolds |
|---|---|---|---|
| `create_save_system` | **7x** | medium | Versioned `PersistenceManager` singleton + `ISaveDataProperty<T>` + JSON autosave + delete-on-version-mismatch. The #1 ask — every persistent game needs it. |
| `create_round_state_machine` | **5x** | medium | `RoundManager` singleton + abstract `RoundState` (Begin/Tick/Finish/OnTimeUp, `[Sync] TimeUntil`) + named state stubs, index-wrap, `CanEnter()` skip, host-event-plus-mirror-RPC. |
| `create_economy_wallet` ✅ v1.10.0 | 2x | easy | Server-authoritative clamped-int `Money` + `Add/Take/Set` spend-gate + per-SteamId JSON save + balance-changed event + HUD label. |
| `create_weighted_loot_table` | 2x | easy | Cumulative-weight picker + optional two-tier category roll from a `[Property] Dictionary<string,int>` or GameResource. |
| `create_interactable` / `create_interactor_base` | 2x | easy | `Component, IPressable` stub (Look/Hover/Blur/Press) + `InputTip.Push` prompt + `IsProxy` guard. |
| `create_leaderboard_panel` | 2x | easy | Razor leaderboard bound to a Steam stat key + `CenterOnMe` row + cached async avatar fetch. |
| `add_interaction_station` | 2x | easy | `IPressable` prop with `[Sync]` occupancy + reservation grace window + unlock-level gate, opening an overlay. |
| `create_stat_modifier_system` | 2x | medium | `PlayerStat` enum + `ModifierType{Set,Add,Mult}` engine with priority/sum/product resolution + per-source removal. |
| `create_event_director` | 2x | medium | Prefab-discovery (`ISceneMetadata`) + interval roll + weighted pick + active-set dedupe + timed self-destruct. The generalized AI-director. |
| `create_save_slots` | 2x | medium | Multi-slot Storage save manager + optional GUID scene-object reconciliation (destroy-missing / rehydrate-survivors). |

## Tier 2 — by theme (single-game but clearly reusable)

**Economy / currency (≈12 variants):** `create_currency`, `create_economy_currency`, `create_economy_ledger`, `create_currency_account` ✅ [2.1.0] (host-guarded deposit/withdraw + transaction ring buffer), `create_currency_pickup` ✅ v1.20.0 (networked coin: host-validated grant into `EconomyWallet` + optional magnet + replicated despawn), `create_idle_income` ✅ v2.0.0 (1s passive accumulator), `create_idle_economy` ✅ [2.1.0] (geometric bulk-buy + offline reconciliation), `create_economy_balance` (static const formula class — FOLDED by shipped tools, [2.1.0] triage), `add_steam_stat_currency` ✅ [2.1.0] (currency over `Services.Stats` Sum/LastValue).

**Save / persistence (≈12 variants) — THEME COMPLETE [2.1.0]:** `create_signed_save` ✅ [2.1.0] (FNV-1a tamper-evident envelope + clamp-on-load + forced-reset + OnTampered), `create_meta_progression` ✅ [2.1.0] (meta-currency + unlock flags, BankRun seam), `create_offline_progress` ✅ v1.20.0 (DateTime delta + clock-rollback guard + clamp + deterministic `SimulateOffline` replay); the remaining six variants — `create_save_service`, `add_json_savegame` (autosave + inspector buttons), `create_save_dto` (flat DTO round-trip), `create_binary_save` (magic+version+typed), `add_local_player_profile`, `add_saveable_field` (`[Save]` onto a property) — FOLDED by the shipped save family ([2.1.0] triage).

**Leaderboards / stats / achievements (≈14 variants):** `create_leaderboard_service`, `add_leaderboard_stat` ✅ [2.1.0] (batched 12s flush + chunking + baseline-delta idempotency), `create_elo_rating_system` ✅ [2.1.0], `create_speedrun_leaderboard` ✅ [2.1.0] (Board2 min-aggregation + friends filter + local-best row), `create_achievement_set` ✅ [2.1.0] (defs + persisted progress + toast HUD), `add_achievement_trigger` ✅ [2.1.0] (trigger zone, once-only latch), `wire_services_stats` (Sum vs LastValue accessor wiring — FOLDED by shipped tools, [2.1.0] triage), `create_stat_tracker` (FOLDED by shipped tools, [2.1.0] triage).

**Round / match / mode flow:** `create_round_phase_manager` (+ vote-to-skip ConCmd), `scaffold_map_vote_flow` ✅ [2.1.0] (vote panel + LCG tie-break + Scene.LoadFromFile), `create_minigame_mode` (SKIPPED [2.1.0] — no game-side Gamemode type to subclass, verified live), `create_round_timer_hud` ✅ [2.1.0] (1 Hz adaptive BuildHash, binds to either round machine), `create_team_assigner` ✅ v2.0.0 (smallest-bucket balanced draft).

**Interaction / use — THEME COMPLETE [2.1.0] (by fold):** `create_interaction_router` (Scene.Trace + Tags dispatch → Razor panel — FOLDED by the shipped interaction family, [2.1.0] triage), `add_interaction_prompt` ✅ v1.20.0 (eye-trace "Press E" HUD bound to `IPressable`), `create_interaction_interface` (`IUse` + PlayerInteractor — FOLDED, [2.1.0] triage), `create_proximity_modal` (Request→Rpc.Host→FilterInclude-confirm — FOLDED, [2.1.0] triage), `create_hold_to_confirm` ✅ v1.20.0 (hold-to-fill bar + `OnConfirmed`).

**Loot / gacha — THEME COMPLETE [2.1.0]:** `create_loot_table_resource` ✅ [2.1.0] ([AssetType] GameResource `.loot` w/ nested tables + depth cap), `create_loot_table_system` (rarity + `NormalizeExpectedValue` EV pass — FOLDED by shipped tools, [2.1.0] triage), `create_gacha_drop_table` ✅ v1.20.0 (per-rarity chance + pity + dup detection, host-auth roll), `create_pity_loot_roll` (per-Connection pity counter — folded into `create_gacha_drop_table` ✅ v1.20.0).

**Vehicles / seats / carry — THEME COMPLETE v2.0.0 wave 4:** `make_drivable`/`add_seat` ✅ (as `create_vehicle_controller` — raycast car + built-in driver seat), `tune_vehicle` ✅ (arcade/drift/offroad/race presets), `create_seat_system` ✅ (host-routed claims + safe-exit), `create_physics_grab_tool` ✅ (physgun-lite, live-physics spring grab), `create_carry_system` ✅ v1.20.0 (rigidbody `MotionEnabled=false` + Pickup/Drop/Throw RPCs + host-routed `Network.AssignOwnership`), `create_physics_grab_tool` (physgun spring + FixedJoint + ownership takeover).

**World / render / atmosphere:** `add_water_body` ✅ [2.1.0] (WaterVolume physics volume + trigger footprint — honest: not the Gerstner `.wtdef` shader; `create_water_profile` remains open), `create_daynight_cycle` (SKIPPED [2.1.0] as a dup — the clock shipped v1.11.0, the visual half is `add_daynight_sun` ✅ [2.1.0]: DirectionalLight arc + gradient + SkyBox2D tint), `add_render_target_camera` ✅ [2.1.0] (covers `create_render_to_texture_screen` — CCTV/portal/mirror), `add_flicker_light` ✅ v1.19.0, `create_camera_shake` ✅ v1.19.0, `create_grass_streamer` (SKIPPED [2.1.0] — GPU-instancing risk), `create_primitive_builder` (SKIPPED [2.1.0] — MeshComponent procedural materials broken, verified live).

**UI / feedback — THEME COMPLETE [2.1.0]:** `override_build_hash` / `add_panel_buildhash` ✅ [2.1.0] (one FILE-EDIT tool covers both — patches an existing `.razor` with a BuildHash override; razor_lint's companion fixer), `create_worldpanel_ui` ✅ v1.20.0 (diegetic clickable WorldPanel + documented `WorldInput` prerequisite), `add_value_floater` / `create_floating_combat_text` / `add_damage_popups` ✅ v1.19.0 (one tool covers all three), `create_combo_meter` ✅ v1.20.0, `create_proxy_nametag` ✅ v1.20.0.

**Networking primitives:** `add_sync_from_host_property`, `add_rate_limited_rpc` (per-Connection cooldown — folded into `create_host_rpc_action` ✅ v1.20.0), `add_targeted_rpc` ✅ v1.20.0 (`Rpc.FilterInclude` single-client side-effect), `create_host_rpc_action` ✅ v1.20.0 (caller-resolve + re-validate + per-SteamId cooldown skeleton), `add_host_migration_recovery` ✅ v1.20.0 (proxy→authority detector + `OnBecameHost` rebuild), `add_network_visible_cull` (SKIPPED [2.1.0] — `INetworkVisible` absent from this SDK, verified live), `create_local_player_resolver` ✅ v1.20.0 (proxy-safe Local, online+offline).

**Other systems:** `create_genetics_system` (Box-Muller gaussian inheritance + mutation — SKIPPED [2.1.0], niche), `create_needs_system` ✅ [2.1.0] (decaying needs → weighted-mean Happiness + OnNeedCritical), `create_utility_ai` ✅ [2.1.0] (self-scoring Action components + hysteresis brain), `create_npc_schedule_brain` ✅ [2.1.0] (daily schedule w/ midnight wrap, clock-capability binding), `create_event_bus` ✅ [2.1.0] (typed local pub/sub), `create_dialog_coroutine_npc` (IEnumerator yield-return lines — superseded by `create_dialogue_system` ✅ v1.20.0, the typewriter Razor-HUD dialogue system), `add_tts_voice` ✅ [2.1.0] (`Sandbox.Speech.Synthesizer`; audio-only by design, viseme data exposed), `create_placement_mode` ✅ v1.13.0 (Sims-style ghost-preview + host re-check + NetworkSpawn), `add_day_night_clock` ✅ v1.11.0 (as `create_day_night_clock`), `create_camera_shake` ✅ v1.19.0.

---

## Notes for whoever builds these
- Model them on the existing scaffold generators (`create_health_system`, `create_pickup`, `create_objective_system`, `create_npc_brain`) in `sbox-bridge-addon/Editor/ScaffoldHandlers.cs` — separate handler file, register in `MyEditorMenu.cs`, TS tool in `sbox-mcp-server/src/tools/`.
- **ALWAYS live-compile-verify the generated
