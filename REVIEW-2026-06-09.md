# Bridge Review — 2026-06-09 (post-v1.11.0)

Full health check + tool-gap analysis across all three parts: the bridge (GitHub repo), the plugin brain (sbox-cookbook), and the npm server. Verdict up front: **nothing is broken, v1.11.0 shipped everywhere, and three high-value tools are now verified-unblocked and ready to build.**

---

## 1. Health check — is anything broken?

**No.** Every layer verified:

| Layer | State | Evidence |
|---|---|---|
| Git repo | Clean, pushed | `7bfba81` (v1.11.0) on `origin/main`, 0 ahead/0 behind at review start. The 54 "modified" files were pure CRLF line-ending noise (confirmed via `--ignore-cr-at-eol` — zero real diff). |
| npm | **1.11.0 published** | `sbox-mcp-server@1.11.0`, published 2026-06-09 21:59 UTC. |
| Plugin manifest | 1.11.0 | `plugins/sbox-claude/.claude-plugin/plugin.json`. |
| Addon | 1.11.0 | `BridgeVersion = "1.11.0"` in `MyEditorMenu.cs`; live bridge reported addon v1.11.0, 173 handlers, heartbeat OK. |
| Compile | Clean | `tsc` builds with no errors; `get_compile_errors` against the live editor log: all clear. |

**Wiring audit (TS ↔ C# parity):**
- 182 TS `server.tool()` registrations · 173 C# `Register()` handlers · **zero duplicates on either side**.
- Every `bridge.send` command has a C# handler (`get_bridge_status` is special-cased at MyEditorMenu.cs:638 — correct).
- Zero dead C# handlers (every one is reachable from TS).
- 8 TS-only tools, all by design (MCP-server-side): `read_log`, `get_compile_errors`, `execute_csharp`*, `run_self_test`, `screenshot_orbit`, `search_docs`, `get_doc_page`, `list_doc_categories`. (*composes other commands.)

**Two findings, neither a code bug:**
1. **Session MCP server was stale.** The live bridge reported addon v1.11.0 but MCP server v1.8.0 (`versionsAligned: false`). The repo and npm are correct — the *running* plugin instance predates the publish. Fix: restart Claude Code / Cowork so the plugin picks up 1.11.0.
2. **Power cut left a stale `.git/index.lock`** — removed; repo fine. s&box was down at review end (post-outage) — restart the editor and the bridge will reconnect.

---

## 2. Graphify — refreshed and what it says

The bridge map was **5 commits stale** (built from `fa92605`, pre-v1.10.0-release). Ran the deterministic code/AST refresh (`graphify update . --force`, no LLM, per `scripts/regen-graph.ps1`) and copied artifacts into `docs/graph/`:

- Now built from **`7bfba815` (HEAD)** — **2,838 nodes · 3,758 edges · 147 communities** (was 2,461 / 3,360 / 149).
- Committed as `8d67e5f` — **1 commit ahead, needs push.**

What the graph confirms about architecture health:
- **`IBridgeHandler` is still the spine** — 176 edges, highest-betweenness cross-community node. Every new handler (DayNightClock, RoundPhaseMachine, EconomyWallet, InvokeMethod, EnsureInputAction, DrivePlayer) extracted as implementing it. No drift.
- **Zero import cycles.**
- The scaffold family clusters cleanly (community 81): all 6 `Create*Handler` scaffolds + `ScaffoldHelpers` together — the scaffold pattern is cohesive, which is why each new one is cheap.
- Caveat unchanged: this was the **code/AST pass only**. The full doc-inclusive semantic re-pass still needs a Gemini key.

---

## 3. Tool-gap analysis — "have we built the best available bridge tools?"

**Coverage today is genuinely broad**: scene authoring, visuals/atmosphere, characters, level tools, physics, navmesh, networking lint/inspection, self-diagnosis (log + compile errors), docs search, play-mode driving, and 6 gameplay scaffolds. The corpus mining (51 games → 352 candidates → 188 ship-worthy) is the right grounding for what's *left*, and the backlog is honest about it.

**The big unlock from this review:** the three tools previously parked as "needs attended API-verification" are now **verified live via `describe_type`** — the blocker is gone:

| Tool | Demand | Risk | API verified |
|---|---|---|---|
| `create_interactable` | 2x + interaction appears in ~every genre recipe | easy | `Component.IPressable` full surface: `Press(Event)→bool`, `Pressing`, `Release`, `Look`, `Hover`, `Blur`, `CanPress`, `GetTooltip→string?`. Exactly matches the cookbook's interaction recipes. |
| `create_weighted_loot_table` | 2x + entire loot/gacha cookbook section | easy | `System.Random` present in the sandbox TypeLibrary (`Game.Random` instance). Cumulative-weight picker needs nothing else. |
| `create_save_system` | **7x — the #1 ask in the whole backlog** | medium | `BaseFileSystem.ReadJson<T>(file, default)`, `ReadJsonOrDefault<T>`, `WriteJson<T>` all confirmed — the exact trio the versioned-save scaffold needs. |

**Recommended build order (demand × risk × cookbook grounding):**
1. `create_interactable` — easy, universal verb, IPressable fully mapped.
2. `create_weighted_loot_table` — easy, pairs with `create_pickup` + economy wallet; the gacha/loot recipes give the exact canonical pick to generate.
3. `create_save_system` — highest demand, medium risk; the persistence cookbook ref (versioned DTO + clamp-on-load + atomic write) is the spec.
4. `create_leaderboard_panel` (2x, easy) — first Razor-generating scaffold; cookbook's Services-leaderboard ref grounds it.

After those four, the **"game director" trio becomes a "game spine" seven**: currency + round-flow + time + interaction + loot + save + leaderboard — which covers the system stack of nearly every genre recipe in the cookbook. `create_round_state_machine` (5x) is partially served by `create_round_phase_machine` already; the full named-state-class variant is the only Tier-1 item I'd still defer.

The remaining ~180 long-tail candidates stay queued in `docs/TOOL_BACKLOG.md` because their per-game research grounding is intentionally kept outside the distributable repository.

---

## 4. Your move

1. **`git push`** — one commit (`8d67e5f`, graph refresh) is ahead.
2. **Restart s&box** (down since the power cut) and **restart Claude Code/Cowork** to align the MCP server to 1.11.0.
3. Say the word and I build the four tools above, verify-gated the same way as the director trio.
4. Optional: set `GEMINI_API_KEY` and re-run `/graphify` for the full semantic map.
